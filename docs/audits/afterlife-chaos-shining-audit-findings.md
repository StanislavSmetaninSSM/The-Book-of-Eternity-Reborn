# Afterlife Chaos Sea / Shining Abode Audit Findings

Tracking issues: #470, #471, #472, #473, #474, #475, #476, #477, #478, #479, #480, #481, #482, #483, #484, #485, #486.

## Rules For This File

- Record every finding before implementing the fix.
- Include source files, affected realm, severity, reproduction or mental experiment, expected behavior, and proposed fix.
- If a finding is too large for the current task, create a separate GitHub issue and link it here.
- Mark status as `open`, `fixed`, `split`, or `wontfix`.

## Findings

| ID | Status | Issue | Realm | Severity | Summary | Source / Evidence | Proposed Fix |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AFT-001 | fixed | #477 | Chaos Sea / Shining Abode | P2 | Afterlife example coverage ignored examples 24-26 because the test only parsed `N. VALID` headings; `EXAMPLE 24-26` could drift without runtime coverage or exemption. | `Examples/E_CLI_Afterlife_Turns.txt` has `EXAMPLE 24`, `EXAMPLE 25`, `EXAMPLE 26`; the previous test only matched `^(\\d+)\\. VALID` and expected 1..23; `example_validation_manifest.json.afterlifeExampleCoverage` ended at 23. | Fixed by parsing both heading styles, expecting examples 1..26, and adding manifest coverage/exemptions for 24-26. Verification: `dotnet test ... --filter "FullyQualifiedName~AfterlifeWorkedExamplesHaveRuntimeScenarioOrExplicitCoverageExemption"` passed 1/1. |
| AFT-002 | fixed | #477 | Chaos Sea / Shining Abode | P3 | The afterlife examples index and daemon coverage wording did not route readers to all newer examples: the index skipped example 24 and example 26, while daemon docs/tests still mentioned `examples 14-25` as the range. | `Examples/E_CLI_Afterlife_Turns.txt` index listed 23 then 25; `CLI_Agent_Daemon_Specification.md` said `examples 14-25` and separately mentioned 26; coverage tests asserted the stale range. | Fixed by adding index bullets for examples 24 and 26 and updating daemon wording/tests to `examples 14-26`. Verification: `dotnet test ... --filter "AfterlifeDocumentationCoverageTests"` passed 63/63. |
| AFT-003 | fixed | #470 | Shining Abode | P2 | Source of Light pending validation treated valid empty `requests: []` hygiene files as active/malformed pending contracts, so stale empty NPC/social/trade wrappers could block Source even though lifecycle cleanup treats them as empty. | `SourceOfLightCapstoneState.TryDescribeBlockingPendingContractAsync` checked `FileExists` for `pending_npc_social_interactions.json`, `pending_npc_trade_inventory_requests.json`, and other requests-bundle pending files. Regression test failed with `source_of_light_pending_blocked_by_other_contract` for empty `requests[]`. | Fixed by classifying requests-bundle pending files by `requests[]` count: missing/empty array is non-blocking, non-empty or malformed remains blocking. Verification: `dotnet test ... --filter "FullyQualifiedName~ValidateGameStateAsync_SourceOfLightPendingWithEmptyRequestsHygieneFiles_DoesNotReportOtherContractBlocker"` passed 1/1. |
| AFT-004 | fixed | #470 | Shining Abode | P3 | Return-to-Chaos-Sea documentation listed Shining pending blockers but omitted `pending_source_of_light_capstone.json`, even though runtime blocks return while that direct Source pending contract is unresolved. | `CLI_Agent_Daemon_Specification.md`, `CLI_API_Specification.md`, `Afterlife_Contract_Matrix.md`, and example 21 return notes listed core/trade/politics/legacy blockers only. Coverage test failed because these `return_to_chaos_sea` sections lacked `pending_source_of_light_capstone.json`. | Fixed by adding Source pending to daemon, API, matrix, examples, and launcher local-lifecycle wording. Verification: `dotnet test ... --filter "FullyQualifiedName~AfterlifeLocalLifecycleRoutesAreDocumented"` passed 1/1. |
| AFT-005 | fixed | #470 | Shining Abode | P3 | Shining pending-bootstrap handoff documentation said Soul Gates require no unresolved contracts, but explicit blocker lists omitted Source of Light pending requests. | Handoff sections in `CLI_API_Specification.md`, `Afterlife_Contract_Matrix.md`, launcher prompt text, generated launcher template, and example 3 listed Guardian/resident/archive/offering/Shining blockers but not `pending_source_of_light_capstone.json`. Coverage failed in `AfterlifePromptDocsDescribeHandoffAsTriggerOnlyAndFileLevelForbiddenRule`. | Fixed by adding Source pending to handoff blocker wording and extending documentation coverage to matrix/API/launcher/generated launcher/worked example handoff sections. Verification: `dotnet test ... --filter "FullyQualifiedName~AfterlifePromptDocsDescribeHandoffAsTriggerOnlyAndFileLevelForbiddenRule"` passed 1/1. |
| AFT-006 | fixed | #471 | Chaos Sea | P2 | The live GM reminder for deterministic Eternal Guardian attraction explained the fiction but omitted the canonical closure surfaces, so a GM could materialize the Guardian narratively while missing `UpdateGuardians`, `activeGuardian`, or `chaosSeaNavigation`. | `SystemGuardianLibraryService.BuildReminderFragmentAsync("Chaos Sea")` mentioned create/route but not `UpdateGuardians` or `chaosSeaNavigation`, while the matrix row for `system_guardian_attraction.json` requires exact Guardian and navigation state. Regression test failed on missing `UpdateGuardians`. | Fixed by adding canonical closure surfaces to the attraction reminder and covering them in `BuildReminderFragmentAsync_ChaosSeaKeepsAttractionClosureInstructions`. Verification: `dotnet test ... --filter "FullyQualifiedName~BuildReminderFragmentAsync_ChaosSeaKeepsAttractionClosureInstructions"` passed 1/1. |
| AFT-007 | fixed | #472 | Chaos Sea / afterlife entity profiles | P2 | Chaos Sea entity profiles could store positive Light Sparks even though Chaos profile economy is Ink Feather-only. | Source: GM-authored `afterlifeEntityProfileUpdates[]`, full canonical `profiles[]`, or `afterlifeEntityProgressionOverrides[].currencyDeltas`; sink: `profiles[].currencies.lightSparks`; authority owner: afterlife entity profile contract. Pre/post defect: a Chaos Sea profile could validate while moving from `currencies.lightSparks = 0` to a positive value such as `3`. | Fixed by rejecting positive `currencies.lightSparks` on `Chaos Sea` / `Море Хаоса` profiles, documenting the invariant in GM-facing contracts, and adding profile/documentation coverage. |
| AFT-008 | fixed | #473 | Mortal World / afterlife authority | P2 | Mortal World realm segregation blocked `guardians.json`, spiritual conflict, entity profiles, and Chaos lore, but missed other Guardian/afterlife authority files. | Source: same-turn GM/direct state write during a validated Mortal World turn; sinks: `guardian_abode_residents.json`, guardian thought/social/project journals, `guardian_projects.json`, and `abode_power_journal.json`; authority owner: afterlife Guardian/Abode systems. Expected diff rule: Mortal World may only use `guardianQuestProgressUpdates[]` for accepted active quest progress; all other Guardian/Abode authority remains byte-equivalent to the validated pre-turn snapshot. | Fixed by extending `IsForbiddenMortalWorldChangedFile`, documenting the direct-write ban in live/docs prompts, and adding realm segregation regression coverage for the missed files. |
| AFT-009 | fixed | #474 | Shining Abode | P2 | Verified Shining scheduler/catch-up processing disabled strict Shining closure diffs too broadly, allowing unrelated Shining surfaces to change during pending contract closure. | Source: accepted Shining founding/realignment/leadership/trade/core closure with valid `progressionControl` and `progressionProcessingReport`; sink: unrelated `shining_abode_state.json` surfaces such as `coreActionReceipts[]`, `lightSparks`, `treasury`, or `sourceOfLightCapstone`; expected behavior: scheduler deltas are allowed only for scheduler-owned Shining/resident/trade progression fields and do not authorize unrelated GM-authored mutations. | Fixed by checking forbidden progression-delta paths even when scheduler deltas are verified, adding a regression test for a verified scheduler + unrelated core receipt/light spark mutation, and documenting the narrow scheduler allowance. |
| AFT-010 | fixed | #480 | Shining Abode / GM prompts | P3 | The narrow Shining scheduler-allowance rule was documented in matrix/API/daemon/examples, but not in `TaskGuides/CLI_Step_Main.txt` or `Rules/Block_CLI_Operations.txt`, so two GM-facing entrypoints could still imply scheduler work permits unrelated Shining mutations. | Source: prompt entrypoint audit for scheduler + pending Shining closures; missing terms `scheduler-owned`, `gachaSystem.gachaHistory`, `treasury`, and `sourceOfLightCapstone` in TaskGuide/Rules; expected behavior: every GM-facing prompt entrypoint warns that scheduler proof allows only scheduler-owned progression fields. | Fixed by adding the rule to TaskGuide and Rules and extending `ShiningClosureCompositeDiffRulesAreDocumentedForGm` to cover both files. |
| AFT-011 | fixed | #475 | Chaos Sea / afterlife entity profiles | P2 | Entity auto-progression could consume a stale or wrong-turn `progression_report.json` before scheduler validation rejected it, causing profile income/upgrades to be projected from an unrelated turn. | `CanonicalStateNormalizer.AfterlifeEntityProfiles.NormalizeAfterlifeEntityProfilesAsync` read `ProgressionScheduleService.ReportPath` and passed it directly to `AfterlifeEntityProfileState.ProjectCanonicalRoot`; `ApplyAutomaticProgression` used processed cycle counts/ordinals without checking the current `input/turn_request.json` / repair context. Regression test: stale report with mismatched `sessionId/requestId/turnNumber` advanced `standardArts.guard` from 0 to 1 before the fix. | Fixed by filtering the report in the normalizer: entity profile projection only receives reports whose `sessionId`, `requestId`, and `turnNumber` match the current turn context. Verification: stale-report regression passed, and `CanonicalStateNormalizerTests|ProgressionScheduleServiceTests` passed 183/183. |
| AFT-012 | fixed | #481 | Shining Abode / local economy | P2 | Local Shining trade purchases could mutate balances and trade inventory while a GM turn lifecycle was still active. | Source command: local Shining trade buy (`ShiningTradeService.BuyAsync`); pre-turn state: `input/turn_request.json` or `game_state/control/pending_turn_snapshot(.json)` exists; mutation path: `soul_state.json.inkFeathers`, `soulRelics.stored[]`, and `shining_abode_state.json.factions[].tradeInventory.items[].soldOut`; expected behavior: local economy writes must wait until the GM turn is accepted, cancelled, or repaired. | Fixed by adding an early active-GM-turn blocker before any purchase mutation and by ignoring stale empty snapshot directories. Regression: `BuyAsync_BlocksActiveGmTurnLifecycleWithoutMutatingState`. |
| AFT-013 | fixed | #481 | Shining Abode / local economy | P2 | Local Shining trade purchases could spend Feathers while unresolved Shining cost contracts already reserved balances. | Source command: local Shining trade buy; pre-turn balance state: a pending core action, founding request, or legacy native-faction discovery has a quoted/reserved Feather/Spark cost; mutation path: purchase deducts Ink Feathers and marks a trade slot sold out before the pending contract closes; expected behavior: cost-bearing pending contracts block local purchases so accepted closures see the same reserved balance. | Fixed by blocking purchases while malformed/cost-bearing core actions, pending founding requests, or legacy native discovery contracts are unresolved. Regression: `BuyAsync_BlocksUnresolvedShiningCostContractsWithoutMutatingState`. |
| AFT-014 | fixed | #481 | Shining Abode / local UI mutations | P2 | Player-facing Shining local commands could create pending requests or mutate Shining state while an accepted GM turn was still active after restart/late response. | Source commands: Shining Gates local saves, project completion/support/retirement, founding/realignment/leadership requests, trade inventory requests, relic gacha, and forge requests; pre-turn state: `input/turn_request.json` or non-empty pending snapshot artifacts; mutation paths include `pending_shining_abode_actions.json`, Shining political pending files, `pending_shining_trade_inventory_requests.json`, `shining_abode_state.json`, and `soul_state.json`. | Fixed with shared `AfterlifeLocalActionGuard` and wired it into Shining UI entrypoints plus trade service. Regression: `AfterlifeLocalActionGuardTests` and `ShiningLocalMutationCommands_MustUseActiveGmTurnBlocker`. |
| AFT-015 | fixed | #487 | Mortal World / Guardian quest progress tests | P2 | Guardian quest progress regression fixtures built a pre-turn baseline with empty `guardian_projects.json` and `abode_power_journal.json` but did not keep current and snapshot payloads byte-identical, so full-suite validation reported a false `realm_segregation_violation`. | Full suite failures: `ValidateGameState_MortalGuardianQuestProgressReadyToTurnInUsesNonPhysicalEvidence` and `ValidateGameState_MortalGuardianQuestProgressPartialCommandPreservesUnchangedFields`; validation saw `guardian_projects.json` and `abode_power_journal.json` as forbidden Mortal World changes unrelated to `guardianQuestProgressUpdates[]` because the current files were copied/stale or differed only by canonical fields/newlines. | Fixed by synchronizing current empty project tracker and abode power journal fixtures from shared canonical payload constants before registering the validated pre-turn snapshot. Regression: the two failing Guardian tests. |
| AFT-016 | fixed | #482 | Chaos Sea / Soul Gates blockers | P3 | Soul Gates treated an explicit-empty `pending_guardian_social_interactions.json` wrapper as an unresolved Guardian-social contract, unlike other requests-bundle hygiene files. | Reproduction: write `game_state/control/pending_guardian_social_interactions.json` as `{ "requests": [] }` and call `CollectIncarnationBlockersAsync`; actual blocker: `pending_guardian_social_interactions.json`; expected behavior: empty requests-bundle is not actionable and should be deleted like empty NPC social/trade and Shining requests wrappers. | Fixed by clearing non-malformed empty Guardian-social request bundles inside `CollectIncarnationBlockersAsync`; malformed and non-empty files still block. Regression: `CollectIncarnationBlockersAsync_EmptyGuardianSocialRequestsDoNotBlockSoulGates`. |
| AFT-017 | fixed | #482 | Chaos Sea / Soul Gates blockers | P3 | Soul Gates treated explicit-empty resident roster/interactions/transfers request bundles as unresolved afterlife contracts. | Reproduction: write any of `pending_guardian_abode_residents_request.json`, `pending_guardian_abode_resident_interactions.json`, or `pending_guardian_abode_resident_transfers.json` as `{ "requests": [] }` and call `CollectIncarnationBlockersAsync`; each path blocks despite having no actionable request. | Fixed by clearing non-malformed empty resident request bundles inside `CollectIncarnationBlockersAsync`; malformed and non-empty files still block. Regression: `CollectIncarnationBlockersAsync_EmptyResidentRequestBundlesDoNotBlockSoulGates`. |
| AFT-018 | fixed | #486 | Chaos Sea / Shining Abode snapshot validation | P2 | Generic tracked-file validation did not classify every specialized afterlife client-owned pending/control contract as client-owned, so some pending files could be double-counted as ordinary tracked state and surface noisy or misleading baseline/realm errors instead of only the dedicated client-owned contract error. | File paths: `pending_player_guardian_foundation.json`, `pending_npc_trade_inventory_requests.json`, `pending_guardian_abode_resident_manifestations.json`, `pending_npc_social_interactions.json`, and `pending_source_of_light_capstone.json`; lifecycle phase: accepted-turn snapshot validation; baseline expectation: these files are handled by the client-owned contract validator, not generic realm/tracked-file diffing. | Fixed by extending `IsClientOwnedSurfaceValidationPath` to cover all afterlife pending paths validated by `ValidateClientOwnedControlFilesAsync`. Regression: `ClientOwnedSurfaceFilter_MustCoverAllValidatedAfterlifePendingContracts`. |
| AFT-019 | fixed | #486 | Chaos Sea / Shining Abode snapshot creation | P2 | `CreateCanonicalBaselineSnapshotAsync` registered absent optional afterlife pending request files in `rollbackBaselineFiles`, so a normal turn with no such pending contract could later treat the absent file as a missing validated baseline. | File paths: offering/trade/NPC trade/Shining core/trade/resident/social/archive pending request files; lifecycle phase: pending-turn snapshot creation; baseline expectation: only files that existed before the turn are rollback baselines, while GM-created client-owned pending files are still rejected by the client-owned validator because current content is non-empty without a validated snapshot. | Fixed by adding optional afterlife pending files to rollback baselines only when the file exists before snapshot creation. Regression: `CreateCanonicalBaselineSnapshotAsync_AbsentAfterlifePendingContracts_AreNotRollbackBaseline`. |

## Audit Checkpoints

| Date | Issue | Scope | Result | Verification |
| --- | --- | --- | --- | --- |
| 2026-05-19 | #478 | Source of Light capstone, Soul Gates blockers, `return_to_chaos_sea`, `reenter_shining_abode`, Shining pending-bootstrap `TriggerIncarnation`, pending-file cleanup, reward tuple trust, and rollback/snapshot handoff coverage. | No additional discrete handoff/rollback defect found after AFT-018/AFT-019. Runtime blockers, Source pending context, Shining pending-bootstrap docs, and lifecycle validation are covered by existing targeted tests. | `dotnet test BookOfEternityClient.Tests\BookOfEternityClient.Tests.csproj --no-restore --filter "SourceOfLightCapstoneValidationTests|GameEngineTurnLifecycleTests|AfterlifeDocumentationCoverageTests"` passed 146/146. |
