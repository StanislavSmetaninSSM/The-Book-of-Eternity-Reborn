CLI STEP MAIN — EXTENDED EXAMPLE FOR VALIDATION, NPC SCOPE, AND REPAIR LOOP

This file supplements TaskGuides/CLI_Step_Main.txt.
Read BOTH files before EVERY turn and reread them before EVERY repair attempt and before handling any terminal protocol failure notice.

JSON/PowerShell reminder:
- write JSON/state files in UTF-8 explicitly
- use `[ordered]@{}` and `@()` for data objects
- never use `{ ... }` where you mean JSON data
- use actual paragraph breaks in player-facing strings; Never place literal PowerShell newline tokens such as backtick+n or backtick+r+backtick+n in `response`, dialogue `text`, or `inputValue`
- if JSON suddenly contains `Ast`, `StartPosition`, `Extent`, or `PipelineElements`, you serialized a PowerShell runtime object instead of game data

Safe player-facing paragraph example:

```powershell
$narrative = [ordered]@{
    response = @'
Первый абзац сцены.

Второй абзац сцены с настоящим переносом строки.
'@
    timestamp = [DateTimeOffset]::UtcNow.ToString('O')
}
Write-BoeJson -RelativePath 'output/narrative_response.json' -Data $narrative
```

Faction Materialization worked-example coverage:
mortal_faction_materialization_populated_creation_v1
mortal_faction_materialization_seven_empty_creation_v1
mortal_faction_materialization_legacy_promotion_v1
mortal_faction_core_changes_update_v1
mortal_faction_materialization_repair_v1

### mortal_faction_materialization_legacy_promotion_v1

This legacy faction is promoted through the full `factionDataChanges` carrier in the same accepted turn that it first gains a formal project and resource ledger.

```json
{
  "factionDataChanges": [
    {
      "factionId": "faction_old_ferry_circle",
      "name": "Старый перевозный круг",
      "purpose": "Сохранять безопасную переправу через Серую реку.",
      "currentAgenda": "Собрать запас дерева для весеннего ремонта.",
      "principles": ["Никого не бросать на воде"],
      "memory": {
        "summary": "Прежде круг существовал только как местное прозвище.",
        "lastUpdatedTurn": 27,
        "enduringFacts": ["Перевозчики отвечают на три колокольных удара."],
        "openThreads": ["Кто будет хранить общий фонд?"]
      },
      "governance": {"model": "совет перевозчиков", "decisionProcess": "Единогласие дежурных капитанов."},
      "leadership": {"leadershipState": "collective", "summary": "Два дежурных капитана.", "leaderNpcIds": ["npc_ferry_ada", "npc_ferry_borin"]},
      "hierarchy": [{"branchId": "core", "name": "Дежурные капитаны"}],
      "resources": {"timber": 8},
      "relations": [],
      "projects": [{"projectId": "project_repair_ferry", "status": "active"}],
      "territoryAndInfluence": [],
      "playerMembership": {},
      "customStates": [],
      "materialization": {
        "schemaVersion": 1,
        "materializationId": "mat_faction_old_ferry_circle_turn_27",
        "factionType": "mortal_faction",
        "factionId": "faction_old_ferry_circle",
        "materializedAtTurn": 27,
        "state": "complete",
        "capabilities": {
          "hasFormalHierarchy": true,
          "usesFactionResources": true,
          "maintainsRelations": false,
          "runsProjects": true,
          "holdsTerritoryOrInfluence": false,
          "supportsPlayerMembership": false,
          "usesCustomMechanics": false
        },
        "sections": {
          "hierarchy": {"state": "populated"},
          "resources": {"state": "populated"},
          "relations": {"state": "empty_by_design", "reason": "Круг ещё не заключал соглашений с другими фракциями."},
          "projects": {"state": "populated"},
          "territoryAndInfluence": {"state": "empty_by_design", "reason": "Круг обслуживает путь, но не заявляет контроль над ним."},
          "playerMembership": {"state": "empty_by_design", "reason": "Вступление пока определяется только личным доверием."},
          "customStates": {"state": "empty_by_design", "reason": "Отдельной механики круга пока нет."}
        }
      }
    }
  ]
}
```

### mortal_faction_core_changes_update_v1

An ordinary update preserves historical materialization and sends only bounded semantic groups. `scribeChronicle` is extracted to the dedicated chronicle surface by the client contract.

```json
{
  "factionCoreChanges": [
    {
      "factionId": "faction_old_ferry_circle",
      "reason": "Совет завершил сбор древесины и выбрал следующую цель.",
      "purposeAndPrinciples": {
        "currentAgenda": "Укрепить восточную опору до половодья."
      },
      "progressionAndPower": {
        "progressionSummary": "Подготовка перешла от сбора материалов к строительству."
      },
      "governanceAndLeadership": {
        "leadershipState": "collective",
        "summary": "Два капитана продолжают совместное руководство."
      },
      "scribeChronicle": {
        "summary": "Общий запас дерева собран."
      }
    }
  ]
}
```

==================================================
1. SCENE-LOCAL TURN WITH NPC SCOPE
==================================================

Example gm_thoughts_markdown:

## Оценка времени и мира
- Прошло игрового времени: 15 минут
- Логические последствия: Существенных мировых или фракционных последствий нет
- Решение о прогрессии: Прогрессия мира и фракций не требуется

## Охват NPC-анализа
- Режим: Scene-local
- Релевантные акторы: Марта, Феликс
- Почему они релевантны: Оба физически присутствуют в сцене и непосредственно реагируют на действие игрока
- Акторы вне охвата: Капитан стражи, Совет ремесленников
- Почему они вне охвата: Локальная сцена не затрагивает их решения и не запускает off-screen последствия

## Reasoning / Размышления NPC
### Марта
- Текущая локация: Рыночная площадь. Она остаётся в лавке и не покидает сцену в этом ходу.
- Ситуация: Она видит, что игрок просит у неё помощь при свидетеле
- Мысли: Она хочет помочь, но боится потерять репутацию у хозяина лавки
- Действия: Она отвечает осторожно, даёт неполную информацию и наблюдает за реакцией Феликса

### Феликс
- Текущая локация: Рыночная площадь. Он уже находится рядом с Мартой и остаётся в этой же сцене.
- Ситуация: Он замечает, что игрок пытается обойти его через Марту
- Мысли: Он не хочет терять контроль над ситуацией и подозревает скрытый мотив
- Действия: Он вмешивается в разговор и переводит инициативу на себя

## Логика прогрессии
- Существенных off-screen обновлений не требуется

Prose State Delta Rationale / связь прозы и состояния:
- Если player-facing response говорит, что известный навык помог, сработал или
  раскрыл деталь, accepted turn должен записать это в канон: например
  `skillMasteryChanges`, `activeSkillChanges` или `passiveSkillChanges`.
- Если response говорит, что по активному квесту найдена новая зацепка,
  accepted turn должен обновить `detailsLog`, objective/status,
  `lastUpdatedTurn`, `quest_history.json` или `plot_outline.json`.
- Если прогресса намеренно нет, не стирай полезную прозу. Добавь в
  `output/debug_logs.json.gm_thoughts_markdown` раздел
  `## Prose State Delta Rationale` с именем навыка или названием квеста и
  clear no-progress rationale.
- Иначе валидатор вернет `accepted_turn_skill_claim_missing_state_delta` или
  `accepted_turn_quest_clue_missing_state_delta`.

Пример реального delta:

```json
{
  "skillMasteryChanges": [
    {
      "skillId": "skill_life_001_seal_reading",
      "skillName": "Чтение печатей",
      "newCurrentMasteryProgress": 1,
      "newMasteryProgressNeeded": 100,
      "masteryLeveledUp": false,
      "reason": "Навык помог заметить поддельную зарубку на контрактной печати."
    }
  ],
  "UpdateQuests": [
    {
      "questId": "quest_life_001_opening_hook",
      "questName": "Печать Серебряной Луны",
      "lastUpdatedTurn": 4,
      "newDetailsLogEntry": "#[4]. Чтение печатей выявило смещенную свидетельскую зарубку на контракте."
    }
  ]
}
```

Пример допустимого no-progress rationale:

```markdown
## Prose State Delta Rationale
- skill: Чтение печатей - no-progress rationale: сцена только перечисляет уже известные ориентиры, новый навык не применялся.
- quest: Печать Серебряной Луны - no-progress rationale: название дела упомянуто как контекст, новой зацепки или стадии квеста нет.
```

==================================================
2. WORLD-PROGRESSION TURN WITH OFF-SCREEN NPCS
==================================================

## Оценка времени и мира
- Прошло игрового времени: 8 часов
- Логические последствия: Несколько активностей НПС и один фракционный проект должны продвинуться
- Решение о прогрессии: Нужны world/faction/NPC updates

## Охват NPC-анализа
- Режим: World-progression
- Релевантные акторы: Лорд Хавьер, Интендант Ирис, Капитан Рувен
- Почему они релевантны: Их решения напрямую определяют реакцию гарнизона, снабжения и текущего фракционного проекта
- Акторы вне охвата: Бармен Йорик, Рыночные торговцы
- Почему они вне охвата: Этот цикл не меняет их состояние и не создаёт для них новых решений

## Reasoning / Размышления NPC
### Лорд Хавьер
- Текущая локация: Крепость гарнизона. Он остаётся в штабе и координирует решения оттуда.
- Ситуация: Он получил отчёт о нехватке провизии и угрозе с южной дороги
- Мысли: Нельзя показывать слабость гарнизона, но и игнорировать снабжение опасно
- Действия: Он перераспределяет охрану, ускоряет переговоры с Ирис и отменяет второстепенные поставки

### Интендант Ирис
- Текущая локация: Склады гарнизона. Она остаётся у складов и работает с поставщиками на месте.
- Ситуация: Она видит дефицит ресурсов и давление со стороны Хавьера
- Мысли: Ей нужно сохранить склады и не допустить паники среди поставщиков
- Действия: Она урезает выдачу припасов и запускает срочную закупку по повышенной цене

### Капитан Рувен
- Текущая локация: Южная дорога у караванного маршрута. Он смещается ближе к зоне риска и усиливает патрули там.
- Ситуация: На нём лежит ответственность за безопасность караванного пути
- Мысли: Он считает южную дорогу уязвимой и ждёт нападения
- Действия: Он усиливает патрули и меняет маршрут сопровождения

## Логика прогрессии
- Фракционный проект снабжения продвинулся
- Активности гарнизона и снабжения обновлены

==================================================
3. GUARDIAN-CENTRIC TURN
==================================================

## Оценка времени и мира
- Прошло игрового времени: один hub-cycle Моря Хаоса
- Логические последствия: Нужно оценить настроение и проект активного Хранителя
- Решение о прогрессии: Guardian/hub processing требуется

## Охват NPC-анализа
- Режим: Guardian-centric
- Релевантные акторы: Аэлион
- Почему они релевантны: Ход целиком сосредоточен на прибытии в его Обитель и его реакции
- Акторы вне охвата: Прочие Хранители
- Почему они вне охвата: В этом ходе они не взаимодействуют с душой и не влияют на результат сцены

## Guardian Thoughts
### Аэлион
- Ситуация: Душа прибыла в его Обитель после долгого отсутствия
- Мысли: Он хочет понять, вернулась ли душа ради знаний или ради выгоды
- Действия: Он приветствует душу сдержанно, проверяет её настрой и открывает разговор на своих условиях

==================================================
4. GUARDIAN GACHA CHARGES PER RETURN
==================================================

Guardian-mediated gacha is limited per Guardian per return from mortal life:
- Hostile(-100..-51): 0 attempts
- Wary/Neutral(-50..49): 1 attempt
- Friendly(50..129): 2 attempts
- Devoted/Legendary(130..300): 3 attempts

Direct `/gacha` remains neutral and does NOT consume Guardian charges.

Valid guardian state fragment:

```json
{
  "guardianId": "guard_magic_aelion_001",
  "name": "Аэлион",
  "relationshipData": {
    "currentReputation": 142
  },
  "gachaSystem": {
    "chargesPerReturn": 3,
    "chargesUsedThisReturn": 1,
    "gachaHistory": [
      {
        "timestamp": "2026-03-09T18:42:00Z",
        "costInFeathers": 120,
        "relicId": "sr_magic_aelion_starglass_001",
        "finalRarity": "Rare"
      }
    ]
  }
}
```

This means:
- Guardian-mediated pull is still allowed
- 2 charges remain for this return
- after the next successful `UpdateGuardians.processGacha`, used charges become `2`
- canonical guardian history stores compact result entries: `timestamp`, `costInFeathers`, `relicId`, `finalRarity`

If `chargesUsedThisReturn` already equals `chargesPerReturn`, do NOT emit `processGacha` for that Guardian.

==================================================
4.5 MEMORY GATES — VALID MECHANICAL REWARD
==================================================

`Memory Gates` is not a lore-only action. It MUST create one pending Memory Legacy for the next mortal life.

Valid `metaStateUpdates.memoryLegacyGrant` example:

```json
{
  "metaStateUpdates": {
    "memoryLegacyGrant": {
      "legacyId": "memory_legacy_scholar_001",
      "legacyType": "startingCharacteristicBonus",
      "sourceLifeHint": "Эхо жизни мудреца, посвятившего десятилетия поиску истины.",
      "characteristic": "intelligence",
      "bonus": 2
    }
  }
}
```

Resulting canonical `soul_state.json` fragment:

```json
{
  "pendingMemoryLegacy": {
    "legacyId": "memory_legacy_scholar_001",
    "sourceLifeHint": "Эхо жизни мудреца, посвятившего десятилетия поиску истины.",
    "legacyType": "startingCharacteristicBonus",
    "grantSource": "memoryLegacyGrant",
    "grantSnapshot": {
      "legacyId": "memory_legacy_scholar_001",
      "legacyType": "startingCharacteristicBonus",
      "sourceLifeHint": "Эхо жизни мудреца, посвятившего десятилетия поиску истины.",
      "characteristic": "intelligence",
      "bonus": 2
    },
    "applicationState": "pending",
    "grantedAtUtc": "2026-03-11T04:15:00Z",
    "characteristic": "intelligence",
    "bonus": 2
  }
}
```

Alternative valid skill-based grant:

```json
{
  "metaStateUpdates": {
    "memoryLegacyGrant": {
      "legacyId": "memory_legacy_archivist_002",
      "legacyType": "startingPassiveKnowledgeSkill",
      "sourceLifeHint": "Память архивариуса, запоминавшего забытые языки и тайные родословные.",
      "skillName": "Echo of Lost Archives",
      "skillDescription": "Отголоски прошлой жизни помогают тебе быстрее узнавать скрытые связи, древние тексты и забытые факты.",
      "group": "Knowledge",
      "playerStatBonus": "+1 к проверкам знания и исследования древних текстов",
      "structuredBonuses": [
        {
          "description": "+1 к проверкам знания и исследования древних текстов",
          "bonusType": "ActionCheck",
          "target": "knowledge, investigation, ancient texts",
          "valueType": "Flat",
          "value": 1,
          "application": "Permanent",
          "condition": null
        }
      ]
    }
  }
}
```

Rules:
- Only one `pendingMemoryLegacy` may exist at a time
- A new `Memory Gates` purchase replaces the old pending legacy
- The client rejects a `Memory Gates` turn if no new valid pending legacy appears
- If an old `pendingMemoryLegacy` already existed before the turn and remains unchanged after the new purchase, the client rejects the turn as an invalid `Memory Gates` outcome
- Canonical `pendingMemoryLegacy` stores `legacyId`, `legacyType`, `sourceLifeHint`, `grantSource`, `applicationState`, `grantedAtUtc`, and `grantSnapshot`
- `applicationAudit` appears later only when the client has already applied the legacy locally during an incarnation turn

Applied-but-not-consumed incarnation state example:

```json
{
  "pendingMemoryLegacy": {
    "legacyId": "memory_legacy_scholar_001",
    "sourceLifeHint": "Эхо жизни мудреца, посвятившего десятилетия поиску истины.",
    "legacyType": "startingCharacteristicBonus",
    "grantSource": "memoryLegacyGrant",
    "grantSnapshot": {
      "legacyId": "memory_legacy_scholar_001",
      "legacyType": "startingCharacteristicBonus",
      "sourceLifeHint": "Эхо жизни мудреца, посвятившего десятилетия поиску истины.",
      "characteristic": "intelligence",
      "bonus": 2
    },
    "applicationState": "applied-awaiting-turn-accept",
    "grantedAtUtc": "2026-03-11T04:15:00Z",
    "characteristic": "intelligence",
    "bonus": 2,
    "applicationAudit": {
      "expectedCharacteristicValue": 3
    }
  }
}
```

Skill-based applied state example:

```json
{
  "pendingMemoryLegacy": {
    "legacyId": "memory_legacy_archivist_002",
    "sourceLifeHint": "Память архивариуса, запоминавшего забытые языки и тайные родословные.",
    "legacyType": "startingPassiveKnowledgeSkill",
    "grantSource": "memoryLegacyGrant",
    "grantSnapshot": {
      "legacyId": "memory_legacy_archivist_002",
      "legacyType": "startingPassiveKnowledgeSkill",
      "sourceLifeHint": "Память архивариуса, запоминавшего забытые языки и тайные родословные.",
      "skillName": "Echo of Lost Archives",
      "skillDescription": "Отголоски прошлой жизни помогают тебе быстрее узнавать скрытые связи, древние тексты и забытые факты.",
      "group": "Knowledge",
      "playerStatBonus": "+1 к проверкам знания и исследования древних текстов",
      "masteryLevel": 1,
      "maxMasteryLevel": 5,
      "structuredBonuses": [
        {
          "description": "+1 к проверкам знания и исследования древних текстов",
          "bonusType": "ActionCheck",
          "target": "knowledge, investigation, ancient texts",
          "valueType": "Flat",
          "value": 1,
          "application": "Permanent",
          "condition": null
        }
      ]
    },
    "applicationState": "applied-awaiting-turn-accept",
    "grantedAtUtc": "2026-03-11T04:15:00Z",
    "skillName": "Echo of Lost Archives",
    "skillDescription": "Отголоски прошлой жизни помогают тебе быстрее узнавать скрытые связи, древние тексты и забытые факты.",
    "group": "Knowledge",
    "playerStatBonus": "+1 к проверкам знания и исследования древних текстов",
    "masteryLevel": 1,
    "maxMasteryLevel": 5,
    "structuredBonuses": [
      {
        "description": "+1 к проверкам знания и исследования древних текстов",
        "bonusType": "ActionCheck",
        "target": "knowledge, investigation, ancient texts",
        "valueType": "Flat",
        "value": 1,
        "application": "Permanent",
        "condition": null
      }
    ],
    "applicationAudit": {
      "expectedPassiveSkillName": "Echo of Lost Archives",
      "expectedGroup": "Knowledge",
      "expectedPlayerStatBonus": "+1 к проверкам знания и исследования древних текстов",
      "expectedStructuredBonusesCount": 1,
      "expectedStructuredBonusesCanonical": "[{\"description\":\"+1 к проверкам знания и исследования древних текстов\",\"bonusType\":\"ActionCheck\",\"target\":\"knowledge, investigation, ancient texts\",\"valueType\":\"Flat\",\"value\":\"1\",\"application\":\"Permanent\",\"condition\":\"\"}]"
    }
  }
}
```

Equivalent reordering of fields or bonus entries is acceptable as long as the canonical semantic bonus content remains the same.

==================================================
4.5B MORTAL WORLD TRAINING MUST MATERIALIZE SKILLS
==================================================

If a Mortal World scene says the player learned, trained, practiced, unlocked, or repeatedly applied a concrete skill, the result must not remain prose-only. Attribute-only checks are allowed when the character has not learned a durable skill yet, but the narrative should make that clear.

Example: a child in an archive spends repeated turns learning the difference between house seals and witness marks. Once the lesson crosses from early practice into a learned technique, the GM should add a passive skill:

```json
{
  "response": "Лейра кладёт рядом две учебные ленты, и Искра наконец перестаёт смотреть только на форму знака. Теперь она видит порядок: главная печать принимает документ, а свидетельская метка отвечает за того, кто видел принятие. Это ещё не даёт права идти в нижнее хранилище одной, но превращает страх перед второй строкой в навык задавать правильные вопросы.",
  "passiveSkillChanges": [
    {
      "skillName": "Чтение свидетельских меток",
      "skillDescription": "Искра умеет отличать домовые печати, свидетельские метки и порядок подписей в архивных лентах.",
      "rarity": "Common",
      "type": "KnowledgeBased",
      "group": "Архивное дело",
      "masteryLevel": 1,
      "maxMasteryLevel": 5,
      "playerStatBonus": "Помогает проверкам Интеллекта и Восприятия при чтении архивных печатей.",
      "structuredBonuses": [
        {
          "targetType": "skill",
          "skillName": "Архивное дело",
          "valueType": "flat",
          "value": 1,
          "condition": "чтение печатей, свидетельских меток и порядка подписей",
          "source": "Чтение свидетельских меток",
          "summary": "Архивное дело +1 при разборе печатей и свидетельских меток"
        }
      ],
      "knowledgeDomain": "архивные печати и свидетельские метки",
      "effectDetails": "Помогает задавать точные вопросы о свидетельских строках, но не заменяет доступ к закрытым архивам."
    }
  ]
}
```

If the scene instead teaches a deliberate combat or contest action, add the full active skill object via `activeSkillChanges` and initialize/update `skillMasteryChanges` for that exact `skillName`. Do not write `skillMasteryChanges` for a skill that is neither already in `game_state/player/skills_active.json` nor created in the same response.

==================================================
4.5C MORTAL WORLD XP AND LEVEL-UP USE EXPERIENCE.JSON
==================================================

Ordinary Mortal World XP is stored in `game_state/player/experience.json`. If combat, a completed objective, or a meaningful victory grants mechanical progress, do not leave the reward as prose-only.

When an active skill was used, update mastery separately from character XP. The player-level reward belongs in `experience.json`; skill practice belongs in `skillMasteryChanges`.

Example: the player wins a dangerous archive duel using an existing active skill and crosses the next level threshold:

```json
{
  "response": "Тень отступает в щель между шкафами, оставляя на полу только холодную пыль. Искра не чувствует победного восторга, но рука уже запомнила короткий выпад: не широкий удар, а один точный толчок в момент, когда противник раскрывается. После этой схватки тело и память делают шаг дальше.",
  "experienceGained": 20,
  "skillMasteryChanges": [
    {
      "skillName": "Быстрый выпад",
      "newMasteryLevel": 1,
      "newCurrentMasteryProgress": 2,
      "newMasteryProgressNeeded": 5,
      "masteryLeveledUp": false
    }
  ]
}
```

Then write these values into `game_state/player/experience.json`, not into the top-level GameResponse:

- `playerLevel`: 2
- `level`: 2
- `currentExperience`: 10
- `experience`: 10
- `totalExperience`: 110
- `experienceForNextLevel`: 150
- `experienceGained`: 20

Include `game_state/player/experience.json` in `Complete-BoeTurn -FilesModified`. Do not edit `game_state/player/stat_points.json` for this ordinary level-up: after the accepted turn, the client detects the higher level, preserves its `levelUpStatPointsAwardedThroughLevel` marker, and opens the stat allocation flow.

Important invariant: after an XP reward, `totalExperience` must be lower than the current `experienceForNextLevel` for the persisted `playerLevel`. If the reward leaves `totalExperience >= experienceForNextLevel`, the level-up is not materialized yet. Repair it by advancing `playerLevel`/`level` and setting `experienceForNextLevel` to the next threshold above the new `totalExperience`; do not compensate by editing `stat_points.json`.

Because the example is a duel, it also needs Mortal combat state. Write `game_state/combat/combat_log.json` with a player-facing summary of the same exchange so `/бой` is useful after the turn. If the opponent remains active, defeated-but-important, or likely to act again, also write `game_state/combat/enemies.json`; if allies took part tactically, write `game_state/combat/allies.json`. Use `MORTAL_COMBAT_STATE_TEMPLATE.md` before writing these files, and include every touched combat file in `Complete-BoeTurn -FilesModified`.

==================================================
4.6 LOCAL GUARDIAN TRADE AWARENESS
==================================================

Local guardian trade is client-side and does NOT create `turn_request.json`, but it is still canonical to the current active Guardian's abode.

Guardian-forced incarnation is a separate lifecycle sanction:
- NEVER use it on the immediate protected return turn if `game_state/control/afterlife_return_guard.json` still has `remainingProtectedTurns > 0`
- It is legal only on an ordinary player-driven Chaos Sea turn after explicit provocation against the current active Guardian
- Valid forced payload fragment in `game_state/control/incarnation_trigger.json`:

```json
{
  "source": "guardian_forced",
  "guardianId": "guard_social_azalia_001",
  "severityBand": "severe",
  "reason": "Азалия решает наказать душу за дерзкое унижение в её собственной обители.",
  "provocationSummary": "Игрок пришёл к Азалии после возвращения, открыто насмехался над её доменом и спровоцировал её на санкцию.",
  "worldDescription": "Мир ночных княжеств, где вампирские дома ведут скрытую войну за пленников и кровь.",
  "characterDescription": "Молодой пленник, предназначенный для жертвоприношения в вампирском городе.",
  "circumstances": "Герой просыпается в клетке под дворцом и понимает, что казнь назначена на рассвете."
}
```

Example persisted fragment in `game_state/meta/guardians.json`:

```json
{
  "guardianId": "guard_social_azalia_001",
  "name": "Азалия",
  "tradeInventory": {
    "tradeCycleId": "return_4",
    "generatedAtUtc": "2026-03-11T08:15:00Z",
    "generationReputationTier": "Friendly",
    "pricingReputationTier": "Friendly",
    "items": [
      {
        "slotId": "trade_guard_social_azalia_001_return_4_1",
        "priceInFeathers": 70,
        "domainTag": "Social",
        "soldOut": true,
        "relicData": {
          "relicId": "trade_guard_social_azalia_001_social_shelkovogo_golosa_return_4_1",
          "name": "Печать Шёлкового Голоса",
          "quality": "Uncommon",
          "description": "Реликвия усиливает убедительность и оставляет после речи ощущение доверия.",
          "effects": {
            "characteristicBonuses": {
              "Persuasion": 5
            }
          }
        }
      },
      {
        "slotId": "trade_guard_social_azalia_001_return_4_2",
        "priceInFeathers": 70,
        "domainTag": "Social",
        "soldOut": false,
        "relicData": {
          "relicId": "trade_guard_social_azalia_001_social_dvorcovoy_ulybki_return_4_2",
          "name": "Знак Дворцовой Улыбки",
          "quality": "Uncommon",
          "description": "Реликвия сглаживает острые углы в разговорах и улучшает первое впечатление."
        }
      },
      {
        "slotId": "trade_guard_social_azalia_001_return_4_3",
        "priceInFeathers": 140,
        "domainTag": "Social",
        "soldOut": false,
        "relicData": {
          "relicId": "trade_guard_social_azalia_001_social_serdechnogo_uzora_return_4_3",
          "name": "Печать Сердечного Узора",
          "quality": "Rare",
          "description": "Реликвия помогает удерживать доверие и мягче направлять эмоциональный тон беседы."
        }
      },
      {
        "slotId": "trade_guard_social_azalia_001_return_4_4",
        "priceInFeathers": 140,
        "domainTag": "Social",
        "soldOut": false,
        "relicData": {
          "relicId": "trade_guard_social_azalia_001_social_salonnoy_gratsii_return_4_4",
          "name": "Вуаль Салонной Грации",
          "quality": "Rare",
          "description": "Реликвия придаёт речи вес и помогает носителю держаться увереннее в светском окружении."
        }
      }
    ]
  }
}
```

Operational rule for GM:
- if the player asks this same Guardian about a relic bought from that local trade stock, the Guardian knows the item and may explain its purpose, symbolism, or domain connection
- the Guardian must not act surprised by the relic's existence or by the fact that the player obtained it from that Guardian's own abode stock

==================================================
5. INVALID CASE — WHAT CLIENT WRITES
==================================================

If GM output is rejected, client writes:
game_state/control/validation_repair_request.json

Example:
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "source": "обработки хода",
  "detectedAtUtc": "2026-03-09T12:00:00Z",
  "revalidationAttempt": 1,
  "gmInstructions": "Текущий ответ/состояние отклонены клиентом. Исправь уже записанные файлы in place, ориентируясь на список ошибок ниже. Прочитай TaskGuides/CLI_Step_Main.txt и Examples/E_CLI_Step_Main.txt. После исправлений создай game_state/control/validation_repair_ready.json с sessionId/requestId/turnNumber. Если клиент переписал этот repair request повторно, используй ТОЛЬКО самые свежие metadata из текущего файла.",
  "errors": [
    {
      "code": "missing_npc_scope",
      "filePath": "output/debug_logs.json",
      "severity": "Error",
      "message": "Отсутствует обязательная секция 'Охват NPC-анализа'",
      "section": "npc_scope",
      "expected": "NPC scope declaration section",
      "actual": "missing",
      "repairHint": "Добавь секцию '## Охват NPC-анализа' с режимом, релевантными акторами и обоснованием."
    },
    {
      "code": "missing_actor_block",
      "filePath": "output/debug_logs.json",
      "severity": "Error",
      "message": "Отсутствует reasoning block для NPC 'Марта'",
      "actor": "Марта",
      "section": "npc_reasoning",
      "expected": "### Марта block",
      "actual": "missing",
      "repairHint": "Добавь блок '### Марта' с ситуацией, мыслями и действиями."
    }
  ]
}

==================================================
6. HOW GM SIGNALS RECHECK
==================================================

After fixing files, GM writes:
game_state/control/validation_repair_ready.json

Example:
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "updatedAtUtc": "2026-03-09T12:03:15Z",
  "note": "NPC scope and actor reasoning blocks repaired"
}

Client behavior:
- sees validation_repair_ready.json
- revalidates current files
- if still invalid:
  - rewrites validation_repair_request.json with updated errors
  - deletes validation_repair_ready.json
  - waits again
- if valid:
  - accepts the turn
  - deletes both repair files

IMPORTANT:
- declared `Relevant actors` must cover any new or changed structured actor updates written in this turn, such as `NPCCoreChanges`, new/promotion `UpdateNPCs`, actor-specific NPC update arrays, and `UpdateGuardians`
- unchanged historical NPC objects may remain in canonical `NPCsInScene` as retained scene state when the actor is physically present; retained scene state is not an ordinary update command. Do not emit an ordinary-existing full object in `UpdateNPCs`
- `Scene-local + Relevant actors: нет` is valid only when no new or changed structured actor updates are emitted
- unresolved ID-only structured updates should not be hard-rejected solely because the client could not recover a reliable name alias from current state
- if `validation_repair_request.json.harnessRepairPackets[]` is present, follow those packets before free-form interpretation of `errors[]`
- if a packet has `kind = "npc_scope_declaration_repair"`, repair only `output/debug_logs.json.gm_thoughts_markdown` unless another validation error names state files; write a complete `## NPC Scope` block, remember that `World-progression`, `Guardian-centric`, and `Mixed` need at least one relevant actor, and add one reasoning block for every relevant actor
- if a packet has `kind = "actor_reasoning_subpoint_repair"`, follow its full Actor Brain `debugLogTemplate`: include `Данные профиля:`, `Мотивация:`, `Ограничения:`, at least two numbered `Варианты стратегий:` with explicit `Выгода:` and `Риск:`, `Выбранная стратегия:`, rejected alternatives, `Действия:`, and `Изменения состояния:`
- if a packet has `kind = "actor_memory_persistence_repair"`, do not replay the decision; append one fresh first-person current-turn thought only to the exact listed journal surface. Guardian repair defaults to the packet-listed `guardianThoughtJournalUpdates` / `guardian_thought_journal.json`; direct repair of that file writes its canonical top-level `entries[]` root. `guardianThoughtJournalUpdates` is a first-pass response command, not the on-disk root, and `schemaVersion` is unsupported there. If routing metadata names a stable actor id, repair that exact actor's journal; never use another same-name actor's entry. Do not touch `guardians.json` unless another current error explicitly lists it. First-pass authoring may still use `guardians[].musings` through `UpdateGuardians.addMusings`, but never duplicate the same reaction across both. Mortal NPCs use `NPCJournals[].journalEntries[]`; residents use `residentThoughtJournalUpdates`; a new afterlife profile initializes `gmThoughtsSummary`, while every later decision appends actor-owned `ledger`/`progressionLedger` in `game_state/meta/afterlife_entity_profiles.json`; an existing Shining faction appends `shiningFactionChronicleUpdates`, never a `shiningFactionStrategicMemoryUpdates` rewrite alone. In packet-listed `output/debug_logs.json`, update only the same actor's `Изменения состояния:` line to name the exact journal command/surface; internal memory-only repair does not rewrite narrative/interface output
- every independent non-player name in relevant actors needs a canonical memory owner. If the actor is genuine, materialize the supported profile with a stable id; otherwise remove the invented/non-actor label from relevant actors and its standalone Actor Brain block
- if a packet has `kind = "accepted_turn_output_artifact_repair"`, read `game_state/control/gm_context_pack/Templates/OUTPUT_ARTIFACT_REPAIR_TEMPLATE.md` and treat `targetFiles[]` as the exact allowlist. Repair only listed files. If `output/debug_logs.json` is absent, preserve it unchanged; if listed, preserve the full Actor Brain and exact journal/ledger surface in every `Изменения состояния:` line. Preserve a genuinely actorless scope without inventing an actor. Do not touch canonical `game_state/*` files unless the same current repair request still lists canonical errors, do not create a new turn, do not write `ready/turn_complete.json`, and finish with `Complete-BoeValidationRepair`
- if one current repair request lists canonical and player-facing targets, write every canonical correction first and only then rewrite dependent narrative/interface output. The client retains the original canonical target set through every later output-only retry. Each output write must be strictly newer than the latest actual write of every retained target; equal timestamps are stale. Request creation, worker dispatch, and repair-loop start do not make an earlier output fresh, and an unobservable target write fails closed

Worked output-only repair after a Guardian canonical correction:

```text
validation_repair_request.json packet targetFiles:
- output/narrative_response.json
- output/interface_updates.json

Accepted output/debug_logs.json already contains:
- Изменения состояния: guardianThoughtJournalUpdates adds the Guardian's first-person thought.

Correct repair:
1. Rewrite narrative_response and interface_updates from the repaired canonical Guardian state.
2. Leave output/debug_logs.json byte-for-byte unchanged because it is not in targetFiles.
3. Run Complete-BoeValidationRepair.

Wrong repair:
- rewrite debug_logs and replace the exact journal surface with "the previous memory remains valid" or "state unchanged".

Worked mixed canonical/output repair order:

```text
validation_repair_request.json lists:
- game_state/meta/soul_state.json.soulFormDescription
- output/narrative_response.json

Correct repair:
1. Rewrite soul_state.json with the accepted canonical soul form.
2. Re-read that repaired state and rewrite narrative_response.json so its timestamp/write occurs after soul_state.json.
3. Run Complete-BoeValidationRepair last.

Wrong repair:
- rewrite narrative_response.json immediately after reading the request, then repair soul_state.json. The prose predates the state it claims to describe and the client requests an output-only repair.
- after the output-only retry appears, forget soul_state.json from the original canonical target set, or accept an output write whose timestamp equals the soul_state write. Both remain stale; rewrite output strictly after the latest retained target write.
```
```

Faction identity repair packet example:
{
  "kind": "faction_identity_repair",
  "priority": "high",
  "title": "Faction identity repair",
  "targetFiles": [
    "game_state/factions/faction_core.json",
    "game_state/factions/faction_custom.json",
    "game_state/control/pending_turn_snapshot/game_state/factions/faction_core.json"
  ],
  "steps": [
    "Use game_state/control/pending_turn_snapshot/game_state/factions/faction_core.json as authority for whether the faction was already permanent before this turn or was still a same-turn temporary faction.",
    "If the snapshot entry has factionId = null plus a non-empty initialId, restore current faction identity to factionId = null, exact initialId, and isNewFaction = true.",
    "If the snapshot entry has a non-empty factionId, use that exact permanent factionId and remove initialId/isNewFaction.",
    "If a sidecar faction file contains this temporary faction with factionId = null, remove that sidecar entry until the client materializes a permanent factionId."
  ],
  "doNotDo": [
    "Do not invent a permanent factionId from the faction name.",
    "Do not write temporary same-turn faction identities into faction sidecar files.",
    "Do not delete ranks, resources, projects, chronicles, or reputation details just to silence identity validation."
  ]
}

For example, if the snapshot has `factionId: null`, `initialId: "temp-faction-merchant-guild-eternia"`, and `isNewFaction: true`, the GM must restore that temporary identity shape instead of trying names such as `faction_merchant_guild` or `faction_capital_merchants`.

Mortal bootstrap materialization repair packet:
{
  "kind": "mortal_bootstrap_materialization_repair",
  "priority": "high",
  "title": "Mortal bootstrap materialization repair",
  "targetFiles": [
    "game_state/control/mortal_bootstrap_scaffold.json",
    "lore/codex_entries.json",
    "game_state/inventory/items.json",
    "game_state/factions/faction_core.json",
    "game_state/world/current_location.json",
    "game_state/world/world_events.json",
    "game_state/player/skills_active.json",
    "game_state/player/skills_passive.json",
    "game_state/player/skill_mastery.json"
  ],
  "expectedShape": [
    "bootstrap_codex_missing_current_world_entries: lore/codex_entries.json must contain current-world entries created for this life.",
    "readable_document_missing_detail_authority: every readable document item must have a readable detail authority that the player can open.",
    "item durability: starting inventory items must write durability as a percentage string such as 100%, never a bare number such as 100.",
    "item journalEntries: journalEntries[] entries must be non-empty strings, not objects.",
    "current_location_coordinates_mismatch: if currentLocationData uses a known locationId from the bootstrap scaffold, copy that locationId coordinates exactly from canonicalCoordinateAuthority.",
    "location shape: the neutral bootstrap current_location and world_map retain identity, coordinates, knownExits, adjacencyMap, factionControl, locationStorages, and activeThreats; location type, traversal, safety, and difficulty are setting-owned and must not be invented by the client.",
    "world_map_new_link_missing_required_fields: every neutral map link preview must include targetLocationId, targetName, and targetCoordinates; setting-owned link type, travel mode, and difficulty are optional until explicitly materialized by the GM.",
    "mortal_bootstrap_placeholder_player_visible_name: accepted Mortal bootstrap must replace client scaffold labels in identity/title fields such as Стартовая сцена, Путь из стартовой сцены, or Ближайший выход из стартовой сцены with concrete in-world names; ordinary description/history prose may quote those phrases.",
    "mortal_bootstrap_requested_teacher_missing: every structuredGmAuthority.actorCapabilities declaration with capability=canTeach and required=true needs a usable NPC teacherProfile with canTeach=true and non-empty skills[].",
    "mortal_bootstrap_requested_trade_missing: every structuredGmAuthority.actorCapabilities declaration with capability=canTrade and required=true needs a usable NPC tradeState with canTrade=true and an explicit valid merchantProfile; NPC prose never resolves it.",
    "mortal_bootstrap_explicit_competency_missing: every structuredGmAuthority.playerSkills entry must remain in the matching active/passive skill state, with active mastery preserved.",
    "mortal_bootstrap_world_event_missing: worldEventRequirements.requiredEventIds must remain in world_events.json so /новости_мира is useful after bootstrap.",
    "canonical_faction_custom_state_missing_required_fields: faction state must keep required identity, rank/reputation/progression, and sidecar-compatible fields."
  ],
  "steps": [
    "Read game_state/control/mortal_bootstrap_scaffold.json first and use it as the client-authored checklist for this first Mortal World materialization.",
    "Treat identity/title scaffold labels as temporary internal anchors only: before Complete-BoeTurn, rename the visible current location, map locations, and exits/links to concrete world-specific names chosen through setting-aware GM judgment. Do not leave Стартовая сцена, Путь из стартовой сцены, or Ближайший выход из стартовой сцены in accepted identity fields; the same phrase inside ordinary narrative prose is allowed.",
    "When editing current_location.json or world_map.json, use mortal_bootstrap_scaffold.json canonicalCoordinateAuthority: the same locationId must have identical coordinates in current_location, world_map newLocations, adjacencyMap targetCoordinates, and newLinks targetCoordinates.",
    "Preserve current_location.json and durable world_map identity, arrays, coordinates, and navigation links. Do not add type, traversal, safety, biome, or difficulty merely to complete the bootstrap.",
    "Patch every neutral map link preview with targetLocationId, targetName, and targetCoordinates. Add setting-owned traversal or difficulty only when the GM explicitly materializes it for the current world.",
    "Patch only current-world bootstrap files named in targetFiles. Keep empty faction and quest collections empty unless the GM explicitly creates complete world-specific entities.",
    "Do not reuse previous-life lore/current_world files; every current-world codex or lore entry must describe the life being created now.",
    "Do not derive a teacher from words in playerAuthoredStart. If the GM makes a setting-aware decision that a teacher is present, record a required canTeach entry in structuredGmAuthority.actorCapabilities and materialize the NPC as a complete first-materialization NPCsInScene or UpdateNPCs object with teacherProfile.canTeach=true, relationshipLevel, summary, and skills[].",
    "Do not derive a merchant from words in playerAuthoredStart. If the GM makes a setting-aware decision that trade is available, record a required canTrade entry in structuredGmAuthority.actorCapabilities and materialize the NPC as a complete first-materialization NPCsInScene or UpdateNPCs object with tradeState.canTrade=true, merchantProfile, relationshipLevel, summary, and matching currentLocationId.",
    "Restore every structuredGmAuthority.playerSkills entry in skills_active.json or skills_passive.json; active skills also keep their skill_mastery.json entry.",
    "Restore every worldEventRequirements.requiredEventIds entry in world_events.json.worldEventsLog with a useful title and description grounded in playerAuthoredStart.startingCircumstances.",
    "Fix readable document items by adding an actual player-openable document/detail authority, not by changing the item type to avoid validation.",
    "Fix inventory item durability by writing percentage strings such as 100%, not bare numbers.",
    "Fix inventory item journalEntries by flattening malformed objects into one player-facing note string per array entry.",
    "Call Complete-BoeValidationRepair as the last action."
  ]
}

This packet means the first Mortal World prompt was created, but its supporting canonical files were incomplete or reused stale world data. Treat it as one bootstrap checklist, not as unrelated independent errors.

First Mortal bootstrap ordinary-turn checklist:

If the turn request says `MORTAL BOOTSTRAP BASELINE`, do not start from large examples or broad source search. Open these first:
- `game_state/control/mortal_bootstrap_scaffold.json`
- `game_state/control/gm_context_pack/Templates/TURN_OUTPUT_TEMPLATE.md`
- `game_state/control/gm_context_pack/Templates/PROGRESSION_REPORT_TEMPLATE.json`

`playerAuthoredStart` is narrative context only. The client baseline contains no inferred or default skills, items, NPCs, capabilities, money, progression, carrying values, factions, quests, location type/traversal/difficulty, faction resources, influence/control, or universal power-profile axes; `game_state/player/experience.json` starts as an empty structural object. Every `structuredGmAuthority.playerProgression`, `carryingRules`, or `factionMechanics` entry must include the exact `canonicalPath` and a non-empty `values` object that repeats every authorized canonical value. `{}`, reason-only records, unrelated paths, and prose do not grant authority. Record other setting-aware decisions in `playerSkills`, `inventoryItems`, `actorCapabilities`, or `resources` and write matching complete canonical state. Every GM-created NPC requires the complete first-materialization envelope.

Worked domain-bound authority example (the values below belong only to this orbital-colony example and are not universal defaults):

```json
{
  "structuredGmAuthority": {
    "playerProgression": [
      {
        "canonicalPath": "game_state/player/experience.json",
        "values": {
          "playerLevel": 1,
          "level": 1,
          "currentExperience": 0,
          "experience": 0,
          "totalExperience": 0,
          "experienceForNextLevel": 240,
          "experienceGained": 0
        },
        "reason": "Навигационная служба использует 240-очковый первый интервал подготовки."
      }
    ],
    "carryingRules": [
      {
        "canonicalPath": "game_state/inventory/items.json",
        "values": {
          "maxWeight": 35,
          "totalWeight": 0
        },
        "reason": "В этой колонии нагрузка учитывается в килограммах."
      }
    ],
    "factionMechanics": [
      {
        "canonicalPath": "game_state/factions/faction_core.json",
        "factionId": "faction_orbital_navigation",
        "values": {
          "influence": 12,
          "powerProfile": {
            "orbitalReach": 4
          }
        },
        "reason": "ГМ явно выбрал модель влияния навигационной службы."
      }
    ]
  }
}
```

When `structuredGmAuthority.playerSkills[]` is non-empty, preserve every matching active/passive skill and the active-skill mastery entry. When `worldEventRequirements` exists, preserve its required opening event in `game_state/world/world_events.json`; explicit GM mechanics and `/новости_мира` must not disappear into prose-only narration.

Every structured actor characteristic must be a finite JSON number. Values such as `1e9999`, `NaN`, infinity, quoted numbers, booleans, nulls, arrays, or objects are not valid repairs; preserve valid numeric characteristics and replace only the exact malformed coordinate named by validation.

When `mortal_bootstrap_scaffold.json.structuredGmAuthority.actorCapabilities` explicitly contains required `canTrade`, at least one scene NPC must be usable by `/торговля_нпс`: write `tradeState.canTrade=true`, an explicit valid `merchantProfile`, `relationshipLevel`, `summary`, and a current location matching the scene. Role, occupation, class, progression type, name, and description never infer a merchant profile.

The neutral scaffold baseline may contain temporary location/navigation identity labels such as `Стартовая сцена`, `Путь из стартовой сцены`, or `Ближайший выход из стартовой сцены`. These labels are never acceptable in player-visible identity/title fields after the first Mortal bootstrap is accepted. Before `Complete-BoeTurn`, rename the durable current location, map locations, and exits/links through setting-aware GM judgment. The validator does not reject the same words when they appear naturally inside a description, history, summary, or other narrative prose.

Minimum successful output for the first Mortal scene:
- `output/narrative_response.json`: the first playable Mortal scene text with a timestamp.
- `output/debug_logs.json`: NPC Scope and reasoning, even if the scene is local and only the player is acting.
- `output/interface_updates.json`: clear player choices for the next action.
- `game_state/control/progression_report.json`: exact sessionId/requestId/turnNumber and zero processed counts when no progression cycle is due.
- `ready/turn_complete.json` through `Complete-BoeTurn -FilesModified ...` as the last action.

If you add or update durable Mortal anchors, use only the compact template for that exact surface before broad examples:
- locations/map: `MORTAL_LOCATION_TRANSITION_TEMPLATE.md`
- NPCs: `MORTAL_NPC_UPDATE_TEMPLATE.md`
- factions: `MORTAL_FACTION_UPDATE_TEMPLATE.md`
- skills: `MORTAL_SKILL_PROGRESSION_TEMPLATE.md`
- XP/level: `MORTAL_EXPERIENCE_LEVEL_TEMPLATE.md`

Never return to idle without writing at least the player-facing output files and one terminal signal. A prior `gm_bridge_idle_without_terminal_signal` trajectory with no player-facing output means the retry must use this checklist before any broad reading. A prior `gm_output_without_terminal_signal` trajectory means payload files were written, but the turn was still rejected because `Complete-BoeTurn`/`Fail-BoeTurn` was skipped; next time write the terminal helper call last.

After `Fail-BoeTurn` or any `ready/turn_error.json` / `ready/turn_complete.json` exists for the current request, stop working on that request. Do not keep writing state/output files and do not try to replace an error with success; the client owns rollback/cleanup and will send a fresh request if the action should be retried.

Mortal NPC repair packets:
{
  "kind": "mortal_npc_location_repair",
  "priority": "high",
  "title": "Mortal NPC same-turn location repair",
  "targetFiles": [
    "game_state/npcs/npc_core.json"
  ],
  "templateRefs": [
    "Templates/MORTAL_NPC_UPDATE_TEMPLATE.md"
  ],
  "expectedShape": [
    "NPCsInScene is only for actors physically present in currentLocationData; voices behind a door, nearbyExitLocationId actors, and corridor/exit pressure are offscreen for the current scene.",
    "NPCsInScene in a known current location use currentLocationId = currentLocationData.locationId, initialLocationId = null, and currentLocationName = visible current location name.",
    "NPCsInScene in a same-turn new location use initialLocationId = currentLocationData.initialId/newLocations.initialId, currentLocationId = null, and currentLocationName = visible current location name.",
    "Existing persistent NPCs keep permanent npcId and use currentLocationId/currentLocationName for a move."
  ],
  "missingFields": [
    "inventory"
  ],
  "safeCorrectionRules": [
    "Patch only targetFiles unless the repair request lists another file.",
    "Use the current turn/session location authority; do not invent a location id from prose."
  ],
  "steps": [
    "Read templateRefs before editing game_state/npcs/npc_core.json.",
    "If missingFields[] is present, patch those exact fields first. For a newly created NPC without carried items, use inventory: [].",
    "Repair the listed NPC entries in place, preserving personality, goals, relationshipLock, journals, and unrelated NPCs.",
    "Call Complete-BoeValidationRepair as the last action."
  ]
}

The same NPC packet family can appear as `mortal_npc_full_object_repair` for missing required profile/social fields, `mortal_npc_relationship_enum_repair` for `attitude`/`relationshipLevel` and `culturalStance`, or `mortal_npc_reference_repair` for orphan `NPCJournals`/sidecar references. For those packets, follow `missingFields[]`, `expectedShape[]`, and `safeCorrectionRules[]` first; do not search implementation code to rediscover canonical fields.

VALIDATION CONTRACT SUMMARY:
- client checks correlated `sessionId/requestId/turnNumber`
- client checks valid JSON
- client checks realm restrictions
- client checks `progressionControl` and required progression reports
- client checks `gm_thoughts_markdown` scope + reasoning blocks
- client checks that declared relevant actors cover structured actor updates when actor identity is known well enough
- if rejected, GM must fix files IN PLACE and create a fresh `validation_repair_ready.json`

==================================================
7. SCENE-LOCAL TURN WITH EMPTY ACTOR SCOPE
==================================================

This is VALID when the turn is truly local and no NPC or Guardian needs reasoning:

## Оценка времени и мира
- Прошло игрового времени: 5 минут
- Логические последствия: Существенных мировых или фракционных последствий нет
- Решение о прогрессии: Прогрессия не требуется

## Охват NPC-анализа
- Режим: Scene-local
- Релевантные акторы: нет
- Почему они релевантны: В этом ходе нет акторов, которым нужно принимать собственное решение
- Акторы вне охвата: Все удалённые НПС и Хранители
- Почему они вне охвата: Ход локальный, не вызывает off-screen последствий и не затрагивает их агентность

No actor blocks are required when relevant actors = нет in Scene-local mode.

==================================================
8. INVALID READY SIGNALS
==================================================

If GM writes malformed JSON:
game_state/control/validation_repair_ready.json
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "updatedAtUtc": "BROKEN
}

Client behavior:
- rejects the ready file
- rewrites validation_repair_request.json with top code `invalid_repair_ready_json`
- deletes the bad ready file
- daemon pings GM again

If GM writes mismatched metadata:
{
  "sessionId": "wrong-session",
  "requestId": "wrong-request",
  "turnNumber": 999,
  "updatedAtUtc": "2026-03-09T12:05:00Z"
}

Client behavior:
- rejects the ready file
- rewrites validation_repair_request.json with top code `mismatched_repair_ready_context`
- deletes the bad ready file
- daemon pings GM again

Rejected terminal ready example:

ready/turn_complete.json
{
  "sessionId": "wrong-session",
  "requestId": "wrong-request",
  "turnNumber": 999,
  "status": "success",
  "timestamp": "2026-03-09T12:06:00Z",
  "filesModified": [
    "output/narrative_response.json",
    "output/debug_logs.json"
  ]
}

Client behavior in active wait:
- retries reading the file a few times
- rejects it as a bad terminal ready signal
- ends the current wait cycle as protocol failure
- restores rollback state if rollback is available
- does NOT treat this as a repair loop
- writes `game_state/control/terminal_protocol_failure_request.json`
- chooses the final success/error/failure branch only AFTER re-reading the current ready files on disk

Example terminal_protocol_failure_request.json:
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "source": "terminal protocol failure: turn_complete",
  "detectedAtUtc": "2026-03-09T12:06:05Z",
  "gmInstructions": "Клиент отклонил terminal ready signal как protocol failure. Это НЕ validation_repair_request.json и НЕ repair loop. Не создавай validation_repair_ready.json и не пытайся продолжать этот уже закрытый wait cycle. Прочитай TaskGuides/CLI_Step_Main.txt и Examples/E_CLI_Step_Main.txt, разберись с terminal protocol problem по списку ошибок ниже и исправь логику для следующего корректного хода.",
  "errors": [
    {
      "code": "mismatched_terminal_ready_context",
      "filePath": "ready/turn_complete.json",
      "severity": "Error",
      "message": "Terminal ready signal содержит metadata, не совпадающие с активным ходом",
      "section": "terminal_ready",
      "expected": "sessionId=game-session-123, requestId=abc123, turnNumber=42",
      "actual": "sessionId=wrong-session, requestId=wrong-request, turnNumber=999",
      "repairHint": "Копируй sessionId/requestId/turnNumber в terminal ready signal ровно из текущего turn_request.json."
    }
  ]
}

What GM must do next:
- understand that this was not accepted as a valid terminal signal
- read `terminal_protocol_failure_request.json`
- do NOT create `validation_repair_ready.json`
- do NOT assume the client is still waiting for this exact turn
- fix the terminal ready metadata logic before the next correct client request
- not expect the client to keep waiting forever silently

This differs from repair loop:
- repair loop starts only after a structurally valid terminal completion led to invalid state after validation
- rejected terminal ready means the client never accepted that terminal signal as valid protocol output

Dual terminal conflict example:

ready/turn_complete.json
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "status": "success",
  "timestamp": "2026-03-09T12:06:00Z",
  "filesModified": [
    "output/narrative_response.json",
    "output/debug_logs.json"
  ]
}

ready/turn_error.json
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "status": "error",
  "timestamp": "2026-03-09T12:06:01Z",
  "error": "Conflicting terminal result"
}

Client/daemon behavior:
- this is NOT a valid choice between success and error
- for one request there must be exactly one terminal signal
- client rejects the terminal phase as protocol failure
- client writes `game_state/control/terminal_protocol_failure_request.json`
- daemon pings GM about terminal protocol failure instead of treating it as a normal repair loop

Example error payload for the dual-signal case:
{
  "code": "dual_terminal_ready_signals",
  "filePath": "ready/turn_complete.json",
  "severity": "Error",
  "message": "Для одного и того же sessionId/requestId/turnNumber одновременно обнаружены ready/turn_complete.json и ready/turn_error.json",
  "section": "terminal_ready",
  "expected": "Exactly one terminal signal for the active turn",
  "actual": "turn_complete(sessionId=game-session-123, requestId=abc123, turnNumber=42, status=success); turn_error(sessionId=game-session-123, requestId=abc123, turnNumber=42, status=error)",
  "repairHint": "Для одного хода записывай ровно один terminal signal: либо ready/turn_complete.json, либо ready/turn_error.json."
}

==================================================
8. SECOND FAILED REPAIR ATTEMPT
==================================================

If GM fixed some files but state is still invalid:
- client reads the new ready file
- revalidates
- finds remaining errors
- rewrites validation_repair_request.json with `revalidationAttempt = 2`
- deletes validation_repair_ready.json
- waits again

GM must always use the MOST RECENT repair request as the source of truth.

Example of another invalid repair cause after revalidation:
- `NPCCoreChanges` changed `Марта`, but `Марта` is absent from `Relevant actors`
- client rewrites repair request with `structured_npc_update_out_of_scope`
- GM must either add `Марта` to scope + reasoning blocks or remove the structural NPC update

An unchanged historical NPC may remain in canonical `NPCsInScene` as retained scene state only while physically present. Do not add that NPC to `Relevant actors` merely because retained state is still present. An ordinary-existing `UpdateNPCs` full object is not accepted as unchanged background. Add the NPC to `Relevant actors` when the turn changes it through `NPCCoreChanges`/another actor-specific command or brings it into the scene as an active participant.

Concrete structured NPC mismatch example:

## Охват NPC-анализа
- Режим: Scene-local
- Релевантные акторы: Феликс
- Почему они релевантны: Игрок разговаривает только с Феликсом
- Акторы вне охвата: Марта
- Почему они вне охвата: Марта не должна менять состояние в этом ходу

game_state/npcs/npc_core.json
{
  "NPCCoreChanges": [
    {
      "NPCId": "npc_marta_01",
      "reason": "Марта пересмотрела отношение к риску после поступка игрока.",
      "profile": {
        "worldview": "Игроку можно доверять больше, чем раньше"
      }
    }
  ]
}

Client result:
- rejects the turn
- writes `structured_npc_update_out_of_scope`
- asks GM to either add `Марта` to `Relevant actors` + reasoning blocks or remove that structural NPC update
- this example isolates the scope mismatch while keeping the ordinary-existing mutation on the executable bounded `NPCCoreChanges` route

Concrete Mortal relevant NPC without persistence example:

## Охват NPC-анализа
- Режим: Scene-local
- Релевантные акторы: Иветта
- Почему они релевантны: Иветта присутствует в коридоре, сообщает Асурана улику про серебряную нить и направляет его к дворецкому Ролану
- Акторы вне охвата: Ролан
- Почему они вне охвата: Ролан пока только упомянут как следующий контакт и не появляется в сцене

## Размышления акторов
### Иветта
- Текущая локация: Коридор поместья Вальмонт.
- Ситуация: Горничная встречает Асурана в коридоре после ночного письма.
- Данные профиля: осторожная служанка дома Вальмонт, напугана ночным визитом, считает безопасность хозяина своим долгом и пока слабо доверяет Асурану.
- Мотивация: предупредить хозяина об угрозе, не выдав себя неизвестному союзнику.
- Ограничения: Иветта не видела лица посыльного и не станет обвинять конкретного человека без доказательств.
- Мысли: Я боюсь, но без моей подсказки хозяин пойдёт вслепую.
- Варианты стратегий:
  1. Рассказать только о серебряной нити. Выгода: Асуран получит проверяемую зацепку. Риск: он заметит, что Иветта недоговаривает.
  2. Полностью отказаться от разговора. Выгода: Иветта не раскроет свою осведомлённость. Риск: хозяин останется без предупреждения.
- Выбранная стратегия: дать ограниченную проверяемую зацепку и направить к Ролану.
- Почему альтернативы отвергнуты: молчание опаснее осторожного сотрудничества, а полной правды Иветта пока не знает.
- Действия: Она сообщает про серебряную нить на манжете и про дворецкого Ролана.
- Изменения состояния: добавить Иветту в canonical NPC state и дописать её first-person мысль в `NPCJournals[].journalEntries[]`.

Broken state:
- `output/debug_logs.json` contains the scope and actor reasoning above
- `response` narrates Иветта and the clue
- but `game_state/npcs/npc_core.json` is missing, empty, or lacks any `UpdateNPCs` / `NPCsInScene` / `NPCJournals` / actor-specific NPC surface for Иветта

Client result:
- rejects the turn
- writes `mortal_relevant_actor_missing_persistence`
- asks GM to materialize Иветта through `UpdateNPCs` or `NPCsInScene` and add an appropriate `NPCJournals`/quest/relationship/activity update, or remove Иветта from `Relevant actors` if she was only background

Corrected shape:

game_state/npcs/npc_core.json
{
  "NPCsInScene": [
    {
      "npcId": "npc_ivetta_01",
      "name": "Иветта",
      "role": "Горничная дома Вальмонт",
      "currentLocationId": "valmont_corridor"
    }
  ]
}

game_state/npcs/npc_journals.json
{
  "NPCJournals": [
    {
      "npcId": "npc_ivetta_01",
      "npcName": "Иветта",
      "lastJournalNote": "Я сказала Асурану о серебряной нити, но пока не доверяю ему всей правды.",
      "journalEntries": [
        {
          "entryId": "ivetta_thought_turn_42",
          "description": "Я сказала Асурану о серебряной нити, но пока не доверяю ему всей правды.",
          "timestamp": "2026-07-10T00:00:00Z",
          "event": "Подсказка о серебряной нити"
        }
      ]
    }
  ]
}

The player can now discover the actor and clue later through `/нпс` and related command drilldowns.

Concrete structured Guardian mismatch example:

## Охват NPC-анализа
- Режим: Guardian-centric
- Релевантные акторы: Аэлион
- Почему они релевантны: Игрок взаимодействует только с Аэлионом
- Акторы вне охвата: Серит
- Почему они вне охвата: Серит не участвует в сцене

game_state/meta/guardians.json
{
  "UpdateGuardians": [
    {
      "command": "setMood",
      "guardianId": "guardian_serit_01",
      "mood": {
        "current": "Suspicious",
        "intensity": 60,
        "reason": "Структурное обновление вне объявленного scope",
        "since": 48
      }
    }
  ]
}

Client result:
- rejects the turn
- writes `structured_guardian_update_out_of_scope`
- GM must either expand scope to include Серит or remove the guardian update

Example of a non-fatal ID-only edge case:
- a structured update references only `npcId = npc-7f2a`
- current state does not provide a reliable name alias for that ID
- client should not hard-reject solely on that unresolved identity mismatch
- GM should still prefer adding a name whenever possible to avoid ambiguity and future stricter checks

==================================================
9. CANCEL AND RESTART BEHAVIOR
==================================================

Cancel behavior:
- if rollback backup is available, client restores the last stable files and tells the player that rollback happened
- if rollback backup is unavailable for that flow, client tells the player explicitly that current files were left as is
- cancelling the wait does not make an already dispatched GM computation disappear; if GM finishes later, the client may still process it as a late response unless the flow has already been fully closed
- rollback restores pre-existing tracked files and also removes newly created tracked files from the rejected/aborted turn

Turn error behavior:
- if GM writes `ready/turn_error.json` for a transition flow, the client restores the last stable tracked file set before continuing
- this includes reverting local realm transitions and deleting new tracked files created only for the failed transition
- if GM writes `ready/turn_error.json` after the user already cancelled waiting, the client still consumes that late error signal and cleans it up on the next loop

Concrete late turn_error after cancel example:
1. Client dispatches turn `turnNumber = 42` and creates pending snapshot.
2. User presses `Escape` while waiting.
3. GM still finishes later and writes:
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "status": "error",
  "timestamp": "2026-03-09T12:07:00Z",
  "error": "The CLI agent failed before writing valid state"
}
4. On the next game loop iteration, client:
   - reads and correlates the late `turn_error.json`
   - shows the GM error
   - restores rollback snapshot if one still exists
   - deletes `ready/turn_error.json`
   - clears pending snapshot artifacts

Valid terminal error example:

ready/turn_error.json
{
  "sessionId": "game-session-123",
  "requestId": "abc123",
  "turnNumber": 42,
  "status": "error",
  "timestamp": "2026-03-09T12:08:00Z",
  "error": "Rules validation inside the CLI workflow failed before final write"
}

Correct behavior:
- daemon treats this as a normal terminal outcome of the turn
- client shows the GM error
- client performs cleanup/rollback according to the current flow rules

Daemon-side stale terminal signal example:
1. Old file remains from a previous turn:

ready/turn_complete.json
{
  "sessionId": "game-session-123",
  "requestId": "old-request",
  "turnNumber": 41,
  "status": "success",
  "timestamp": "2026-03-09T12:06:00Z",
  "filesModified": [
    "output/narrative_response.json",
    "output/debug_logs.json"
  ]
}

2. A new turn request arrives with:
- `sessionId = game-session-123`
- `requestId = new-request`
- `turnNumber = 42`

3. Correct daemon behavior:
- read metadata from the existing terminal file
- detect that `requestId/turnNumber` do not match the current turn
- delete the stale `ready/turn_complete.json`
- continue waiting for a correlated `ready/turn_complete.json` or `ready/turn_error.json`

Important:
- file existence alone is not enough to complete a turn
- daemon must correlate terminal files by `sessionId`, `requestId`, and `turnNumber`

Restart behavior:
- daemon restart: daemon re-checks existing validation_repair_request.json on startup and pings GM again
- daemon restart: daemon also re-checks existing terminal_protocol_failure_request.json on startup and pings GM again
- client restart with active repair artifacts:
  - if pending snapshot manifest + ready/turn_complete.json + repair request still exist, client resumes through the correlated late-response/validation path
  - if repair files exist without a matching pending snapshot manifest or without ready/turn_complete.json, client treats them as stale and clears them
- client restart with `terminal_protocol_failure_request.json`:
  - the file is preserved by default
  - the client must not delete it only because a pending snapshot manifest still exists
  - this can be a valid crash/restart state after the protocol failure was already recorded but before later cleanup
- save/load does NOT preserve repair files or pending turn snapshots

==================================================
10. PRACTICAL RULE
==================================================

Do NOT guess what the client wants.
Do NOT narrate first and think later.

First:
- decide actor scope
- explain why those actors matter
- think from their perspective

Only then:
- narrate outcomes
- update files
- signal readiness

==================================================
11. MORTAL ACTOR MATERIALIZATION V1
==================================================

Actor Materialization v1 applies when a genuinely new significant Mortal NPC first becomes durable state. Bind the full NPC and its immutable envelope by exact `mortal_npc:<initialId>`. Do not infer skills, inventory, teaching, trade, combat ability, or identity from a display name, prose, occupation, or setting genre. Names, archetype prose, item types, and genre keywords never grant mechanics. The example world below is only evidence that the contract is setting-neutral; it is not a keyword template for other worlds.

Every required section is either `populated` by structured NPC data or `empty_by_design` with a concise in-world reason. `empty_by_design` keeps the real canonical empty field physically present in its exact empty array/object/null shape; omission is not deliberate emptiness. First materialization contains 3-5 `personalityTraits`; every trait has an integer `value` from 1 through 10. An already materialized existing NPC keeps the original envelope unchanged; later changes use a dedicated delta command or bounded `NPCCoreChanges` instead of resending the full object. An untouched legacy NPC remains readable, but the first assignment of teacher, merchant/trade, combat, Actor Brain, or another significant structured role is a promotion and requires a complete envelope in that accepted turn.

A new Mortal `UpdateNPCs` actor has exactly one non-empty location authority: a known canonical `currentLocationId` with null `initialLocationId`, or a valid same-turn initialLocationId with null `currentLocationId`. Both missing/null, both non-null, or an unknown current location block the turn. The worked first-materialization object below uses the canonical-current branch.

Identity continuity rule: same-turn initialId must not collide with a validated pre-turn permanent NPCId. If the validated snapshot already owns that id as a permanent NPCId, restore the permanent identity; do not emit `NPCId: null` plus the old permanent id as `initialId`.

Worked legacy-promotion inventory continuity: if validated pre-turn authority contains `inventory=[{"itemId":"existing_item_01","count":1}]`, the promotion's complete `UpdateNPCs` object repeats that inventory snapshot with the same meaning and adds the first envelope. It may additionally change only the role fields that establish the newly gained capability: `teacherProfile`, `tradeState`, `activeSkills`/`passiveSkills`, or Actor Brain `plans`/`currentActivity`/`completedActivities`. History, profile, progression, relationships, quests, journals, and every other unrelated pre-turn field remain unchanged. Changing the inventory count, adding another entry, or removing the entry through that snapshot is rejected. Such a mutation is authored separately through `NPCInventoryAdds`, `NPCInventoryUpdates`, or `NPCInventoryRemovals`; the promotion snapshot remains semantically identical to the validated pre-turn inventory snapshot. Trade stock and training showcases are separate request-owned displays, not promotion fields.

Worked `mortal_npc_inventory_update_repair` decision:
- A genuinely new NPC may keep initial inventory in its full object when validated continuity confirms `NPCId: null` plus a new non-empty `initialId`.
- For an ordinary existing NPC, remove the whole ordinary-existing full-object resend from `UpdateNPCs`. Express every legitimate skill, inventory, relationship, journal, activity, equipment/resource, or other supported change through its dedicated command surface. Goals/quests, renames, unlock events, and any remaining supported domains likewise use their documented dedicated commands. Express supported profile, location, progression, setting-owned characteristic results, faction affiliation, and locked/unrealized Fate Card definition changes through `NPCCoreChanges`. If a requested protected domain is intentionally outside both contracts, use the main-GM rollback/repair path; never widen `NPCCoreChanges` or retain a schema-invalid partial full object.
- A true legacy promotion must retain schema-required inventory and copy the exact semantically unchanged validated pre-turn inventory snapshot from the validation issue's `expected` field. Do not remove or reconstruct that snapshot; author real mutations through the dedicated inventory commands.

Worked ordinary-existing location plus progression/characteristic change:

`NPCCoreChanges` is a non-carrier command. Every value below is an absolute resulting value from explicit current-world authority; the target is an exact existing permanent `NPCId`, not a name or same-turn `initialId`.

```json
{
  "NPCCoreChanges": [
    {
      "NPCId": "npc_liana_voss_permanent_01",
      "reason": "Лиана присоединилась к экспедиции и перенесла работу в архивный модуль.",
      "profile": {
        "worldview": "Наблюдение должно вести к ответственному действию."
      },
      "location": {
        "currentLocationId": "location_orbital_archive_lab",
        "initialLocationId": null
      },
      "progression": {
        "level": 4,
        "experience": 25,
        "experienceForNextLevel": 500,
        "progressionType": "Companion",
        "lastPlayerXPValueOnSync": 1200
      },
      "characteristicValues": {
        "field_observation": 15,
        "biosafety_discipline": 13
      }
    }
  ]
}
```

Worked ordinary-existing faction plus locked Fate Card definition change:

The target faction and removed card ID below must exist in validated authority. `fateCardsToAdd` accepts a complete new card only with `isUnlocked: false`; unlocking an existing card still uses `NPCFateCardUnlocks`.

```json
{
  "NPCCoreChanges": [
    {
      "NPCId": "npc_liana_voss_permanent_01",
      "reason": "Исследовательский союз признал Лиану полевым координатором и её прежняя нереализованная линия судьбы утратила смысл.",
      "factionAffiliationsToUpsert": [
        {
          "factionId": "faction_orbital_research_union",
          "factionName": "Орбитальный исследовательский союз",
          "rank": "Полевой координатор",
          "branch": "Ксенобиологический корпус",
          "membershipStatus": "Active"
        }
      ],
      "fateCardsToAdd": [
        {
          "cardId": "fate_orbital_responsible_contact",
          "name": "Ответственный контакт",
          "image_prompt": "xenobiologist opening a sealed observation chamber toward a luminous alien signal, cinematic science fiction",
          "description": "Лиана сможет превратить осторожное наблюдение в безопасный первый контакт.",
          "unlockConditions": {
            "requiredRelationshipLevel": 40,
            "plotConditionDescription": "Игрок доказывает, что контакт можно установить без эксплуатации образца.",
            "conjunction": "AND"
          },
          "rewards": {
            "description": "Открывает протокол безопасного контакта."
          },
          "isUnlocked": false
        }
      ],
      "fateCardIdsToRemove": [
        "fate_liana_obsolete_hypothesis"
      ]
    }
  ]
}
```

Worked first-materialization fragment for `game_state/npcs/npc_core.json`:

The characteristic keys in this fragment come from this current world's canonical characteristic authority. Another world must copy its own keys rather than reuse this list. This world supplies no carrying-capacity formula for the actor, so `maxWeight` and `totalWeight` remain `null`; absent characteristic fields are not invented.

```json
{
  "NPCsInScene": [
    {
      "NPCId": null,
      "initialId": "npc_orbital_xenobiologist",
      "npcName": "Лиана Восс",
      "name": "Лиана Восс",
      "role": "ксенобиолог исследовательской станции",
      "image_prompt": "orbital xenobiologist in a practical pressure suit beside a sealed specimen bay, cinematic science fiction portrait",
      "rarity": "Rare",
      "worldview": "Любое неизвестное явление следует сперва наблюдать, а уже затем пытаться использовать.",
      "personalityArchetype": "cautious_field_scientist",
      "culturalStance": "Pragmatist",
      "race": "Человек",
      "class": "Исследователь",
      "appearanceDescription": "Женщина средних лет в рабочем комбинезоне с потёртым планшетом на предплечье.",
      "history": "Руководит наблюдением за организмами, обнаруженными под ледяной корой спутника.",
      "progressionType": "static_social_npc",
      "initialLocationId": null,
      "currentLocationId": "location_orbital_specimen_bay",
      "currentLocationName": "Орбитальный ксенобиологический отсек",
      "age": 43,
      "level": 3,
      "experience": 0,
      "experienceForNextLevel": 300,
      "relationshipLevel": 0,
      "attitude": "Neutral",
      "playerCompanionDirective": "not_companion",
      "culturalLayer": "Научный экипаж дальнего орбитального комплекса",
      "personalityTraits": [
        {
          "traitId": "trait_liana_methodical",
          "name": "Методичность",
          "summary": "Не делает выводов без проверяемых наблюдений.",
          "traitName": "Методичность",
          "description": "Лиана последовательно отделяет факты от предположений.",
          "value": 8,
          "valueDescription": "Сложнее убедить её рисковать, но она замечает противоречия в доказательствах."
        },
        {
          "traitId": "trait_liana_protective_curiosity",
          "name": "Оберегающая любознательность",
          "summary": "Стремится понять неизвестное, не подвергая других неоправданному риску.",
          "traitName": "Оберегающая любознательность",
          "description": "Её любопытство всегда сопровождается ответственностью за экипаж и образцы.",
          "value": 7,
          "valueDescription": "Она продолжает исследование даже под давлением, но остановит опыт при реальной угрозе людям."
        },
        {
          "traitId": "trait_liana_reserved_empathy",
          "name": "Сдержанная эмпатия",
          "summary": "Проявляет заботу через точные действия, а не через открытые эмоции.",
          "traitName": "Сдержанная эмпатия",
          "description": "Лиана редко говорит о чувствах, но внимательно замечает страх и усталость коллег.",
          "value": 6,
          "valueDescription": "Она предложит практическую помощь, хотя её тон может остаться сухим."
        }
      ],
      "maxWeight": null,
      "totalWeight": null,
      "isOverloaded": false,
      "progressionTrackers": {
        "active": [],
        "completed": [],
        "summary": "Личная линия развития пока не началась."
      },
      "plans": "Проверить, почему образец реагирует на голос игрока.",
      "personalQuests": [],
      "relationshipLock": {
        "isLocked": false,
        "lockState": "none",
        "reason": "",
        "breakthroughQuestId": null
      },
      "characteristics": {
        "xenobiology": 14,
        "field_observation": 13,
        "biosafety_discipline": 12,
        "zero_gravity_coordination": 9
      },
      "activeSkills": [],
      "passiveSkills": [],
      "equippedItems": {},
      "fateCards": [],
      "inventory": [],
      "goals": {
        "primary": {
          "goalId": "goal_liana_understand_specimen",
          "title": "Понять реакцию образца",
          "status": "active",
          "summary": "Установить, почему организм отвечает на присутствие игрока."
        },
        "shortTerm": "Получить безопасную запись реакции образца.",
        "longTerm": "Определить, является ли организм разумным."
      },
      "sceneStatus": "present",
      "turn": 4,
      "lastSeenAtUtc": "2026-07-18T05:00:00Z",
      "materialization": {
        "schemaVersion": 1,
        "materializationId": "mat_npc_orbital_xenobiologist_turn_4",
        "actorType": "mortal_npc",
        "actorId": "npc_orbital_xenobiologist",
        "materializedAtTurn": 4,
        "state": "complete",
        "capabilities": {
          "canFight": false,
          "canTeach": false,
          "canTrade": false,
          "ownsItems": false
        },
        "sections": {
          "skills": { "state": "empty_by_design", "reason": "В этой встрече Лиана не раскрыла оформленных игровых навыков." },
          "inventory": { "state": "empty_by_design", "reason": "Значимых переносимых предметов при ней сейчас нет." },
          "fateCards": { "state": "empty_by_design", "reason": "Отдельный узел её судьбы ещё не проявился." },
          "personalQuests": { "state": "empty_by_design", "reason": "Она пока не просит игрока о личной помощи." },
          "relationships": { "state": "populated" }
        }
      }
    }
  ]
}
```
