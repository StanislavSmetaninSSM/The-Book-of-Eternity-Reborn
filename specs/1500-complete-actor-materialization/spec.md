# Feature Specification: Complete Actor Materialization

**Feature Branch**: `1500-complete-actor-materialization`

**Created**: 2026-07-18

**Status**: Approved

**Input**: Prevent technically valid but hollow Mortal and afterlife actors by enforcing a setting-agnostic materialization contract.

## Source Issues & Scope

- **Source GitHub issue(s)**: [#1500](https://github.com/StanislavSmetaninSSM/The-Book-of-Eternity-Reborn/issues/1500); related startup findings [#1446](https://github.com/StanislavSmetaninSSM/The-Book-of-Eternity-Reborn/issues/1446) and [#1461](https://github.com/StanislavSmetaninSSM/The-Book-of-Eternity-Reborn/issues/1461).
- **Issue type**: Harness/RLM enhancement, validation hardening, canonical-state contract.
- **Spec Kit justification**: The change spans Mortal NPC state, Chaos Sea and Shining Abode actors, validation, cross-file authority, repair packets, client-owned fresh-game seeds, GM prompts, documentation, examples, manifests, and tests.
- **Contract scope**: GM-facing prompts; runtime state; validation; normalization preservation; repair loop; Mortal World; Chaos Sea; Shining Abode; documentation; worked examples; source guards. Console and browser remain readers of canonical state and must not expose contract metadata as player-facing prose.
- **Third re-review remediation scope**: R3-I-2, R3-I-3, and R3-M-1 are Mortal-only. They add the `NPCCoreChanges` Mortal response contract, setting-owned characteristic/carrying authority, and first-materialization personality completeness. They do not add, remove, rename, or alter any Chaos Sea or Shining Abode pending/control file, action type, response field, receipt, report, validator, normalizer, scheduler, lifecycle mode, or authority path.
- **Fourth re-review remediation scope**: R4-I-1 through R4-I-4 harden Mortal `npc_core.json` authority, historical-carrier continuity, Fate Card reduction, and canonical ID-less skill evidence. They add no Chaos Sea or Shining Abode pending/control surface. Because the reused Block 5 Combat Action validator now enforces its already documented mandatory effect `value`, the shared afterlife matrix and worked example explicitly document that inherited item/relic/skill contract while keeping `specialArts[].combatEffect` unchanged.
- **Out of scope**: Inventing actor content in the client; deriving professions, skills, items, roles, or capabilities from prose; forcing every actor to own an item or Fate Card when fiction explicitly says otherwise; redesigning NPC/afterlife player-facing screens; adding Mortal inventory to afterlife entities; retroactively fabricating content for untouched legacy actors.

## User Scenarios & Testing

### User Story 1 - Complete Mortal NPCs (Priority: P1)

As a player, when a significant Mortal NPC first appears, I can later inspect a coherent person rather than an identity stub: the NPC has personality, characteristics, goals, memory ownership, and explicit skill, possession, Fate Card, quest, and relationship state.

**Why this priority**: Hollow Mortal NPCs directly break dialogue, combat, training, trade, progression, and Actor Brain behavior.

**Independent Test**: Submit first-materialization NPC objects through `NPCsInScene` and `UpdateNPCs`. Confirm that populated profiles pass, accidental empty sections fail, and deliberately empty sections pass only with structured reasons.

**Acceptance Scenarios**:

1. **Given** a new Mortal NPC with non-empty characteristics and personality but empty skills, inventory, Fate Cards, personal quests, and relationships, **when** no materialization section dispositions are supplied, **then** validation blocks the turn and requests only the missing dispositions or content.
2. **Given** an itemless non-combatant NPC whose empty skill and possession sections are explicitly declared `empty_by_design` with non-empty in-world reasons, **when** the remaining required profile sections are complete, **then** validation accepts the materialization.
3. **Given** a new NPC explicitly declared able to teach, trade, or fight, **when** the corresponding canonical teacher, merchant, or skill authority is absent, **then** validation blocks the materialization without parsing the NPC name, occupation, history, tags, or narrative.
4. **Given** an existing NPC, **when** the GM changes inventory, skills, relationships, activity, or memory, **then** existing dedicated delta commands remain authoritative and the full materialization envelope is not resent.
5. **Given** a first-materialization Mortal NPC with `characteristics={}`, **when** its complete object is validated, **then** validation reports `npc_characteristics_empty`; at least one setting-defined numeric property is required without prescribing characteristic names.
6. **Given** a legacy Mortal NPC receives its first complete envelope during a structured promotion, **when** the schema-required `UpdateNPCs.inventory` differs from validated pre-turn inventory, **then** validation rejects the resend; the same promotion passes when the inventory snapshot is semantically unchanged and all mutations use dedicated atomic commands.
7. **Given** validated pre-turn state owns a permanent `NPCId`, **when** a current `UpdateNPCs` object sets its permanent ID to null and reuses that value as same-turn `initialId`, **then** validation rejects the identity collision and still applies existing-actor inventory continuity to changed, added, or removed entries.
8. **Given** an ordinary existing permanent NPC needs a supported worldview, race/history, location, progression, setting-owned characteristic, faction-affiliation, or locked/unrealized Fate Card definition change, **when** the GM sends one closed-schema `NPCCoreChanges` entry with exact permanent identity, reason, and absolute resulting values, **then** validation and normalization apply only those named fields and consume the command.
9. **Given** a current first-materialization Mortal NPC, **when** its `personalityTraits` array has fewer than three or more than five entries, or any trait omits an integer `value` from 1 through 10, **then** full-object validation rejects it without imposing that cardinality on untouched legacy records.

---

### User Story 2 - Complete Afterlife Actors (Priority: P1)

As a player, when a Guardian, resident, radiant actor, Shining faction head, Saref agent, or other significant afterlife entity appears, its identity resolves to a complete afterlife profile with spiritual progression, agency, relationships, Fate Cards, custom states, and actor-owned memory.

**Why this priority**: Afterlife actor data is currently fragmented across strong type-specific files and a partially optional common profile, allowing important entities to exist without inspectable or actionable authority.

**Independent Test**: Materialize each supported afterlife actor type and verify cross-file profile binding, section completeness, Actor Brain inputs, memory initialization, and bounded repair behavior in Chaos Sea and Shining Abode.

**Acceptance Scenarios**:

1. **Given** a newly significant afterlife entity, **when** no matching `afterlife_entity_profiles.json` profile is created, **then** the accepted turn is blocked and repair identifies the exact missing actor/profile link.
2. **Given** a new afterlife profile with currencies, progression, and arts but absent Fate Cards, relationships, agency, custom states, or progression history, **when** no explicit section dispositions exist, **then** validation blocks the profile as incomplete.
3. **Given** a profile section that is legitimately empty, **when** its materialization disposition is `empty_by_design` with a non-empty reason, **then** the profile can be accepted without client-authored narrative invention.
4. **Given** non-vacant Shining leadership, **when** its Guardian, resident, or radiant head lacks matching complete actor-profile authority, **then** cross-file validation rejects the leadership state or requests profile materialization.
5. **Given** a client-owned system Guardian created during New Game, **when** its canonical state is seeded, **then** the client deterministically writes a complete materialization envelope and does not ask the GM to replace it.
6. **Given** a new afterlife profile whose agency disposition is `populated`, **when** it contains only a mask or progression strategy without a goal, personal quest, current activity, or completed activity, **then** validation rejects the agency disposition.
7. **Given** an afterlife actor declares `canTrade=true`, **when** it is neither the exact active Guardian in the current Chaos Sea abode nor an operational secure/contested Shining faction head at trade tier 1 or higher, **then** validation fails closed without reading names, roles, or prose.
8. **Given** an already bound Guardian, resident, Radiant, or Saref legacy profile receives its first envelope, **when** its actual actor-owned memory is absent, **then** accepted-turn validation rejects the transition; the unchanged legacy profile remains readable when no envelope is added.

---

### User Story 3 - Safe Legacy Adoption and Repair (Priority: P2)

As a returning player, I can load an older save without the client silently inventing biographies, skills, inventory, or spiritual powers, while newly created or newly promoted significant actors must use the current contract.

**Why this priority**: Strict unconditional validation of every historical actor would make valid existing saves unusable; silently filling them would violate GM authority.

**Independent Test**: Load legacy files without materialization metadata, verify unchanged actors remain readable, then touch or promote one actor and confirm the repair loop requests only the missing current-contract sections.

**Acceptance Scenarios**:

1. **Given** an untouched legacy actor without a materialization envelope, **when** a save is loaded and the actor is not newly created or promoted in the current turn, **then** validation does not fabricate data or destroy the save.
2. **Given** that legacy actor becomes newly relevant through a first canonical profile promotion, teacher/merchant enablement, or Shining leadership appointment, **when** the turn is validated, **then** current-contract materialization is required.
3. **Given** a repair packet for an incomplete actor, **when** some sections are already valid, **then** repair preserves them and names only missing or contradictory sections.
4. **Given** a worker proposes a bounded actor repair, **when** the proposal also changes personality, another section, another actor, or unrelated root state, **then** the apply gate rejects the proposal before canonical files are written.
5. **Given** the canonical Guardian thought journal does not exist and one exact `guardian:<id>` memory-missing issue is routed to it, **when** a completed proposal uses `changeKind=add`, `beforeSha256=missing`, exact proposal-bound content, and one fresh owned entry under `{ "entries": [...] }`, **then** the apply gate accepts creation; wrong-owner or extra-root data remains rejected.
6. **Given** a memory-missing issue originates in Shining or Saref source state, **when** repair targets the common profile file, **then** the issue routes by exact `actorType:actorId` and only that profile's `gmThoughtsSummary` may change; another actor, root state, currencies, progression, envelope, or scalar change is rejected.
7. **Given** a worker proposal omits `status` or carries the zero/unspecified value, **when** it reaches deserialization, inbox, contract validation, or apply, **then** it is invalid and cannot be stored or applied as `completed`; explicit terminal statuses remain diagnostic with empty `changedFiles`.
8. **Given** `npc_initial_id_collides_with_existing_permanent_id`, **when** worker repair is considered or a crafted proposal reaches apply, **then** dispatch and apply fail closed to the main-GM rollback/repair path.
9. **Given** `npc_existing_inventory_resend_forbidden` with an exact validated pre-turn JSON-array snapshot, or `npc_characteristics_empty` for one exact actor/carrier, **when** a worker proposes only the issue-bound field correction, **then** it may apply; any same-actor sibling, other actor, root, add, delete, malformed target, or non-snapshot inventory change is rejected.

---

### User Story 4 - GM and Client Contract Clarity (Priority: P2)

As the GM agent, I receive one bounded, setting-agnostic authoring contract and worked examples for Mortal and afterlife actors; as a player, I never see its implementation metadata.

**Why this priority**: Validator-only behavior would create repair loops unless prompts and examples teach the same contract, while exposing raw metadata would degrade both clients.

**Independent Test**: Run documentation/source guards, inspect generated repair packets, and render representative actor details in console/browser tests to confirm metadata stays internal.

**Acceptance Scenarios**:

1. **Given** a first actor materialization request, **when** the GM reads the context packet, **then** it receives the correct type-specific template and no genre-specific keyword guidance.
2. **Given** a validation failure, **when** a repair packet is generated, **then** it contains actor identity, missing section names, allowed dispositions, and canonical target surfaces without asking for unrelated rewrites.
3. **Given** a complete materialization envelope in canonical JSON, **when** console and browser detail views render the actor, **then** they display gameplay data but not schema versions, receipt IDs, section-state tokens, or empty-state implementation labels.
4. **Given** the generated Mortal NPC template, **when** the GM authors `characteristics`, **then** the example contains only a setting-defined placeholder and directs the GM to current-world canonical characteristic authority rather than a universal genre list.
5. **Given** an existing-inventory validation repair, **when** the high-priority packet is generated, **then** it separately explains genuinely new actors, ordinary existing updates, and true legacy promotions. It removes the whole ordinary-existing full-object resend in favor of dedicated delta/command surfaces or main-GM fallback, and never tells the GM to remove schema-required promotion inventory.

### Edge Cases

- An actor can legitimately be itemless, non-combatant, unable to teach, unable to trade, without current quests, or without discovered Fate Cards. Each governed empty section requires an explicit in-world reason; absence or an empty string is not equivalent.
- A `populated` section must contain structurally valid data. A disposition cannot launder an empty or malformed section.
- `empty_by_design` conflicts with non-empty content and is rejected rather than normalized away.
- Capabilities are explicit booleans and must agree with canonical fields. `canTeach=true` requires teacher authority; `canTrade=true` requires merchant authority; Mortal `canFight=true` requires an active or passive skill; afterlife `canFight=true` requires at least one usable standard or special art.
- Arbitrary settings are supported. Validation never searches for fantasy, science-fiction, historical, modern, or post-apocalyptic words in names, occupations, descriptions, item labels, skill labels, or IDs.
- A background mention is not automatically a significant actor. Significance begins when the actor is canonically created, placed in relevant Actor Brain scope, receives a persistent update, becomes a teacher/merchant/combat participant, or occupies a supported political/leadership role.
- Vacant Shining leadership requires no head profile. A player-soul head resolves to the existing client-owned player profile.
- Guardians and residents retain their type-specific canonical files; the common profile complements rather than replaces those files.
- Afterlife possessions are not Mortal inventory. Trade stock, offerings, relic links, and mentor showcases remain their existing explicit capability surfaces.
- Legacy files are accepted only as unchanged baseline authority. A current-turn new actor cannot omit metadata by presenting a permanent-looking ID.
- A legacy Mortal promotion may repeat only an inventory snapshot semantically equal to validated pre-turn authority; changed, added, or removed inventory entries remain atomic-command-only.
- `NPCsInScene` is retained canonical scene state, not a second existing-actor mutation channel. A historical scene actor's protected core and inventory remain semantically unchanged unless the response uses the matching dedicated command or `NPCCoreChanges`.
- Characteristic keys, progression grants, and carrying-capacity formulas come only from explicit current-world authority. If no carrying authority or compatible item-weight authority exists, `maxWeight`/`totalWeight` remain setting-owned nullable results rather than triggering invented characteristic fields.
- A worker repairing `npc_characteristics_empty` must read and hash-pin `game_state/misc/characteristics.json` as read-only context. Missing, malformed, duplicate-member, non-object, or empty authority blocks worker dispatch/apply, and every proposed characteristic key must already exist in that authority.
- Optional `NPCCoreChanges` mutation groups are omitted when unused. Authoritative templates containing a present empty group are invalid and must be rejected by executable documentation guards before release.
- Player-facing output written during canonical repair is fresh only when it postdates the latest actual write of every repaired canonical target. Output written after repair starts but before the canonical mutation remains stale.
- Duplicate JSON members in current actor/inventory/materialization data or validated pre-turn actor/inventory authority fail closed as structured validation issues. Semantic comparisons remain property-order-insensitive, but malformed or duplicate JSON is never equal and never escapes validation as an exception.
- Malformed, non-object, or duplicate-member current `npc_core.json` authority fails closed before normalization even when no command can be safely materialized; it never turns into an empty NPCCore issue list or an unhandled exception.
- Every actor-owned field of a historical Mortal NPC is continuous across full carriers. Direct changes to personality, skills, goals, relationships, journals, activities, equipment, teacher/trade state, or other owned domains are rejected unless the exact dedicated command contract authorizes them.
- A Fate Card added through `NPCCoreChanges` must satisfy the same production validator as a Fate Card embedded in a complete NPC, including nested skill, combat action, effect type, value, duration, poise, and target constraints.
- A complete Block 7 active or passive skill remains combat evidence without `skillId` or `id`; optional identifiers cannot determine capability or promotion semantics.
- A missing Guardian thought journal is treated as `{ "entries": [] }` only inside the exact issue-bound Add path. Other missing actor files, owners, issue codes, or proposed roots remain fail-closed.
- Generated worker audit event IDs remain readable but must be unique even when multiple helper calls occur in the same millisecond.

## Requirements

### Functional Requirements

- **FR-001**: The system MUST define a versioned `materialization` envelope shared conceptually by new Mortal NPC objects and new afterlife entity profiles.
- **FR-002**: The envelope MUST contain exact `actorType` and `actorId` bindings, a stable `materializationId`, `materializedAtTurn`, `schemaVersion`, `state=complete`, explicit capability booleans, and per-section dispositions.
- **FR-003**: A governed section disposition MUST be either `populated` or `empty_by_design`; the latter MUST include a non-empty in-world reason.
- **FR-004**: Validation MUST reject a missing disposition, a `populated` disposition with empty content, an `empty_by_design` disposition with content, unknown section keys, duplicate authority, and malformed envelope fields. Duplicate members in current or validated pre-turn actor, inventory, and materialization authority MUST produce structured issues without escaping `ValidateGameStateAsync`; semantic equality MUST remain property-order-insensitive while treating malformed or duplicate JSON as unequal.
- **FR-005**: Mortal first materialization MUST govern combined skills, inventory/equipment, Fate Cards, personal quests, and NPC relationships through dispositions while continuing to require existing core identity, personality, characteristics, goals, progression, location, and memory fields. It MUST contain 3-5 `personalityTraits`, and every trait MUST include integer `value` in the inclusive range 1-10. `characteristics` MUST contain at least one numeric property, but the validator MUST NOT prescribe setting-specific characteristic names. Personality cardinality applies to current first materialization/full promotion, not arbitrary untouched legacy records.
- **FR-006**: A Mortal NPC with `canFight=true` MUST have at least one valid active or passive skill.
- **FR-007**: A Mortal NPC with `canTeach=true` MUST have canonical `teacherProfile.canTeach=true` and non-empty teacher skill authority.
- **FR-008**: A Mortal NPC with `canTrade=true` MUST have canonical merchant/trade authority; initial stock may remain pending only through the existing explicit trade request contract.
- **FR-009**: A Mortal NPC with `ownsItems=true` MUST have non-empty canonical inventory, and equipped item references MUST resolve to that inventory.
- **FR-010**: Existing Mortal NPCs MUST mutate through their dedicated delta commands or the closed `NPCCoreChanges` core command; validators MUST forbid using either full carrier, the first-materialization envelope, or a null permanent ID plus colliding same-turn `initialId` to bypass existing update contracts. Validation MUST derive one effective identity from canonical permanent/same-turn fields, reject reuse of any validated pre-turn permanent `NPCId` as `initialId` with stable code `npc_initial_id_collides_with_existing_permanent_id`, and apply exact pre-turn inventory continuity to both `UpdateNPCs` and `NPCsInScene`. A true legacy promotion MAY include the schema-required inventory snapshot only when it is semantically identical to validated pre-turn inventory; any add, removal, or change through either full carrier MUST be rejected and authored through `NPCInventoryAdds`, `NPCInventoryUpdates`, or `NPCInventoryRemovals`.
- **FR-010a**: `NPCCoreChanges` MUST be a Mortal-only non-carrier command mapped to `game_state/npcs/npc_core.json`. Each entry MUST target one exact existing permanent `NPCId`, require a non-empty `reason` and at least one non-empty closed mutation group, reject unknown/protected members recursively, and carry only absolute resulting values. Allowed groups are exactly profile (`worldview`, `race`, `history`), paired location, coherent progression, setting-authorized finite numeric `characteristicValues`, exact faction-affiliation upserts, complete new initially locked Fate Cards, and validated pre-turn locked/unrealized Fate Card IDs to remove. The reducer MUST update every unambiguous canonical mirror, preserve all siblings and materialization, consume only valid commands, and leave invalid commands for repair. Existing dedicated commands retain authority for rename, unlock, inventory/equipment, skills/mastery, relationships/locks, journals/memory, goals/quests, activities, masks, custom states, and teacher/trade capabilities.
- **FR-011**: A newly significant non-player afterlife actor MUST have a matching common afterlife entity profile unless its actor type is explicitly client-owned and documented as equivalent authority.
- **FR-012**: New afterlife profiles MUST govern standard arts, special arts, custom states, Fate Cards, relationships, actor agency, and progression history through dispositions and MUST initialize actor-owned memory. A populated agency section MUST contain meaningful goals, personal quests, a current activity, or completed activity history; masks, disposition, or progression strategy alone MUST NOT satisfy agency.
- **FR-013**: An afterlife actor with `canFight=true` MUST have at least one usable standard or special spiritual art.
- **FR-014**: An afterlife actor with `canTeach=true` MUST have canonical mentor authority and at least one teachable art or explicit supported teaching surface.
- **FR-015**: An afterlife actor with `canTrade=true` MUST resolve to exact current realm authority: the one active Guardian in the current Chaos Sea abode, or a non-player secure/contested head of an operational Shining faction at trade tier 1 or higher. Mortal NPC trade files MUST remain forbidden in afterlife realms, and names, roles, descriptions, or genre vocabulary MUST NOT create authority.
- **FR-016**: Cross-file validation MUST bind Guardians, residents, Shining political actors, and non-vacant Shining faction heads to exact actor type and stable ID profile authority.
- **FR-017**: Actor Brain inputs and actor-owned memory MUST be initialized on first materialization, including the first envelope added to an already bound legacy profile. Guardian and resident transitions MUST use their dedicated current journal authority; Radiant, Saref, and other common-profile actors MUST use the exact profile's non-empty `gmThoughtsSummary`. Unchanged legacy profiles without a new envelope remain loadable, and later significant decisions remain append-only under existing memory rules.
- **FR-018**: The client MUST determine new/current-turn materialization from validated pre-turn authority and current structured commands, not from display names, ID prefixes, prose, or current pathname state alone.
- **FR-019**: Untouched legacy actors MUST remain loadable without invented data. Current-turn creation, promotion, or newly significant role assignment MUST trigger the current contract.
- **FR-020**: Repair packets MUST preserve valid actor data and report only missing, empty, contradictory, or unbound sections for the exact stable actor identity. Mortal inventory repair guidance MUST distinguish genuinely new actors, ordinary existing updates, and true legacy promotions. An ordinary existing full-object resend MUST be removed as a whole, with every legitimate supported change re-authored through its dedicated delta/command surface; when no required surface exists, repair MUST use the main-GM rollback/repair path rather than retain a schema-invalid full object without `inventory`. Promotion repair MUST retain schema-required inventory and restore the exact validated pre-turn snapshot while real mutations use dedicated commands.
- **FR-020a**: The worker apply gate MUST compare a proposed actor repair with canonical JSON and MUST reject protected actor data changes outside the exact actor and named repair section. Memory-missing issues MUST route by actor type: Guardian to the canonical thought journal or explicitly supported Guardian memory path, resident to resident state/journal, and Radiant/Saref/other common-profile actors to `afterlife_entity_profiles.json`. Common-profile repair may mutate only the exact actor's `gmThoughtsSummary`; unrelated actors, root state, currencies, progression, envelope, and scalar fields are protected. The only missing-baseline actor-memory creation exception is an exact Guardian thought journal Add routed solely by `afterlife_actor_materialization_memory_missing` for one exact `guardian:<id>`; preservation treats that baseline as `{ "entries": [] }` and accepts exactly one fresh meaningful owned entry.
- **FR-020b**: Validation-repair proposals MUST pin exact canonical bytes with 64-character SHA-256 context/before hashes, bind every non-delete result to its own proposal-scoped content and exact after hash, and apply or roll back through one cross-process compare/exchange write protocol so a concurrent canonical writer cannot be overwritten.
- **FR-020c**: Worker dispatch MUST happen before exposing the legacy main-GM repair request. Once the apply gate accepts a worker repair, the worker remains the sole repair owner even if ready-signal publication fails; the client MUST revalidate directly and MUST NOT start a second legacy repair. Player-facing output freshness MUST use an explicit repair boundary rather than depend on a legacy request file.
- **FR-020d**: Worker proposal `status` MUST be explicitly present and MUST NOT default to `completed`; omitted, zero/`unspecified`, or unknown values are invalid at deserialization/contract boundaries and MUST NOT be stored or applied. Only an explicit `status=completed` proposal MAY enter the apply gate. `failed`, `timed-out`, and `rejected` proposals MUST carry an empty `changedFiles` collection, remain diagnostic evidence only, and MUST NOT mutate canonical state.
- **FR-020e**: Worker audit appends MUST serialize their read-and-append operation under the shared cross-process canonical-write lock. Every worker audit producer MUST obtain its event ID from one shared generator using readable UTC milliseconds plus a unique 32-hex GUID suffix; hand-built producer IDs are forbidden by source guard, and generated IDs MUST remain unique under concurrent or same-millisecond calls. Failure to publish audit telemetry after a canonical apply is accepted MUST NOT roll back or revoke the accepted canonical bytes.
- **FR-020f**: The stable Mortal issue codes `npc_initial_id_collides_with_existing_permanent_id`, `npc_existing_inventory_resend_forbidden`, and `npc_characteristics_empty` MUST carry exact `mortal_npc:<id>` actor coordinates and explicit sections. Identity collision MUST remain main-GM-only. Inventory repair MAY dispatch only with an exact validated pre-turn JSON-array snapshot and MAY replace only that actor/carrier's inventory with that snapshot. Characteristics repair MAY replace only that actor/carrier's empty object with one or more setting-defined numeric properties. The apply gate MUST reject every unrelated same-actor field, other actor, root mutation, unsupported add/delete, malformed target, or broader rewrite before post-apply validation.
- **FR-021**: Normalization MUST preserve valid materialization metadata but MUST NOT generate narrative reasons, capabilities, skills, inventory, Fate Cards, goals, relationships, or profile content.
- **FR-022**: System Guardian fresh-game builders MUST emit deterministic valid envelopes for their client-owned Guardian/profile seeds, and every capability boolean, including `canTrade`, MUST match the exact current seeded authority.
- **FR-023**: GM prompts, Mortal NPC documentation, afterlife contract documentation when applicable, worked examples, manifests, and source/documentation guards MUST be updated in the same change. Generated Mortal templates MUST document `NPCCoreChanges`, require 3-5 integer-valued first-materialization personality traits, use setting-defined characteristic placeholders, and MUST NOT publish a universal characteristic-key list or carrying/progression formula. Mortal-only waves MUST record why afterlife contracts remain unchanged; changes to shared validators used by afterlife authoring MUST update the applicable afterlife matrix/example even when no pending/control surface changes.
- **FR-024**: Player-facing console and browser projections MUST ignore materialization metadata unless an explicit advanced/debug mode requests it.
- **FR-025**: Tests and whole-source guards MUST prove that no materialization decision or authoritative Mortal carrying/progression rule uses genre-specific keyword dictionaries, prose inference, universal characteristic names/formulas, or warrior/mage characteristic assumptions. Narrative uses of ordinary words remain legal when they do not grant mechanical authority.
- **FR-026**: Pre-normalization validation MUST parse current `npc_core.json` with duplicate-member detection and MUST emit a stable blocking issue for malformed JSON, a non-object root, or any duplicate property. Parsing/materialization failures MUST NOT silently skip `NPCCoreChanges` validation or escape accepted-turn validation as exceptions.
- **FR-027**: For a Mortal actor present in validated pre-turn authority, both `NPCsInScene` and envelope-free `UpdateNPCs` MUST preserve every actor-owned field unless the response uses the exact dedicated command for that domain. Whole-carrier continuity MUST cover personality, skills, goals/quests, relationships/locks, journals/memory, activities, inventory/equipment, teacher/trade state, and all other actor-owned fields while preserving genuine first materialization.
- **FR-028**: `NPCCoreChanges.fateCardsToAdd` MUST pass the same complete production Fate Card/skill/combat validation used by canonical NPC validation before any mutation is reduced or any command is consumed. Any nested failure MUST leave all actor mirrors and the command unchanged.
- **FR-029**: Mortal `canFight` consistency and current-turn promotion detection MUST recognize a production-valid active or passive skill by its canonical structured content. Optional `skillId`/`id` members MUST NOT be required as combat evidence.
- **FR-030**: A worker repair for `npc_characteristics_empty` MUST include `game_state/misc/characteristics.json` as hash-pinned read-only context without granting proposal/write authority. Dispatch and apply MUST fail closed when that authority is missing, malformed, duplicate-bearing, non-object, or empty. Every proposed characteristic MUST resolve exactly to a non-metadata key in that authority and MUST be a finite numeric value; the worker MUST NOT invent setting vocabulary or materialize NaN/infinity-equivalent JSON numbers.
- **FR-031**: Every authoritative `NPCCoreChanges` template MUST omit unused optional mutation groups and MUST contain at least one non-empty group. Production-path documentation tests MUST extract and validate the exact templates supplied to the GM; vocabulary-only source checks are insufficient.
- **FR-032**: After an accepted canonical repair, player-facing output MUST be strictly newer than the latest actual write of each canonical target repaired in that cycle. Repair-request creation time, worker dispatch time, or loop start time MUST NOT substitute for canonical mutation time. The repair loop MUST retain the original canonical target set and boundary through any derived output-only retry until final acceptance; a later rewrite of an original target MUST re-stale the output. If a required repaired target has no trustworthy mutation boundary, freshness MUST fail closed.
- **FR-033**: Every afterlife or mixed validation-repair worker task MUST include `game_state/meta/soul_state.json` as exact hash-pinned read-only realm authority without granting proposal/write authority. Dispatch and apply MUST fail closed when that authority is missing, malformed, duplicate-bearing, non-object, lacks one unambiguous supported `currentRealm`, changes concurrently, or does not match the task's afterlife realm gate.
- **FR-034**: Worker changed-file operations MUST use exactly `Add`, `Replace`, or `Delete`. Omitted, zero, undefined numeric, or otherwise unsupported `WorkerFileChangeKind` values MUST be rejected by contract validation and MUST never reach apply semantics.
- **FR-035**: Every worker subprocess MUST run against an ephemeral detached execution snapshot under client-owned `.worker_runtime`, containing only exact hash-pinned task context. The live canonical session MUST NOT be exposed as the worker session path. Direct snapshot edits and undeclared artifacts MUST be discarded; only a contract-valid proposal and the exact bytes of its declared non-delete `contentRef` entries may be imported into the live proposal inbox.
- **FR-036**: Every `game_state/meta/` validation-repair target MUST be classified as afterlife-scoped, including non-actor metadata, and mixed Mortal/afterlife issue batches MUST fail task construction closed. The apply gate MUST hold one canonical write lease from final exact-byte context/authority verification through all target writes, complete-state validation, read-only context revalidation, rollback when required, and decision linearization. Setting and Soul authority paths MUST remain read-only in every standalone or mixed task.
- **FR-037**: Before publishing any worker handoff artifact, the bridge MUST verify every declared non-delete `contentRef` against its exact `afterSha256`. Every validation-repair dispatch MUST have a globally unique task ID while retaining its attempt number for audit. Task identifiers MUST be immutable and reserved by create-only compare/exchange across bridge-pool instances. A proposal MUST be staged as one complete bundle containing `proposal.json` and all declared content, then published through one create-only atomic directory rename while the canonical lease verifies the exact originating task bytes still belong to the current session generation. The durable bundle MUST remain authoritative if derived inbox or audit publication fails. Each profile's `MaxConcurrentTasks` MUST be enforced before task publication through reference-counted gates that retire when idle; cancellation MUST kill and await the complete worker process tree before releasing its slot. Malformed handoff data MUST NOT erase an observed timeout. Canonical session-path identity MUST be case-insensitive, duplicate aliases MUST fail closed, `lore/current_world/**` and `game_state/core/player_status.json` MUST classify as Mortal, and every exact afterlife validation-repair allowlist surface MUST belong to `game_state/meta/**`; typed afterlife content tasks MAY use exact task-provided control/report surfaces. Standalone identity-collision repair MUST remain main-GM-only. Detached cleanup MUST NOT traverse reparse points, and cleanup failure MUST remain diagnostic without replacing the worker result.
- **FR-038**: Built-in backup, restore, clear, save, load, worker apply, proposal publication, state refresh, and client-owned mirror repair MUST share one external canonical write-lease protocol without a replaceable in-session apply lock. Load MUST use a durable external journal and staged/backup directories under `.boe_runtime/load-transactions`, recover an interrupted swap before ordinary session-directory initialization, preserve the last valid backup and active journal when rollback fails, restore in-memory state after recovery, and allow lease-aware refresh/mirror operations without reacquiring the same lease.

### Key Entities

- **Actor Materialization Envelope**: Versioned proof that a first canonical actor creation addressed every governed content section and declared gameplay capabilities explicitly.
- **Section Disposition**: `populated` or `empty_by_design` state for one governed actor section, with a required reason for deliberate emptiness.
- **Actor Capabilities**: Explicit setting-neutral booleans for combat, teaching, trade, and possessions; these constrain but never generate canonical content.
- **Mortal NPC Materialization**: Existing complete NPC object plus semantic section dispositions and capability consistency.
- **NPC Core Change**: One closed Mortal-only ordinary-existing command entry targeting exact permanent identity and carrying only allowed absolute profile/location/progression/setting-owned characteristic/faction/Fate Card definition results.
- **Afterlife Entity Profile Materialization**: Common profile for Guardian, resident, Shining resident/head, radiant actor, Saref/system actor, player soul, or custom afterlife actor plus type-specific cross-file links.
- **Materialization Repair Packet**: Bounded GM task describing exact actor identity and only the missing or contradictory sections.

## Success Criteria

### Measurable Outcomes

- **SC-001**: All focused tests reject 100% of new Mortal NPC fixtures that omit governed content without explicit empty-state reasons.
- **SC-002**: All supported valid minimal Mortal NPC fixtures pass with 3-5 integer-valued traits and without requiring a genre-specific characteristic, skill, item, profession, carrying formula, or Fate Card.
- **SC-003**: New Guardian, resident, radiant actor, and Shining faction-head fixtures cannot pass without exact common-profile binding or an explicitly documented client-owned equivalent.
- **SC-004**: Existing unchanged legacy fixtures remain valid and no test observes client-authored narrative content added during normalization or repair preparation.
- **SC-005**: Repair tests show that already valid actor sections remain semantically equivalent, only the exact named repair subtree may change, actor-type-aware memory routing protects unrelated profile/root data, stale-byte proposals and concurrent overwrite/rollback attempts are rejected, omitted/unspecified/non-completed proposal statuses and undefined changed-file operations cannot enter apply, audit append races lose no events, every audit producer uses the shared collision-safe ID generator, audit publication failure cannot revoke an accepted apply, characteristic repair cannot exceed hash-pinned setting authority or introduce non-finite values, afterlife worker realm authority is exact-byte pinned for every `game_state/meta/` target, direct worker writes in a detached snapshot cannot mutate the live session, every declared import digest is verified before publication, repeated repair cycles receive unique task IDs, concurrent task reservations and complete-bundle proposal publications admit one winner without partial durable evidence, stale worker results from an earlier session generation are rejected, `MaxConcurrentTasks` gates publication across pool instances, canceled workers and children exit before slot reuse, idle gates retire, timeout truth survives malformed handoff data, canonical path aliases, foreign/wildcard afterlife validation-repair surfaces, and mixed current-world-lore/core-status repairs are rejected without disabling typed task-provided control/report surfaces, detached cleanup never follows reparse points or replaces the worker result, one canonical write lease closes context/authority TOCTOU windows through decision, player output remains strictly newer than every original canonical target throughout derived output-only retries, and proposals that alter protected actor data are rejected before apply.
- **SC-006**: Mortal World, Chaos Sea, and Shining Abode worked examples pass documentation validation and demonstrate at least one populated and one deliberately empty section.
- **SC-007**: Source guards find no actor materialization branch based on genre-specific names, descriptions, occupations, tags, labels, IDs, or keyword tables.
- **SC-008**: Existing console/browser actor detail tests continue to pass without rendering materialization metadata in player mode.
- **SC-009**: Save/load tests prove interrupted swaps recover before session initialization, failed rollback retains the journal and last valid backup for startup retry, restored bytes and in-memory state agree, mirror repair reuses the active lease without deadlock, and load cannot cross a worker-apply decision interval.

## Verification Plan

- **C# verification**: `dotnet test BookOfEternityClient.Tests\BookOfEternityClient.Tests.csproj --no-restore --filter "ActorMaterialization|NpcCoreChanges|NpcFullObject|AfterlifeEntityProfile|GuardianAbodeResident|ShiningLeadership|SystemGuardianLibrary|GmWorker|ValidationRepair"`
- **Documentation/contract verification**: `dotnet test BookOfEternityClient.Tests\BookOfEternityClient.Tests.csproj --no-restore --filter "ExampleDocumentationValidationTests|AfterlifeDocumentationCoverageTests|ValidationSourceGuardTests|GameEngineSourceGuardTests"`
- **Frontend verification**: Existing browser projection tests affected by actor DTO changes; run `npm run verify` only if frontend source changes.
- **Manual/player-facing verification**: Fresh Mortal opening with populated actor; intentionally minimal itemless actor; Chaos Sea Guardian/resident creation; Shining faction-head appointment; inspect `/нпс` and `/профили_загробья` to confirm no metadata leakage.

## Assumptions

- “Complete” means every governed section is intentionally addressed, not that every section must contain an entry.
- The GM remains semantic authority for all narrative actor content and reasons.
- Existing explicit teacher, merchant, combat, inventory, Guardian, resident, and Shining leadership contracts remain authoritative and are cross-validated rather than replaced.
- Actor materialization metadata is private canonical/harness authority and is not inherently player-facing.
- Player soul remains client-owned for currencies, progression, and ordinary spiritual arts; its existing profile is not forced through non-player requirements.
