# Afterlife Contract Matrix — Chaos Sea and Shining Abode

This guide is mandatory operational context for every GM turn where `game_state/meta/soul_state.json.currentRealm` is `Chaos Sea`, `Море Хаоса`, `Shining Abode`, or `Сияющая Обитель`, and for any turn whose `progressionControl` contains afterlife debt.

The GM normally does not need to read client code. Use this matrix to decide which afterlife contracts are active, which files must be read, which canonical surfaces must be written, and which Mortal World substitutions are forbidden. Use `Examples/E_CLI_Afterlife_Turns.txt` for full worked JSON fragments after selecting the relevant contract rows here. If the matrix and examples still leave a mechanical contract unclear, or a validation repair points at an exact schema/surface mismatch, the GM may inspect client code as a fallback source of truth for file mappings, allowed fields, validators, canonical state surfaces, receipts, reports, and normalizers. This fallback never replaces the prompts and must not be used to invent new gameplay outcomes or bypass afterlife rules.

## Universal Afterlife Rules

- Always run realm gate first: ordinary `Chaos Sea`, ordinary active `Shining Abode`, or `Shining Abode pending-bootstrap handoff`.
- Always read `input/turn_request.json.progressionControl` before selecting the scene, tone, relevant actors, or player-action resolution.
- Always inspect `game_state/control/` pending afterlife files before deciding that the turn is only narrative.
- Always inspect non-pending afterlife control guards such as `system_guardian_attraction.json` and `afterlife_return_guard.json`; these are client-owned contracts/guards, not optional lore.
- If a pending file exists, it is a client-authored contract. It must close through the exact canonical state/receipt surface named below.
- Realm-wrong pending files are not free authoring prompts. A valid MortalWorldProfile-only companion manifestation file is preserved as next-life context, while valid empty request files may be removed as stale clutter; malformed pending files are preserved for repair. Non-empty or malformed Mortal-only NPC social/trade pending files block Soul Gates until repair or Mortal resolution. The GM must not close realm-wrong files by inventing afterlife receipts, Mortal NPCs, world events, or manual notifications.
- If afterlife scheduler debt is due, write `game_state/control/progression_report.json` through `progressionProcessingReport`.
- `game_state/control/progression_schedule.json` is client-owned scheduler state. The GM reads `input/turn_request.json.progressionControl` as the turn contract and writes only `progression_report.json`; never edit schedule baselines, pending cycle counts, `last*Ordinal`, or catch-up markers directly.
- Copy `sessionId`, `requestId`, and `turnNumber` exactly from the current request into every required report/receipt that carries turn correlation.
- Declare all changed Guardians, residents, Shining factions, halls, gates, head actors, and institutions in `gm_thoughts_markdown` actor scope.
- Afterlife spiritual conflicts are legal only through `afterlifeSpiritualConflictUpdate` and `game_state/meta/afterlife_spiritual_conflict_state.json`. They never use Mortal combat files (`enemiesData`, `alliesData`, `combat_log_markdown`), HP/energy/poise/status changes, Mortal skills, NPC/world/faction files, or direct resource rewards.
- Do not write `game_state/control/afterlife_notifications.json`; the client derives notifications from canonical state and receipts such as archive receipts, trade receipts, resident/Guardian social receipts, Shining core receipts, and Shining blessing-effect consumption.
- Do not satisfy afterlife meaning through Mortal World channels. File-level rule: during `Chaos Sea` / `Shining Abode`, no response surface may write or mutate any file mapped to `game_state/core/player_status.json`, `game_state/player/*`, `game_state/inventory/*`, `game_state/world/*`, `game_state/npcs/*`, `game_state/combat/*`, `game_state/factions/*`, `lore/current_world/*`, Mortal quest files (`game_state/quests/regular_quests.json`, `quest_history.json`, `plot_outline.json`), or Mortal misc files (`game_state/misc/characteristics.json`, `vehicles.json`, `storage_access.json`, `player_interactions.json`). This includes `UpdateNPCs`, `NPCsInScene`, `UpdateQuests`, `worldEventsLog`, `factionDataChanges`, `factionProjectUpdates`, `completeFactionProjects`, `factionChronicleUpdates`, `currentLocationData`, `worldMapUpdates`, `timeChange`, `setWorldTime`, `weatherChange`, combat fields, XP, skills, money, health, poise, ordinary item inventory, vehicles, storage access, and current-world lore.

## GM Decision Loop

Use this loop for every `Chaos Sea` / `Shining Abode` turn before writing files:

1. Classify the realm mode. If this is Shining pending-bootstrap handoff, stop ordinary afterlife processing and preserve `preparedIncarnationPackage`. If the package is present but invalid, fail closed: preserve the package and all pending Shining files for repair.
2. Read `progressionControl` and list every due contour, expected count, catch-up pressure tier, and catch-up contour.
3. Read all afterlife pending files. Each present pending file activates one row in this matrix and must close through the exact state surface and receipt listed here.
4. Resolve in order: bounded catch-up summary outcomes first, ordinary due cycles second, pending contracts and direct player action third.
5. Write only afterlife state surfaces plus exact receipts/reports. If a due contour produces no state mutation, record the reason in `gm_thoughts_markdown` and still report the processed cycle.
6. Audit the response for forbidden Mortal World substitutions, missing actor scope, missing receipts, stale gates, and package mutation before terminal completion.

## Realm Mode Matrix

| Mode | When active | What GM may process | What GM must suppress |
|---|---|---|---|
| Unresolved realm fault | `currentRealm` is missing, null, or empty | No gameplay systems; preserve state and require authoritative `soul_state.currentRealm` repair | Do not infer Chaos Sea; do not run afterlife scheduler, pending contracts, Mortal World systems, or lifecycle handoffs |
| Ordinary `Chaos Sea` | `currentRealm = Chaos Sea`/`Море Хаоса`, no active Shining package handoff | Guardians, Abodes, Soul state, Soul Relics, direct Chaos Sea gacha, afterlife Ink Feather whitelist, Guardian projects, residents, archive, ordinary incarnation and ascension choices | Mortal World combat/NPC/location/faction/time systems |
| Ordinary active `Shining Abode` | `currentRealm = Shining Abode`, `shining_abode_state.json.availability = active`, and `preparedIncarnationPackage = null`/absent | Shining civic state, Shining factions, Shining trade, Shining core actions, Guardians/residents that persist into the Abode, afterlife scheduler | Mortal World systems, sealed Shining gameplay, pending-bootstrap handoff, and ordinary Chaos-only lifecycle shortcuts that do not apply in Shining |
| `Shining Abode pending-bootstrap handoff` | `currentRealm = Shining Abode`, `preparedIncarnationPackage` is a valid bootstrap package object, and Soul Gates found no unresolved afterlife pending/control contracts | Only `TriggerIncarnation` / `game_state/control/incarnation_trigger.json` for the frozen package; client performs Mortal bootstrap after acceptance | Ordinary Shining scheduler, Guardian/Abode roleplay, Shining core actions, trade, politics, package clearing/mutation, unrelated pending contract closure, GM-side Mortal bootstrap materialization |
| `Shining Abode package fault` | `currentRealm = Shining Abode` and `preparedIncarnationPackage` is present but not an object or fails package validation | No ordinary Shining gameplay; preserve state for repair | Do not infer ordinary active Shining, do not process Shining core actions, do not delete/null the package |

## Living-World Scheduler Matrix

Each due contour is mandatory. If the correct outcome is stability/no mutation, the GM still explains that decision in `gm_thoughts_markdown` and reports the contour as processed.

| Contour | Trigger | GM must read | Legal outputs | Required report fields | Example |
|---|---|---|---|---|---|
| Chaos Sea hub | `mustEvaluateChaosSeaProgression=true` | `soul_state.json`, `guardians.json`, `guardian_projects.json`, `guardian_abode_residents.json`, relevant pending files | Narrative hub pressure, Guardian/Soul/Abode state when concrete systems change | `chaosSeaCyclesProcessed`, `newLastChaosSeaSimulationOrdinal` | 1, 16 |
| Guardian projects | `mustEvaluateGuardianProjectProgression=true` | `guardian_projects.json`, `guardians.json`, Guardian journals, relevant project/power pending state | `startGuardianProjects`, `guardianProjectUpdates`, `completeGuardianProjects`, `guardianThoughtJournalUpdates`, `guardianSocialJournalUpdates`, `guardianPowerEvents`, limited `UpdateGuardians` | `guardianProjectCyclesProcessed`, `newLastGuardianProjectCycleOrdinal` | 1, 5, 16 |
| Resident agency | `mustEvaluateResidentAgencyProgression=true` | `guardian_abode_residents.json`, resident pending files, resident-linked Soul Quest state | `residentThoughtJournalUpdates`, `residentInteractionLogUpdates`, `UpdateGuardianAbodeResidentHistoryLog`, `UpdateGuardianAbodeResidents`, `UpdateSoulQuests`, resident relic reward surfaces | `residentAgencyCyclesProcessed`, `newLastResidentAgencyCycleOrdinal` | 1, 10, 11, 17 |
| Shining Abode civic state | `mustEvaluateShiningAbodeProgression=true` | `shining_abode_state.json`, halls, gates draft, radiance, Light Sparks, client-owned treasury, core receipts, package state | Canonical `shining_abode_state.json` mutation plus Shining receipt arrays when a contract is active; treasury operations are local client-owned mutations with no GM receipt | `shiningAbodeCyclesProcessed`, `newLastShiningAbodeCycleOrdinal` | 2, 14, 17 |
| Shining factions | `mustEvaluateShiningFactionProgression=true` | `shining_abode_state.json.factions[]`, `shiningPoliticalActors[]`, resident alignment, leadership, faction pending files | Shining faction state, political actor registry updates, faction receipts, leadership history, resident ascension/alignment updates | `shiningFactionCyclesProcessed`, `newLastShiningFactionCycleOrdinal` | 2, 9, 17 |
| Shining trade | `mustEvaluateShiningTradeProgression=true` | Shining factions, trade inventory, trade receipts, pending Shining trade requests | Faction `tradeInventory`, `tradeInventoryReceipts[]`, trade availability/sold-out state | `shiningTradeCyclesProcessed`, `newLastShiningTradeCycleOrdinal` | 2, 17 |
| Bounded afterlife catch-up | `afterlifeCatchupRequired=true` | All state for contours listed in `afterlifeCatchupContours[]` | Exactly `afterlifeCatchupSummaryEventsRequired` high-level summary outcomes across listed contours | `afterlifeCatchupProcessed=true`, `afterlifeCatchupSummaryEventsProcessed` equal to required count, plus per-contour fields for processed contours | 1, 2 |

Scheduler report rule: each `newLast*Ordinal` belongs only to its own contour. Do not advance Guardian, resident, Shining faction, or trade markers by the largest backlog from another contour.

## Living-World Outcome Selection

- Chaos Sea hub cycles answer what the Sea itself did while the player focused elsewhere: soul-current pressure, Abode omens, Guardian politics, relic/archive currents, or metaphysical hazards. If nothing changes, explain the stable equilibrium and still process the contour.
- Guardian project cycles answer which Guardian plan advanced, stalled, stabilized, created pressure, relieved pressure, or produced a musing/lore/power consequence. Use project surfaces and Guardian journals, not NPC activity.
- Resident agency cycles answer which resident thought, remembered, requested, withdrew, helped, realigned, transferred, unlocked history, linked a Soul Quest, or granted a relic. Residents are authored afterlife actors and must not freeze between player visits.
- Shining Abode cycles answer which hall, gate draft, public ritual, Radiance pressure, Light Spark context, civic order, or Abode-wide tension changed. If a Shining mutation invalidates gate inputs, mark open gates stale.
- Shining faction cycles answer which institution changed strength, support, leadership, claims, loyalty, resident alignment, unrest, alliance, or project pressure. Use Shining faction state, not Mortal faction files. If leadership uses `headActorType = radiant_actor`, maintain the `shiningPoliticalActors[]` registry so `headActorId` resolves to an existing `actorId`; accepted radiant-actor succession sets the new head actor to `currentFactionId=<factionId>`, `politicalStatus=head`, and moves a replaced radiant head to `politicalStatus=former_head`.
- Shining trade cycles answer which faction trade inventory, rarity ceiling, service multiplier, sold-out state, or availability changed. Use Shining trade receipts and canonical inventories.
- Catch-up cycles are never simulated one by one. Produce exactly the requested number of summary outcomes and map each summary to the affected `afterlifeCatchupContours`.

## Pending Contract Matrix

| Pending/control file | Valid mode | GM must write | Required closure proof | Forbidden/common mistake | Example |
|---|---|---|---|---|---|
| Client-authored pending/control files in this matrix | Pre-turn input contract | Close through the row's canonical response/state surfaces only | The live pending/control file remains byte-identical to the validated pre-turn snapshot until the client cleanup step removes it | Do not rewrite, retarget, replace, clear, or "advance" the pending/control file in GM output; validator treats valid-looking replacements as GM-side mutation | All |
| `game_state/meta/afterlife_spiritual_conflict_state.json` / `afterlifeSpiritualConflictUpdate` | Ordinary `Chaos Sea` or ordinary active `Shining Abode`; GM starts by roleplay, responds to `[AFTERLIFE_SPIRITUAL_ACTION: conflictId]`, or resolves ordinary player prose that clearly acts inside an existing `activeConflict` | `afterlifeSpiritualConflictUpdate` with `mode=start`, `exchange`, `resolve`, or `repair_cancel` | Active state uses `activeConflict` with `conflictId`, `realm`, `sideModel`, `playerSide`, `oppositionSide`, side strain fields, `conflictPosition`, `resolutionState`, and `exchangeLog[]`; resolved state moves into `recentConflicts[]`; contested `exchange` / contested `resolve` must carry `diceAudit` from `input/turn_request.json.preGeneratedDices1d20`; command tags are optional routing help but, when present, must match active `conflictId` | Do not write Mortal combat fields; do not resolve a contested outcome from GM preference/prose alone; do not require a command tag for clear roleplay actions during an active conflict; do not use root `opponent`, `playerStrain`, or `opponentStrain`; non-player lead contestants need `actorArtTierSnapshot` and `artAuthoritySource`; forced incarnation requires conflict loss/surrender/concession proof and is not the same as voluntary TriggerIncarnation | 24 |
| `game_state/control/progression_schedule.json` | Client-owned afterlife and Mortal scheduler state | Nothing directly; when `progressionControl` requires work, write `progressionProcessingReport` / `game_state/control/progression_report.json` only | Report matches current `sessionId`, `requestId`, `turnNumber`, exact processed counts, catch-up proof, and per-contour `newLast*Ordinal` values from `progressionControl` | Do not write or repair `progression_schedule.json` manually; do not advance schedule ordinals by editing the schedule; do not put processed counts into the schedule; do not use Mortal `world_time.json` or `timeChange` as afterlife proof | 1, 2, 16, 17 |
| `pending_abode_offering.json` with `offeringType=ink_feathers` | Afterlife only | `guardianPowerEvents` with `reasonType=offering` and `sourceSurface=guardianAbodeOffering`; `output/ink_feather_action_result.json` with `actionTag=ABODE_OFFERING` | Ink Feather receipt `stateEvidence` includes affected files plus `guardianId`, `returnCycleId`, `powerGain`, `powerEventId`; power event id matches the Abode Power journal/event | Do not use an unsupported feather amount; do not omit `powerGain`; do not list unrelated proof files | 8, 16 |
| `pending_abode_offering.json` with `soul_relic`, `archive_lore_fragment`, or `archive_secret_record` | Afterlife only | `guardianPowerEvents` with `reasonType=offering` and `sourceSurface=guardianAbodeOffering` | Matching offering audit and Abode Power event; for `soul_relic`, preserve pending `relicRarity` exactly as the client's canonical snapshot resolved from stored relic aliases `rarity -> quality -> relicRarity` | Do not write `output/ink_feather_action_result.json`; no fake `costInFeathers`; do not recompute a Soul Relic rarity from a different alias | 8 |
| `pending_guardian_trade_request.json` | Afterlife with current Guardian trade context | Materialized `guardian.tradeInventory` plus `UpdateGuardianTradeInventoryReceipts` | Matching `requestId`, `guardianId`, `abodeId`, `tradeCycleId`, `status=ready`, `itemCount`, timing; every `tradeInventory.items[].slotId` is unique inside the inventory | Do not place commandless `tradeInventory` inside ignored `UpdateGuardians`; stock without receipt is not closed; duplicate `slotId` values make local purchase ambiguous and are invalid | 4 |
| `pending_guardian_abode_residents_request.json` | Afterlife; `requestMode=standard_roster` creates an ordinary Abode roster, while `requestMode=founder_attraction` creates the first residents attracted by a player-founded Guardian feature | Full canonical residents through `UpdateGuardianAbodeResidents` plus `UpdateGuardianAbodeResidentRosterReceipts` | Receipt with matching request and roster count; resident objects include required identity, kind, origin, presence, bond, imprint/reward fields; founder-attraction requests also require `founderFeatureTitle` and `founderFeatureSummary` in the pending request context | Do not emit identity-only resident fragments; for `founder_attraction`, do not transfer old-patron residents or derive the roster from the former Guardian's Abode | 10 |
| `pending_guardian_abode_resident_interactions.json` | Afterlife | `UpdateGuardianAbodeResidentInteractionReceipts`; accepted outcomes also update resident logs/history/state when relevant | Matching `requestId`, `residentId`, `interactionType`, `status`, `abodeId`; history requests include `historyEntryId` pointing to `UpdateGuardianAbodeResidentHistoryLog` | Do not close a history request with a receipt that has no canonical history entry | 1, 10 |
| `pending_guardian_abode_resident_transfers.json` | Afterlife | `UpdateGuardianAbodeResidentTransferReceipts`, source/target resident state, matching history/log entries | Matching `requestId`, transfer mode/status, source and target abode/guardian data | Do not move a resident silently without a transfer receipt | 11 |
| `pending_guardian_social_interactions.json` | Afterlife | `guardianSocialJournalUpdates` | Matching `requestId`, `guardianId`, `interactionType` (`talk` or `lore`), `status`, title/summary/timing | Do not use NPC social journals or `UpdateNPCs` for Guardians | 12 |
| `pending_npc_social_interactions.json` | MortalWorldProfile-only | Nothing in afterlife; preserve as wrong-realm repair-only context | None in afterlife. Runtime rejects NPC social pending contracts while `currentRealm` is `Chaos Sea` or `Shining Abode`; non-empty or malformed files block Soul Gates until repair or Mortal resolution. | Do not close it through `npcInteractionJournalUpdates`, `UpdateNPCs`, or any `game_state/npcs/*` surface during afterlife | Mortal docs |
| `pending_npc_trade_inventory_requests.json` / `[NPC_TRADE_REQUEST]` | MortalWorldProfile-only | Nothing in afterlife; preserve as wrong-realm repair-only context | None in afterlife. Runtime rejects NPC trade pending contracts while `currentRealm` is `Chaos Sea` or `Shining Abode`; non-empty or malformed files block Soul Gates until repair or Mortal resolution. | Do not materialize NPC trade stock, `UpdateNpcTradeInventoryReceipts`, or any `game_state/npcs/*` surface during afterlife | Mortal docs |
| `pending_resident_companion_manifestation_request.json` | MortalWorldProfile-only next-life companion manifestation | Nothing in afterlife; preserve valid non-empty files as next-life context | None in afterlife. Valid files can originate from afterlife resident rewards or imprint-bearing Soul Relics, closure happens later after Mortal bootstrap through source fields such as `sourceResidentId`, `sourceImprintId`, `sourceGuardianId`, `futureCompanionPrompt`, and `targetIncarnation`, and valid non-empty files do not block Soul Gates. Malformed files block Soul Gates until repair. | Do not materialize Mortal NPCs, encounters, `UpdateNPCs`, `NPCsInScene`, `worldEventsLog`, or companion quests while current realm is `Chaos Sea` or `Shining Abode`; malformed files are preserved for repair rather than "fixed" by an afterlife response | 22 |
| `pending_player_guardian_foundation.json` | Ordinary Chaos Sea only, and only when the runtime preconditions below are true | `UpdateGuardians.create`, `guardians`, `activeGuardian`, `chaosSeaNavigation`, `metaStateUpdates` for soul foundation state, `playerGuardianFoundationHistory` | New founded Guardian materialized, former patron preserved as `former_patron`, navigation points to the new abode | Do not rewrite the player's soul as the Guardian; do not change patron roles without the foundation create authority | 13 |
| `game_state/control/system_guardian_attraction.json` | Ordinary Chaos Sea only with deterministic Eternal Guardian attraction | Exact requested system Guardian materialized or selected through `UpdateGuardians`, canonical `guardians`, `activeGuardian`, and `chaosSeaNavigation`; `guardians.json.pendingGuardianCreation.mode=system_preset` is related authority context | Active Guardian points to the requested preset and carries `sourcePreset` with `presetId`, `displayName`, `version`, `library`; existing requested Guardian is routed to, not duplicated | Blocks `/incarnate` until resolved or explicitly cancelled; do not create, close, or repair this from Shining Abode; do not substitute a similar archetype; do not use `UpdateNPCs`; do not omit `sourcePreset`; do not clear or replace the attraction with a different Guardian | 20 |
| `game_state/control/archive_candidate_manifest.json` | Client-owned Life Evaluation / afterlife archive candidate manifest | No GM-authored output; read only for context/repair | Runtime validates JSON object shape, `sourceLife`, `lastExtractedAt`, and `candidates[]`; candidates may later become archive entries through client-controlled flows | Do not edit, clear, or treat as an afterlife pending contract; it is not closed by GM receipt | 22 |
| `game_state/control/next_life_scenario_core.json` | Client-owned next-life bootstrap context generated/refreshed from `incarnation_world_setup.json` | No GM-authored output; read only when preparing or resolving the next Mortal bootstrap | `scenarioCoreAssertions[]` are hard facts for the next life and must not be contradicted; `candidateAssertions[]` are candidate-only until explicitly confirmed by later accepted state | Do not edit, clear, or close this file through a receipt; do not treat candidate assertions as canonical facts; do not let narrative override scenario core assertions | 18 |
| `game_state/world/guardian_corrections.json` | Client-owned current-Mortal-life correction journal created at bootstrap from Guardian correction resolution | No GM-authored afterlife output; read only in Mortal turns when present | It explains which compatible Guardian corrections were applied around `next_life_scenario_core.json`, including `claimants[]`, `contestedSlots[]`, `resolutionOrder[]`, and `corrections[]`; matching Abode Power spends use `guardianPowerEvents.reasonType=correction_spend` with `sourceSurface=guardian_corrections` | Do not create, edit, clear, or close it from Chaos Sea/Shining Abode; do not treat it as a pending request; do not overwrite scenario core facts through corrections | 22 |
| `pending_archive_consultation_request.json` | Afterlife | `archiveActionResolutions` and canonical `soul_state.afterlifeArchive.actionReceipts[]` | Matching `requestId`, `archiveId`, requested mode/status, available archive reservation or completed receipt | Do not create mortal inventory/lore notifications as the closure | 5 |
| `pending_archive_project_fuel_request.json` | Afterlife | `archiveActionResolutions`; project/log effects only when allowed by the request | Matching archive action receipt; `lore_fragment` may resolve as project work, `secret_record` resolves as pressure relief | Do not use a `secret_record` as `project_work` fuel | 5 |

Guardian quest hooks must preserve their machine origin. Ordinary lore-research hooks use `questOrigin=lore_research_hook`; special lines unlocked by lore research use `questOrigin=lore_research_special_line`; archive consultation hooks use `questOrigin=archive_consultation_hook`; voluntary / добровольные baseline assignments for a future mortal life use `questOrigin=guardian_baseline_mortal_life_hook`. When the hook came from a concrete Guardian project or consultation tied to a project, keep `sourceProjectId=<projectId>` in `availableQuests`, `activeQuests`, and `completedQuests`; never replace this with only title text. Baseline hooks do not require `sourceProjectId`, are offers in `availableQuests`, and become `activeQuests` only after explicit player acceptance. Do not fail validation only because a Guardian currently has no available quests.

Guardian quest lifecycle across Mortal life:
- `availableQuests[]` are offers; `status` may be omitted or `available`.
- `activeQuests[]` are accepted quests; `status` may be omitted for legacy entries or be `active`, `ready_to_turn_in`, `failed`, or `expired`.
- Mortal World may use only `guardianQuestProgressUpdates[]` to update an existing pre-turn `activeQuests[]` entry. It must not use `UpdateGuardians`, Guardian reputation, Abode navigation, Gacha, projects, or Guardian identity mutation from Mortal World.
- `ready_to_turn_in` means the mortal objective is satisfied and waits for afterlife hand-in. Required proof lives in `readyToTurnInEvidence` and is non-physical: `memoryImprint`, `lifeEventEvidence`, `itemEcho`, `locationWitness`, `craftedOutcome`, `knowledgeTrace`, or `soulResonance`.
- A Mortal inventory item never crosses into afterlife. Do not write `physicalItem`, `inventoryItem`, `transferredItem`, `transferredItemId`, or `mortalInventoryTransfer` as Guardian quest proof.
- Final closure after return to the Guardian uses `UpdateGuardians.completeQuest`; it can target only accepted quests in `activeQuests[]`, never unaccepted offers in `availableQuests[]`. Explicit `status=active`, `failed`, or `expired` is not ready for successful hand-in.
| `pending_shining_abode_actions.json` | Ordinary active Shining Abode only: `currentRealm = Shining Abode`, `availability = active`, and `preparedIncarnationPackage=null`/absent; file root is `requests[]` and contains exactly one active Shining core request, not a GM-managed queue | Canonical `shining_abode_state.json` mutation plus `coreActionReceipts[]` | Receipt matches request `requestId`, `actionType`, status/timing, target ids, exact `quotedCostFeathers`/`quotedCostLightSparks`, and action-specific snapshots; accepted `prepare_incarnation_package` receipts must include non-empty `selectedCardIds[]` and matching `selectedCards[]` frozen snapshots; forge receipts also echo the request mutation payload (`replacementProperty` for retune, `addedProperties` for uplift) | Do not write a singular root `request`; do not process while `availability = sealed_until_next_ascension`, package handoff, or package fault is active; if seen from Chaos Sea, treat as wrong-realm repair-only context and preserve it instead of resolving Shining receipts; do not route through mortal factions; do not invent a second simultaneous Shining core request; do not omit receipt cost audit fields even when the cost is `0` | 14, 17, 18 |
| `pending_shining_faction_foundings.json` | Ordinary active Shining Abode | New/updated Shining faction state plus `factionFoundingReceipts[]` | Matching request, unique `requestId` inside the file, faction identity, founder/role data, status/timing, `quotedCostFeathers=25`, `quotedCostLightSparks=15`; live client-authored requests include `reservedInkFeathersBefore` and `reservedLightSparksBefore`, and current `soul_state.inkFeathers.current` / `shining_abode_state.lightSparks` must equal those reserved balances minus quoted costs | Do not process during package handoff/fault; if seen from Chaos Sea, treat as wrong-realm repair-only context and preserve it instead of resolving Shining politics; do not create Mortal World `factionDataChanges`; do not omit the cost audit fields from request or receipt; do not restore pre-reservation Feathers/Sparks in GM output; do not keep multiple unresolved player-soul founding requests because each accepted founding makes `player_soul` the new head and conflicts with the single-head invariant | 9 |
| `pending_shining_faction_realignments.json` | Ordinary active Shining Abode | Resident/faction alignment state plus `factionRealignmentReceipts[]` | Matching resident, unique `requestId` inside the file, source/target faction, realignment mode, status/timing; accepted/departed outcomes must include `residentHistoryEntryId` pointing to the matching resident history entry | Do not process during package handoff/fault; if seen from Chaos Sea, treat as wrong-realm repair-only context and preserve it instead of resolving Shining politics; do not use non-canonical `shiningAlignment`; ascended residents need canonical Shining resident fields; do not use `refused` for a `departure_to_neutral` closure, use `departed_to_neutral` or `withdrawn` | 9 |
| `pending_shining_faction_leadership_transitions.json` | Ordinary active Shining Abode; `transitionMode` is `abdication`, `peaceful_succession`, or `revolt` | Faction leadership state/history plus faction `leadershipReceipts[]` | Matching transition mode, unique `requestId` inside the file, non-vacant incumbent, resolved candidate/vacancy, status/timing; accepted/refused outcomes require `leadershipHistory[]` with the canonical event mapping from example 9C | Do not process during package handoff/fault; if seen from Chaos Sea, treat as wrong-realm repair-only context and preserve it instead of resolving Shining politics; do not put leadership in Mortal World faction chronicles; do not use `succeeded` for `revolt` or `revolted` for peaceful succession; any leadership transition requires `leadershipState != vacant`; if `leadershipState = vacant`, preserve the state for a future explicit vacancy-fill feature instead of creating/closing this pending file; resident candidate/incumbent actors must not already be locked by pending realignment, ordinary resident transfer, founding support, or another leadership flow; for `revolt`, supportingResidentIds[] must never include the incumbent resident; for an active-guardian auto faction (`faction_{slug(activeGuardianId)}` / `hall_{slug(activeGuardianId)}`), `candidateHeadActorType=guardian` may only use the same `activeGuardianId`, never another guardian | 9 |
| `pending_shining_trade_inventory_requests.json` | Ordinary active Shining Abode; supports `requests[]` but the uniqueness keys are `requestId` and `(factionId, tradeCycleId)` | Faction `tradeInventory` plus faction `tradeInventoryReceipts[]` | Inventory matches derived tier/slot count/rarity/service multiplier/trade cycle; receipt has `status=ready`, `itemCount`, `soldOutCount`, timing; every item has a unique `relicData.relicId` absent from `soulRelics.equipped` and `soulRelics.stored` | Do not process during package handoff/fault; if seen from Chaos Sea, treat as wrong-realm repair-only context and preserve it instead of resolving Shining trade; do not invent one-slot inventory when request derives more slots; do not omit item `priceInFeathers` or nested `relicData`; do not duplicate a `requestId` or the same faction/cycle request; do not reuse an already-owned Soul Relic id | 2, 17 |
| `shining_abode_state.json.pendingNativeFactionDiscovery` | Legacy ordinary active Shining Abode state-local discovery payload | Close as legacy `discover_native_faction`: materialize one hall, one `native_radiant` faction, 2..4 ascended residents, exactly 2 seeded completed projects, +20 Radiance XP, spend `costFeathers` from Soul, preserve current Light Sparks because the legacy queue already reserved `costLightSparks`, set `pendingNativeFactionDiscovery = null`, and append `coreActionReceipts[]` with the same `requestId` and `actionType=discover_native_faction` | `requestId`, `hallId`, `resolvedFactionId`, `newResidentIds[]`, `seededProjectIds[]`, `resolvedAtTurn`, `resolvedAtUtc`, and exact legacy receipt audit: `quotedCostFeathers = costFeathers`, `quotedCostLightSparks = 0` | Do not create a duplicate `pending_shining_abode_actions.json`; do not leave the legacy field non-null after accepted closure; do not use Mortal World factions; do not mutate existing Shining halls/factions/residents/political actors or unrelated Soul state while closing discovery | 14A |

### Player-founded Guardian foundation preconditions

The GM must treat `pending_player_guardian_foundation.json` as valid only when all of these are true in the pre-turn/current input context:
- Current realm is exactly `Chaos Sea`; never process the ritual in `Shining Abode` or `Mortal World`.
- The soul has a non-empty `soulName` / founder identity; the new Guardian is a separate Guardian actor, not the player soul rewritten as an NPC.
- `shining_abode_state.json.availability = sealed_until_next_ascension`; `active` means the Shining Abode is still open and the foundation ritual is not authorized.
- `shining_abode_state.json.preparedIncarnationPackage` is absent/null; a prepared package handoff blocks foundation.
- No active, malformed, wrong-reason, or otherwise blocking `afterlife_return_guard.json`; no malformed/existing `pending_player_guardian_foundation.json` from another request; and no existing `soul_state.playerFoundedGuardianId` or already founded Guardian for this soul.
- The pending request must echo `sourceShiningAvailability = sealed_until_next_ascension`, `founderSoulName`, previous Guardian identity, proposed mantle identity, `mantleSummary`, `mantleCreed`, appearance motifs, turn and UTC timing.

## Preview Ownership Rules

- A contract-backed preview is a player-facing audit for a GM-resolved afterlife action. If the preview names a `playerAction` tag, pending/control file, request id, cost, or closure surface, the GM must close that exact contract through the matrix row for the tag/file.
- A client-local mutation preview is not an afterlife-turn contract. Soul Relic equip/unequip is the canonical local example: no GM turn is sent, no pending/control file is created, no receipt/progression report/gm_thoughts output is required, and no existing pending contract is closed.
- Do not convert a client-local preview into GM-authored state. Only the client applies the listed local state mutation after the player confirms the preview.
- High-cost or destructive Chaos Sea previews must expose the exact target/source identity before confirmation: Guardian id/name for donation/favor/offering/trade, resident id/name for resident notifications, Soul Relic/archive id/name/rarity/type for consumed objects, and companion/NPC id/name plus traits/personality markers for `SOUL_IMPRINT`.
- `SOUL_IMPRINT` receipt proof must include `stateEvidence.imprintId`, `stateEvidence.companionName`, source provenance (`sourceCompanionId`/`companionId` or `NPCId`/`sourceNpcId`), and `stateEvidence.affectedFiles` pointing at `game_state/meta/soul_state.json`; the persisted `soulImprint` must keep the same source provenance. It prepares a future companion/imprint path and does not create a Mortal NPC during the afterlife turn.
- The preview must show projected state/resource deltas when the client can know them: Ink Feathers before/after, Abode Power current/projected plus `baseDelta`/`finalDelta`, Guardian reputation expected change, trade/buyback price and projected balance, and the pending/control file or local state surface that will change.
- Post-resolution player-visible detail surfaces must expose the matching receipt/audit identifiers when available: `requestId`, `historyEntryId`, `guardianPowerEvents.powerEventId`, trade `slotId`, buyback `buybackEntryId`, archive action receipt id, resident roster/interaction/transfer receipt identity, and the derived current state snapshot.
- Guardian trade purchase/sale/buyback previews are client-local coordinated writes, not GM turns, but they still need full audit JSON so the player can see `priceInFeathers`, `currentFeathers`, `projectedFeathers`, sold-out or buyback status, and cloned `relicData`.

## PlayerAction Routing Tag Matrix

These tags may appear inside `input/turn_request.json.playerAction`. Treat them as routing markers for the contract named here; do not resolve them as generic prose.

| `playerAction` tag | Contract type | GM routing rule | Required/forbidden outputs | Example |
|---|---|---|---|---|
| `[ABODE_RESIDENT_RELIC_GRANT]` | Direct resident action, no pending file | The player accepts a companion-echo reward from an existing afterlife resident. Resolve directly through resident and Soul Relic state. | Required: `metaStateUpdates.soulRelicOperations.addRelic` must add a new current-turn `relicType=companion_echo` with complete `companionSeed`, `UpdateGuardianAbodeResidents` must move the existing pre-turn resident to `bondRewardState=granted` and `grantedRelicId=<new relicId>`, plus a new current-turn `residentInteractionLogUpdates` entry. Forbidden: `UpdateGuardianAbodeResidentInteractionReceipts`, roster receipts, transfer receipts, `pending_resident_companion_manifestation_request.json`, `UpdateNPCs`. | 22 |
| `[ABODE_RESIDENT_QUEST_REQUEST]` | Direct resident action, no pending file | The player helps or accepts a request from an afterlife resident. Resolve as an ordinary Soul Quest linked back to the resident. | Required: `UpdateSoulQuests` must create or progress a current-turn Soul Quest with `relatedAfterlifeResidentId`; if resident state changes, `UpdateGuardianAbodeResidents` with `linkedSoulQuestId` or bond fields; add a new current-turn `residentInteractionLogUpdates` entry. Forbidden: invented resident request receipts unless a real pending resident file exists. | 22 |
| `[GUARDIAN_TRADE_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_trade_request.json`; close the Guardian trade row in the Pending Contract Matrix. | Materialize `guardians[].tradeInventory` and `UpdateGuardianTradeInventoryReceipts`. Do not put commandless trade inventory into `UpdateGuardians`. | 4, 22 |
| `[ARCHIVE_CONSULTATION_REQUEST]` | Pending-backed routing marker | Read `pending_archive_consultation_request.json`; close the archive consultation row. | Use `archiveActionResolutions` and canonical `soul_state.afterlifeArchive.actionReceipts[]`; do not create mortal lore/inventory output. | 5, 22 |
| `[ARCHIVE_PROJECT_FUEL_REQUEST]` | Pending-backed routing marker | Read `pending_archive_project_fuel_request.json`; close the archive project-fuel row. | Use `archiveActionResolutions`; `lore_fragment` may become `project_work`, while `secret_record` is only `pressure_relief`. | 5, 22 |
| `[ABODE_RESIDENT_ROSTER_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_abode_residents_request.json`; close the resident roster row. | Use full `UpdateGuardianAbodeResidents` plus `UpdateGuardianAbodeResidentRosterReceipts`. Do not emit identity-only resident fragments. | 10, 22 |
| `[ABODE_RESIDENT_HISTORY_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_abode_resident_interactions.json`; close the matching resident `interactionType=history` request. | Use `UpdateGuardianAbodeResidentInteractionReceipts` with `abodeId` and `historyEntryId`; also write the matching `UpdateGuardianAbodeResidentHistoryLog` entry. | 10, 22 |
| `[ABODE_RESIDENT_TALK]` | Pending-backed routing marker | Read `pending_guardian_abode_resident_interactions.json`; close the matching resident `interactionType=talk` request. | Use `UpdateGuardianAbodeResidentInteractionReceipts` with canonical resident `responseMode`; write resident logs/state only when the scene changes them. | 10, 22 |
| `[ABODE_RESIDENT_TRANSFER_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_abode_resident_transfers.json`; close the exact transfer request. | Preserve `selectionMode`, `competitionScore`, `competitionLabel`, and `competitionReason` from the pending contract in reasoning/receipt context; write transfer receipt plus source/target resident state/history. | 11, 22 |
| `[GUARDIAN_SOCIAL_TALK_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_social_interactions.json`; close the matching Guardian `interactionType=talk` request. | Use `guardianSocialJournalUpdates` with matching `requestId`, `guardianId`, `interactionType`, `status`, optional Guardian `responseMode`, title/summary/turn/timestamp. | 12, 22 |
| `[GUARDIAN_SOCIAL_LORE_REQUEST]` | Pending-backed routing marker | Read `pending_guardian_social_interactions.json`; close the matching Guardian `interactionType=lore` request. | Use `guardianSocialJournalUpdates`; `responseMode` is normally `lore_revealed`, `lore_refused`, `warning`, or `refusal` depending on what the Guardian actually does. | 12, 22 |
| `[PLAYER_GUARDIAN_FOUNDATION]` | Pending-backed routing marker | Read `pending_player_guardian_foundation.json`; close the player-founded Guardian row. | Use foundation authority surfaces: `UpdateGuardians.create`, canonical `guardians`, `activeGuardian`, `chaosSeaNavigation`, soul foundation fields, and `playerGuardianFoundationHistory`. | 13, 22 |
| `[GUARDIAN_PROVOCATION]` or `[GUARDIAN_PROVOCATION: guardianId]` | Legacy deterministic evidence tag for Guardian-forced incarnation, not a generic pending file | Only relevant if the GM writes `TriggerIncarnation.source=guardian_forced`; no-id tag is accepted as explicit provocation, id tag is accepted only when it equals the trigger `guardianId` and the current active Guardian's pre-turn `CurrentReputation <= -21` | Do not use this tag in Shining handoff or protected post-life turns; do not point it at one Guardian and force incarnation from another; conflict-resolved forced incarnation may instead use current-turn `recentConflicts[]` proof below | 6A, 15 |
| `[AFTERLIFE_SPIRITUAL_ACTION: conflictId]` | Explicit active afterlife spiritual conflict action, no pending file; ordinary prose can use the same row when it clearly acts inside the already-active conflict | Read `game_state/meta/afterlife_spiritual_conflict_state.json.activeConflict`; resolve the player's declared operation as an exchange or final resolution | Write only `afterlifeSpiritualConflictUpdate`; use side-vs-side fields, exchange proof, and `diceAudit` for any contested outcome. `blocked` may keep before/after identical only when `incomingAction` proves what was prevented | Do not create Mortal combat state, do not reward currency directly, do not start a new conflict if tagged `conflictId` does not match the active conflict; do not ignore clear prose just because the tag is absent; do not decide a contested result without dice | 24 |

## Afterlife Spiritual Conflict Contract

Use this system when roleplay creates a real spiritual contest in `Chaos Sea` or ordinary active `Shining Abode`: coercive Guardian pressure, duels between Guardians/residents/radiant actors, contested bindings, forced-incarnation attempts, or side-vs-side champion contests. The GM starts the conflict when the scene requires it; the client does not pre-create the conflict. Conflict realm/mode authority is evaluated from the validated pre-turn `soul_state.json` and, for Shining, the validated pre-turn `shining_abode_state.json`; same-turn edits to `currentRealm`, Shining `availability`, or `preparedIncarnationPackage` cannot make a conflict legal. For a worked valid start/exchange pattern, see example 24 in `Examples/E_CLI_Afterlife_Turns.txt`.

Canonical state lives in `game_state/meta/afterlife_spiritual_conflict_state.json`:

- `schemaVersion`: positive integer.
- `activeConflict`: object or null.
- `recentConflicts[]`: compact resolved conflict summaries.
- `activeConflict.sideModel`: one of `direct_duel`, `assisted_duel`, `champion_duel`.
- `activeConflict.playerSide` and `activeConflict.oppositionSide`: each has exactly one `leadContestant`; `supporters[]` are minor contributors unless the model chooses a champion lead.
- Side strain fields are `playerSideStrain` and `oppositionSideStrain`, each one of `clear`, `strained`, `fractured`, `overwhelmed`, `broken`.
- `conflictPosition`: `opposition_dominant`, `opposition_advantaged`, `contested`, `player_advantaged`, or `player_dominant`.
- `activeConflict.resolutionState`: only non-terminal live states: `active`, `concession_pending`, `surrender_pending`, `retreat_pending`, or `ready_to_resolve`.
- Terminal states `resolved` and `repair_cancelled` are valid only after closing the active slot through `mode=resolve` or `mode=repair_cancel`; do not leave terminal state under non-null `activeConflict`.
- `exchangeLog[]` records each meaningful operation with `operationType`, `outcome`, before/after audit, and `incomingAction` when an operation is blocked/countered.

Dice audit rule:
- Every contested `mode=exchange` must include `exchange.diceAudit`; every contested `mode=resolve` that determines loss, victory, surrender under pressure, concession under pressure, or Guardian-forced incarnation proof must include `resolution.diceAudit`.
- `diceAudit.diceSource` is `input/turn_request.json.preGeneratedDices1d20`; never use `gachaBaseResult`, hidden random rolls, or GM preference for afterlife conflict results.
- Use two visible d20 entries for a contested side-vs-side check: one player-side die and one opposition-side die. If another documented surface in the same turn already consumed visible dice, use the next unused indices and list them explicitly.
- Required fields: `formulaVersion="afterlife_spiritual_conflict_v1"`, `diceSource`, `diceUsed[]` with `side`, `sourceIndex`, `sides=20`, and `value`, `playerTotal`, `oppositionTotal`, `margin`, `outcomeBand`, and `modifierBreakdown`.
- `margin = playerTotal - oppositionTotal`. The GM may narrate freely, but `outcome`, strain changes, `conflictPosition`, and terminal proof must match the audit.
- Outcome bands for v1: `margin >= 8` -> `decisive_player_success`; `margin 3..7` -> `player_success`; `margin -2..2` -> `mixed_or_no_effect`; `margin -7..-3` -> `opposition_success`; `margin <= -8` -> `decisive_opposition_success`.
- `no_effect` is allowed only when the action truly produces no state delta; if the player's or opponent's action is contested and changes position, strain, or terminal proof, write `diceAudit`.

Supported `afterlifeSpiritualConflictUpdate.mode` values:

- `start`: create `activeConflict` from `conflictSeed`, `conflictState`, or `activeConflict`.
- `exchange`: append an exchange and update conflict position/strain/resolution state.
- `resolve`: close active conflict, append `recentConflicts[]`, set `activeConflict=null`.
- `repair_cancel`: clear a malformed/impossible conflict without gameplay rewards or punishments.

Non-player lead contestants must carry `actorArtTierSnapshot` and `artAuthoritySource`; do not copy the player's `purchasedArtTierSnapshot` onto Guardians, residents, or radiant actors. Player profile lives in `soul_state.afterlifeCombatProfile` and may carry `enlightenmentRank`, `radianceRank`, `retainedRadianceRank`, and `artTiers`.

Forced incarnation rule: voluntary `TriggerIncarnation` remains ordinary Soul Gates/handoff flow. A Guardian forcing the soul into a life is coercive and must be backed either by legacy explicit provocation evidence or by current-turn resolved afterlife spiritual conflict proof. For the conflict path, `recentConflicts[]` must include `mode=resolve`, `resolutionState=resolved`, `resolvedAtTurn=<current turn>`, matching `guardianId`, `operationType=force_incarnation`, and `playerOutcome=lost|surrendered|conceded` or equivalent `resolutionKind=player_loss|player_surrender|player_concession`. The active Guardian, current Abode, reputation threshold, and severity band are evaluated from the validated pre-turn `game_state/meta/guardians.json` snapshot; same-turn edits to `guardians.json` cannot make forced incarnation legal. Do not treat force and consent as the same lifecycle path.

## Social Response Modes And Transfer Metadata

- Guardian social `responseMode` values are `talk_scene`, `lore_revealed`, `lore_refused`, `warning`, `refusal`, `trust_shift`, and `attitude_shift`. Use them only on `guardianSocialJournalUpdates` / Guardian social closure entries.
- Resident interaction `responseMode` values are `talk_scene`, `history_revealed`, `history_refused`, `history_partial`, and `bond_shift_only`. Use them on `UpdateGuardianAbodeResidentInteractionReceipts` and matching resident journal/history outputs.
- Resident transfer `selectionMode` values are `competition_recommended`, `manual_override`, and `departure_only`. When the pending request carries a competition decision, preserve `competitionScore` in the range `0..100`, `competitionLabel` exactly as one of `strong_pull`, `plausible_pull`, `weak_pull`, and non-empty `competitionReason`; `departure_only` must not carry target Guardian/Abode competition metadata.

## Shining Core Action Matrix

All rows below use `pending_shining_abode_actions.json` as input and close through `shining_abode_state.json.coreActionReceipts[]`. The file root is always `requests[]`; current runtime accepts exactly one active request inside that array. They are legal only in ordinary active `Shining Abode`.

For every Shining core action, materialize only the canonical projected diff for that exact request plus its matching `coreActionReceipts[]` entry. Accepted outcomes are validated against the projected Shining/Soul/resident state; `refused` or `withdrawn` outcomes must leave canonical Shining, Soul, and resident state unchanged except for the closure receipt. Supported receipt statuses are only `accepted`, `refused`, and `withdrawn`.

The player-facing Shining core preview may include `projectedStateFragment` with sanitized `beforeFullShiningRoot`, `afterFullShiningRoot`, and focused `gates`/faction/project before-after fragments. Forge previews may also include sanitized `beforeFullSoulRoot`/`afterFullSoulRoot`; Shining gacha previews may include `gachaAccounting`, `expectedGachaHistoryEntryShape`, and `oneNewRelicEvidence`. These are audit/preview aids, not output files and not separate GM-authored schemas. Use them to understand the canonical diff, then persist the full canonical `game_state/meta/shining_abode_state.json`, `game_state/meta/soul_state.json` when listed in `changedSurfaces`, and the matching `coreActionReceipts[]`. Do not copy hidden blessing `effectPayload` keys; previews expose only sanitized state and safe effect details.

Canonical Shining owner-state ids must stay globally unambiguous: every `halls[].hallId`, `factions[].factionId`, `shiningPoliticalActors[].actorId`, and every `projects[].projectId` across all factions is unique. Completed projects with `isSupported=true` must not exceed the Radiance support cap (`tier 0..1 => 1`, `tier 2..3 => 2`, `tier 4 => 3`). `gachaSystem.currentReturnCycleId` may be empty only before a cycle is synced; while it is empty, `chargesUsedThisReturn` must be `0`.

If one accepted active Shining turn closes a core action, scheduler debt, and another Shining pending contract, apply the contracts compositionally. Example: an accepted `pending_shining_faction_foundings.json` may create a new hall/faction and move supporter residents, while an accepted `open_gates` core action rebuilds gates from that resulting Shining state. If `progressionControl` also requires Shining/resident cycles, those living-world deltas are allowed only when `progression_report.json` is correlated and closes the exact requested bounded counts. Do not discard either side as "unrelated"; the final files must contain every independent receipt, the scheduler proof, and the combined canonical state.

Composite Shining closure validation is snapshot-backed. The client compares current `game_state/meta/shining_abode_state.json`, `game_state/meta/guardian_abode_residents.json`, and `game_state/meta/soul_state.json` against the pre-turn `game_state/control/pending_turn_snapshot` roots plus every accepted closure projection. Missing snapshots surface as `shining_closure_missing_pre_turn_shining_state`, `shining_closure_missing_pre_turn_resident_state`, or `shining_closure_missing_pre_turn_soul_state`; unrelated final diffs surface as `shining_closure_unexpected_shining_state_diff`, `shining_closure_unexpected_resident_state_diff`, or `shining_closure_unexpected_soul_state_diff`. If Soul state changes in the same turn, that change must come from a separate accepted afterlife contract such as `ABODE_OFFERING`; do not hide unrelated Soul edits inside a Shining closure.

Legacy `shining_abode_state.json.pendingNativeFactionDiscovery` is not the normal queue, but if it is non-null in a pre-turn save it is an active state-local contract or malformed fail-closed evidence and must be closed or repaired before starting a new `discover_native_faction` request or sealing the Abode through client-owned `return_to_chaos_sea`. Use the row above and example 14A for object payloads; do not duplicate it into `pending_shining_abode_actions.json`.

If a Shining core action mutates the faction/project inputs used by the blessing-card gates draft (`invest_in_faction`, `complete_project`, `support_project`, `unsupport_project`, or `retire_project`) and the pre-turn state has `gates.hasOpenDraft = true`, preserve the canonical `gates` object and set `gates.isStale = true`. A stale draft cannot be used for `prepare_incarnation_package`; the player must regenerate it through `open_gates`.

| `actionType` | GM state responsibility | Receipt must identify | Required caution |
|---|---|---|---|
| `discover_native_faction` | Materialize a new hall, new native Shining faction, 2..4 new ascended residents, 2 new seeded completed projects, Radiance XP, exact costs | `hallId`, `resolvedFactionId`, `newResidentIds[]`, `seededProjectIds[]`, exact quoted cost audit fields | The discovered ids must not reuse any pre-turn Shining hall/faction/project ids; the discovered faction must be Shining state, not Mortal World faction state; the accepted diff is constrained to the discovery surfaces only |
| `invest_in_faction` | Spend exact costs, increment faction investment, recompute strength, mark gates stale when open | `factionId` | Costs/effects must match the client-authored pending request |
| `complete_project` | Spend exact costs, append one completed project from request draft, update Radiance/faction strength, mark gates stale when open | `factionId`, completed `projectId` | Project result must come from request `projectDraft`; favored archetype only discounts completion cost by 5 Feathers/5 Light Sparks and never changes `strengthReward` (tier only: 8/12/16); do not invent unrelated projects |
| `support_project` | Set an existing completed project supported and mark gates stale when open | `factionId`, `projectId` | Zero-cost action: request and receipt must both carry `quotedCostFeathers = 0` and `quotedCostLightSparks = 0`; do not spend resources |
| `unsupport_project` | Set an existing supported project unsupported and mark gates stale when open | `factionId`, `projectId` | Same zero-cost rule as `support_project`: both quoted cost fields are exactly `0` |
| `retire_project` | Mark eligible completed project retired, clear support, recompute strength, mark gates stale when open | `factionId`, `projectId` | Do not delete unrelated project history |
| `open_gates` | Rebuild canonical blessing-card draft from current Shining state or copy the client-provided `projectedStateFragment.afterFullShiningRoot.gates` exactly | `generatedDraftVersion` | Deterministic projection: `draftVersion = previous + 1`, `hasOpenDraft=true`, `isStale=false`, `selectedBlessingCardIds=[]`, `nextCandidateCursor=availableBlessingCards.Count`, `shownBlessingCardIds` equals the ids of the available draft, `rerollsRemaining` equals supported remembrance projects, and `allCandidateBlessingCards`/`availableBlessingCards` are built from canonical candidate sources; `gates` is not a custom registry |
| `prepare_incarnation_package` | Persist frozen package and clean gates as canonical helper projects | `selectedCardIds[]`, `selectedCards[]`, `generatedDraftVersion` | Pending request must carry non-empty unique ordered `selectedCardIds[]` and `selectedCards[]` snapshots matching the current canonical `gates.availableBlessingCards[]` entries in the same order; receipt must carry the exact frozen package snapshot; do not trigger incarnation in the same turn; package uses `generatedFromDraftVersion`, `preparedAtTurn`, `preparedAtUtc` |
| `pull_relic_gacha` | Spend exact feathers, add one Soul Relic, update Shining gacha charges/history | `factionId`, `returnCycleId`, `relicId`, `relicName`, `baseRarity`, `finalRarity` | `baseRarity` must be a non-empty value from current `turn_request.json.gachaBaseResult.baseRarity`; `finalRarity` must be >= `baseRarity` and must not exceed the request's `projectedGachaBonusSteps` ladder allowance; this is not direct Chaos Sea `/gacha` and not Guardian-mediated `UpdateGuardians.processGacha` |
| `forge_relic.reshape` | Spend exact forge costs and change the canonical Soul Relic form tag | `factionId`, `relicId`, `targetFormTag` | Keep target relic identity aligned with request |
| `forge_relic.retune_property` | Spend exact forge costs and replace one canonical relic property | `factionId`, `relicId`, `propertyIndex`, exact `replacementProperty` from the request | Replacement property must be request-compatible and echoed in the receipt exactly as requested |
| `forge_relic.strengthen_band` | Spend exact forge costs and upgrade one property band by the allowed step | `factionId`, `relicId`, `propertyIndex` | Do not exceed request-authorized outcome |
| `forge_relic.stabilize_echo` | Spend exact forge costs and improve companion echo/manifestation quality on the target relic | `factionId`, `relicId` | Do not invent unrelated relic effects |
| `forge_relic.uplift_rarity` | Spend exact forge costs, uplift rarity, append required added properties if needed | `factionId`, `relicId`, exact `addedProperties` from the request | Resulting rarity and added properties must match Shining forge contract |

### Deterministic `open_gates` Projection

If the request is accepted, the safest GM path is to copy the sanitized client preview `projectedStateFragment.afterFullShiningRoot.gates` into the persisted full `shining_abode_state.json` and then append the matching receipt. If the preview is unavailable, rebuild the draft mechanically:

- Radiance thresholds: `experience 0..99 => tier 0`, `100..219 => tier 1`, `220..379 => tier 2`, `380..579 => tier 3`, `580+ => tier 4`.
- Draft size by Radiance tier: `0=>4`, `1=>6`, `2=>7`, `3=>8`, `4=>10`; pick cap: `0=>1`, `1=>2`, `2=>2`, `3=>3`, `4=>4`.
- Radiance rarity ceiling: `tier 0 common`, `tier 1 uncommon`, `tier 2 rare`, `tier 3 rare`, `tier 4 radiant`; faction strength ceiling: `0..24 common`, `25..49 uncommon`, `50..74 rare`, `75..100 radiant`; a head card uses the lower of those ceilings.
- Candidate sources are Shining faction heads, supported completed Shining projects, and resident descent sources from canonical Shining resident bindings. Do not use Mortal factions, Mortal NPCs, retired/unsupported projects, or non-ascended residents.
- Candidate generation is deterministic from the current canonical Shining root plus resident root. Runtime de-duplicates by `dedupeKey`, keeps deterministic order, writes all candidates to `allCandidateBlessingCards`, takes the first `draftSize` into `availableBlessingCards`, and writes their ids to `shownBlessingCardIds`.
- Rerolls are local later mutations, not the accepted `open_gates` receipt. Accepted `open_gates` starts with `selectedBlessingCardIds=[]`; stale behavior is explicit: later faction/project mutations set `gates.isStale=true`, and a stale draft cannot be used for `prepare_incarnation_package`.

### Exact Shining Forge Mutation Table

Every forge action requires ordinary active Shining Abode, a valid `factionId`, a real Soul Relic `relicId`, enough resources, and at least one supported completed `refinement` project in that faction. Base costs are before any client-authored entitlement discount or service quote already embedded in the pending request: `reshape=10 Feathers/10 Light Sparks`, `retune_property=20/15`, `strengthen_band=30/20`, `stabilize_echo=25/15`, `uplift_rarity=45/30`. Required Radiance tiers are `reshape 0`, `retune 1`, `strengthen 2`, `stabilize 3`, `uplift 4`.

Forge ladders are canonical. Soul Relic rarity upgrades one step only: `common -> uncommon -> rare -> epic -> legendary`. Property band strengthens one step on the same ladder, or numeric bands increment by `+1`. Minimum property counts after rarity uplift are `common=1`, `uncommon=2`, `rare=3`, `epic=4`, `legendary=5`; if uplift would be below the minimum, the request must provide exact `addedProperties[]` and the receipt must echo them. Service multiplier by faction strength is `0..24 => 0.75`, `25..49 => 1.0`, `50..74 => 1.25`, `75..100 => 1.5`; `stabilize_echo` adds `floor(15 * serviceMultiplier)` to `companionManifestationQualityBonus`. Consumed relic refinement entitlements are client/runtime state: supported statuses are `pending_relic_entitlement` before use and `consumed` after use with `consumedAtTurn`/`consumedAtUtc`; `expired` is not a valid `relicRefinementEntitlements` status. Deadline-based `expired` statuses belong only to route/lore/descent style deferred effect arrays.

## Lifecycle And Direct Action Matrix

| Action | Valid mode | GM must write | Required caution | Example |
|---|---|---|---|---|
| Ordinary `TriggerIncarnation` | Ordinary Chaos Sea with explicit player choice/prerequisites | `TriggerIncarnation` to `game_state/control/incarnation_trigger.json` | Requires validated accepted-turn authority from the current turn request, accepted `ready/turn_complete.json`, and `game_state/control/pending_turn_snapshot`; input-only snapshots are not authority. Do not write the trigger out-of-band or as manual repair. Do not switch `soul_state.currentRealm` to Mortal World in the same turn; client performs bootstrap | 6 |
| Guardian-forced `TriggerIncarnation` | Ordinary player-driven Chaos Sea turn with either explicit provocation against current active Guardian or current-turn resolved afterlife spiritual conflict proof. Legacy evidence is `[GUARDIAN_PROVOCATION]` or `[GUARDIAN_PROVOCATION: guardianId]`; conflict evidence is `recentConflicts[]` with matching `guardianId`, `resolvedAtTurn`, `operationType=force_incarnation`, and player loss/surrender/concession. Current active Guardian pre-turn `CurrentReputation <= -21` is still required in both paths | `TriggerIncarnation` with `source=guardian_forced`, guardian id, severity, reason, provocation summary, and normal world/character/circumstances | Requires validated accepted-turn authority from the current turn request, accepted `ready/turn_complete.json`, and `game_state/control/pending_turn_snapshot`; input-only snapshots are not authority. Harsh but survivable; severity mapping is `-21..-50 => harsh`, `-51..-100 => severe`; active Guardian, current Abode, reputation, and severity are read from the validated pre-turn guardian snapshot, not from same-turn `guardians.json` edits; never from Shining pending-bootstrap handoff; never when `afterlife_return_guard.json` still protects the first post-life turn | 6A, 15, 24 |
| Active `afterlife_return_guard.json` | Ordinary afterlife turn immediately after Life Evaluation returned the soul | Do not write or mutate this client-owned file; process ordinary afterlife narration/scheduler only | This guard protects at least one ordinary afterlife turn; while active, never write `TriggerIncarnation` with `source=guardian_forced`; malformed or wrong-reason guard state is fail-closed until validation repair or explicit client/runtime clear | 21 |
| `AscensionTrigger` / `game_state/control/ascension.json` | Chaos Sea only, max Enlightenment/Transcendence, explicit `playerChoice=Ascension`, and validated pre-turn Chaos Sea snapshot | `AscensionTrigger=true` and `playerChoice="Ascension"` only | `ascension.json` contains only the ascension trigger contract; the client performs the realm handoff after validation | Do not combine with `TriggerLifeEnd` / `life_transitions.json`, `TriggerIncarnation`, manual `soul_state.currentRealm` switch, Shining faction/trade state, Mortal bootstrap, or Mortal World files | 6B |
| Shining pending-bootstrap `TriggerIncarnation` | `Shining Abode` with non-null valid `preparedIncarnationPackage` and no unresolved afterlife pending/control contracts | `TriggerIncarnation` only | Requires validated accepted-turn authority from the current turn request, accepted `ready/turn_complete.json`, and `game_state/control/pending_turn_snapshot`; input-only snapshots are not authority. Preserve `preparedIncarnationPackage` exactly; runtime consumes/clears it after successful Mortal bootstrap. If any Guardian, resident, archive, offering, Shining core/trade/politics, malformed wrong-realm NPC, or legacy `pendingNativeFactionDiscovery` contract is still present, Soul Gates must block handoff until it is resolved/repaired; the GM must not combine that closure with the bootstrap turn. | 3, 18 |
| Client-owned `reenter_shining_abode` | Chaos Sea local route into an already active stored Shining Abode; malformed, wrong-reason, active, or otherwise blocking `afterlife_return_guard.json` blocks the route fail-closed | No GM-authored output; client re-enters locally without resetting ascension-local counters or refilling Light Sparks; runtime may sync return-cycle gacha charges and create a Shining trade auto-refresh request if inventory is stale | Do not synthesize Shining state, do not consume `preparedIncarnationPackage`, and do not treat this as a new Shining bootstrap | covered by core docs |
| Client-owned `return_to_chaos_sea` / legacy alias `new_game_plus` | Ordinary active Shining Abode with no active or malformed Shining pending contracts and `pendingNativeFactionDiscovery = null` | No GM-authored output; client seals the Abode, returns locally to Chaos Sea, resets Enlightenment/Просветление to baseline, and preserves Ink Feathers, Soul Relics, Guardians, Shining achievements, halls, factions, and Radiance progress | Non-empty `requests[]` or malformed Shining pending files block return and must be resolved/repaired; valid explicit-empty `{ "requests": [] }` pending files are stale local artifacts that runtime may delete; do not use while `shining_abode_state.json.pendingNativeFactionDiscovery` is any non-null value, including malformed/non-object evidence; GM must not describe this as a destructive global reset or Ink Feather wipe | covered by core docs |
| Client-owned Shining Gates select/deselect/reroll | Ordinary active Shining Abode with an open, non-stale gates draft and no live Shining core pending request | No GM-authored output; the client mutates only local `gates` draft fields such as `selectedBlessingCardIds`, `shownBlessingCardIds`, `availableBlessingCards`, `rerollsRemaining`, and `nextCandidateCursor` | No `pending_shining_abode_actions.json`, no `coreActionReceipts[]`, no `TriggerIncarnation`, and no GM turn are created by select/deselect/reroll. These local operations are blocked by a stale draft, a prepared package, or an unresolved Shining core pending request. | 18 |
| Client-owned Shining Treasury `/shining_treasury` / `/казначейство` | Ordinary active Shining Abode only | No GM-authored output; the client mutates only `shining_abode_state.json.treasury`, `shining_abode_state.json.lightSparks`, and `soul_state.json.inkFeathers` for local deposits/withdrawals/interest/exchange | Only Ink Feathers can be deposited. Light Sparks cannot be deposited, cannot earn interest, and cannot be exchanged back into Feathers. Fixed conservative exchange is `25` Ink Feathers -> `1` Light Spark, max `3` Light Sparks per Shining return cycle. Interest is small, tiered by deposited Ink Feathers, capped at `5` Ink Feathers per cycle, and claimable locally. No `coreActionReceipts[]`, pending file, GM report, or daemon response field exists for treasury operations. If validated pre-turn `shining_abode_state.json` already contains `treasury`, every GM accepted turn must preserve that object unchanged; omitting or resetting it is a client-owned mutation. | client-owned |
| Direct Chaos Sea `/gacha` / `[CHAOS_SEA_DIRECT_GACHA]` | Ordinary Chaos Sea | Soul Relic result through Soul/meta surfaces | Read full `turn_request.json.gachaBaseResult` (`diceUsed`, `baseScore`, `baseRarity`, `formula`) for audit; do not emit `UpdateGuardians.processGacha`; do not spend feathers a second time if the client already deducted cost; preserve exact `<N> Чернильных Перьев` / `<N> Ink Feathers` phrase from playerAction; final rarity must equal `gachaBaseResult.baseRarity` exactly with no upgrade/downgrade path; accepted state may only contain exact feather spend plus one new Soul Relic append, with all pre-existing Soul Relic JSON and unrelated soul fields byte-equivalent to the pre-turn snapshot | 7 |
| `[ABODE_RESIDENT_RELIC_GRANT]` | Ordinary afterlife with explicit existing resident reward action | New current-turn companion-echo relic through `metaStateUpdates.soulRelicOperations.addRelic`, current-turn resident reward transition through `UpdateGuardianAbodeResidents`, new `residentInteractionLogUpdates` entry | No pending file is closed by this tag; do not satisfy it with an already granted relic/resident state; do not invent resident interaction/roster/transfer receipts | 22 |
| `[ABODE_RESIDENT_QUEST_REQUEST]` | Ordinary afterlife with explicit existing resident quest action | New or changed current-turn `UpdateSoulQuests`, optional `UpdateGuardianAbodeResidents`, new `residentInteractionLogUpdates` entry | The quest must carry `relatedAfterlifeResidentId`; do not satisfy it with an unchanged pre-existing quest/log; do not route through Mortal `UpdateQuests` or NPC state | 22 |
| `[CHAOS_SEA_TRAVEL]` Abode travel | Ordinary Chaos Sea, target abode selected from Guardian Abodes already present in pre-turn `chaosSeaNavigation.discoveredAbodes[]` | `game_state/meta/guardians.json`: `activeGuardian` set exactly to `targetGuardianId`, `chaosSeaNavigation.currentAbodeId` set exactly to `targetAbodeId`, `chaosSeaNavigation.discoveredAbodes[]` still contains `targetAbodeId`, target guardian `abode.isDiscovered=true` | The GM must not redirect to a different discovered abode/guardian or make travel legal by adding the target to `discoveredAbodes` during the same turn; keep target `guardianId`/`abodeId` aligned; do not use Mortal World `currentLocationData`, `UpdateNPCs`, weather/time, or `worldEventsLog` for afterlife travel | 7A |
| Freeform Chaos Sea Abode search from `/обители` | Ordinary Chaos Sea only; the player uses free text after the "Поиск новой обители" preview and there is no pending/control file | Failure: narrative only and no hidden state. Distant signal: narrative clue only unless a concrete Guardian/Abode is actually materialized. Found Abode: `UpdateGuardians.create` for a new Guardian or supported Guardian update for an existing one, plus canonical `guardians`, `activeGuardian`, and `chaosSeaNavigation` roots when the player actually arrives | Every stateful search outcome must carry explicit `guardianId`, `guardianName`, `abodeId` or location label, and `reason/source = chaos_sea_abode_search` in the authored audit/reasoning. Do not create hidden Guardians, do not teleport on a distant signal, do not write Mortal World `currentLocationData`, `UpdateNPCs`, `worldEventsLog`, weather, time, or world travel state | 23 |
| Guardian-mediated gacha | Ordinary afterlife with eligible Guardian and remaining charges | `UpdateGuardians.processGacha` and resulting Soul Relic state | Respect per-return charge limits from Guardian reputation tier; rarity upgrades are only Abode Power ceiling / completed `relic_forging`; direct `/gacha` does not consume Guardian charges | 7 |
| Freeform Guardian command | Afterlife only | Supported `UpdateGuardians` command, Guardian journals/projects/power surfaces as needed | Guardians are not NPCs; actor scope must cover changed Guardians | 15 |
| Afterlife Ink Feather action | Afterlife only and action-specific prerequisites satisfied | `output/ink_feather_action_result.json` plus the canonical state surface for the action | Afterlife whitelist is only Donate to Guardian, Cultivate Enlightenment, Guardian Favor, Memory Gates, Soul Imprint, and Ink Feather Abode Offering. Donate to Guardian must apply exactly `min(25, max(15, costInFeathers / 3))` to the target Guardian's reputation, not just any positive increase | see Ink Feather examples |

### TriggerIncarnation accepted-turn authority

`game_state/control/incarnation_trigger.json` is a lifecycle control file, not a free-standing repair or setup file. The GM may author it only inside a valid accepted afterlife turn whose `input/turn_request.json`, `ready/turn_complete.json`, validated `game_state/control/pending_turn_snapshot`, and client-owned `game_state/control/pending_turn_snapshot.authority.json` context still identify the same session, request, and turn. If that validated accepted-turn authority is missing, stale, unreadable, or mismatched, runtime raises/repairs through `incarnation_trigger_invalid_validated_snapshot_context` and ignores/deletes the trigger instead of starting Mortal bootstrap. Do not recreate the trigger later by hand; rerun the correct Chaos Sea or Shining pending-bootstrap turn. The GM must not author, repair, or edit `pending_turn_snapshot.authority.json`; it is a client-owned hash/authority proof used only to explain lifecycle authority errors.

For freeform Chaos Sea Abode search, see example 23 before deciding whether the outcome is failure, distant signal, found Abode, or rare Guardian.

## Canonical State Reminders

- `shining_abode_state.json.availability` accepts canonical availability values such as `active` and `sealed_until_next_ascension`. Ordinary active Shining gameplay requires `availability = active`; `sealed_until_next_ascension` is a sealed historical state for post-return/foundation flows, not a mode where the GM may process Shining core/trade/political gameplay. `Shining Abode pending-bootstrap handoff` is not an `availability` value; it is derived from `preparedIncarnationPackage` being a non-null valid object.
- Shining Gates select/deselect/reroll are client-owned local draft mutations. The GM may read `selectedBlessingCardIds`, `shownBlessingCardIds`, `availableBlessingCards`, `rerollsRemaining`, and `nextCandidateCursor` as current context, but must not create receipts, pending files, or narrative-only state patches for those local operations.
- Shining resident faction persistence requires canonical Shining resident fields. A resident aligned to Shining state must remain an ascended resident with `ascensionState = "ascended"` and a `shiningFactionId` that resolves to an existing Shining faction. If the resident is not ascended, canonical normalization uses `ascensionState = "remained_in_chaos_sea"`, clears `shiningFactionId` and `residentRole`, and resets `factionLoyaltyLevel = 0`, `factionLoyaltyTier = alienated`, `factionRestlessness = 0`, and `factionRealignmentState = settled`. If the resident is ascended but `shiningFactionId` is missing or points at a missing Shining faction, runtime preserves `ascensionState = "ascended"` and treats the resident as ascended but unaffiliated: `shiningFactionId` and `residentRole` are cleared and the same loyalty/restlessness/realignment fields reset. If the resident is ascended and the faction exists, normalization derives or validates `residentRole`, `factionLoyaltyLevel`, `factionLoyaltyTier`, `factionRestlessness`, and `factionRealignmentState` from the current resident/faction state; do not preserve stale non-canonical `shiningAlignment` fields.
- `next_life_scenario_core.json` is a client-owned bootstrap input, not a GM-authored pending contract. Read it before Mortal bootstrap or Shining handoff reasoning if present. Treat `scenarioCoreAssertions[]` as non-contradictable hard facts; treat `candidateAssertions[]` as candidate-only until later confirmation. Do not edit, clear, or close this file in an afterlife response.
- Shining enum vocabulary for GM-authored faction/project/resident state:
  - `leadershipState`: `secure`, `contested`, `vacant`.
  - `leadership.headActorType`: `guardian`, `player_soul`, `resident`, `radiant_actor`; `player_soul` must use `headActorId = player_soul`, resident heads must reference an ascended resident, and radiant heads must reference `shiningPoliticalActors[].actorId`.
  - `shiningPoliticalActors[].politicalStatus`: `head`, `former_head`, `claimant`, `elder`, `retired`; exactly the current radiant head for a faction uses `currentFactionId=<factionId>` and `politicalStatus=head`.
  - Shining faction `originType`: `ascended_guardian`, `native_radiant`, `player_founded`.
  - `residentRole`: `archive_support`, `forge_support`, `social_support`, `resource_support`, `descent_support`.
  - `factionLoyaltyTier`: `alienated`, `uncertain`, `attached`, `devoted`, `steadfast`.
  - `factionRealignmentState`: `settled`, `wavering`, `restless`, `considering_realignment`, `ready_to_realign`.
  - `pending_shining_faction_realignments.json.requests[].realignmentMode`: `accepted_transfer`, `refused_transfer`, `departure_to_neutral`.
  - `factionRealignmentReceipts[].status`: `accepted`, `refused`, `withdrawn`, `departed_to_neutral`. `refused` is valid only for `refused_transfer`; `departure_to_neutral` closes as `departed_to_neutral` or `withdrawn`.
  - `projectArchetype`: `revelation`, `accord`, `provision`, `remembrance`, `refinement`, `passage`, `warding`, `subversion`.
  - `projects[].status`: `active`, `completed`, `retired`; completed/retired projects are retained as civic history, not deleted.
- Canonical Abode resident object fields used by roster, transfer, social, and Shining migration contracts:
  - Required identity/context: `residentId`, `residentName` or `displayName`, `residentKind`, `originType`, `isPresent`, `sourceGuardianId`, `sourceAbodeId`.
  - Required disposition/progression: `bondLevel`, `bondTier`, `abodeDevotionLevel`, `abodeDevotionTier`, `restlessness`, `migrationState`, `abodeDisposition`.
  - Optional but canonical when relevant: `formerGuardianId`, `formerAbodeId`, `linkedSoulRelicId`, `grantedRelicId`, `relatedQuestIds`, `historyLog`, `interactionLog`.
  - Shining-specific extension fields are allowed only for ascended residents: `ascensionState`, `shiningFactionId`, `residentRole`, `factionLoyaltyLevel`, `factionLoyaltyTier`, `factionRestlessness`, `factionRealignmentState`.
- Abode resident canonical disposition/devotion/migration vocabulary:
  - `abodeDisposition.powerSensitivity`, `communalOrientation`, and `stabilityNeed`: `low`, `medium`, `high`.
  - `abodeDisposition.migrationDisposition`: `rooted`, `selective`, `opportunistic`, `wandering`.
  - `abodeDevotionTier`: `alienated`, `uncertain`, `attached`, `devoted`, `steadfast`; derived from `abodeDevotionLevel` ranges `0..19`, `20..39`, `40..59`, `60..79`, `80..100`.
  - `migrationState`: `settled`, `wavering`, `restless`, `considering_departure`, `ready_to_transfer`; derive from devotion/restlessness: `ready_to_transfer` when devotion `<=15` and restlessness `>=70`, `considering_departure` when devotion `<=30` and restlessness `>=55`, `restless` when devotion `<=45` or restlessness `>=45`, `wavering` when devotion `<=60` or restlessness `>=30`, otherwise `settled`.
  - Founder-attraction and ordinary roster residents use the same Abode resident vocabulary; `guest` is never a valid `abodeDevotionTier`.
- Guardian project `assistAudit` / `sabotageAudit` scoring is deterministic. Every axis is an integer `0..2`. `assistScore = DomainRelevance + RiskOrCost + ScarcityOrUniqueness + DirectProjectImpact`; classifications are `<=2 not qualified as project assist`, `3..4 minor assist`, `5..6 meaningful assist`, `7+ major breakthrough`. With `auditKind=defense`, the same bands become `minor defensive help`, `meaningful protection`, and `major defensive breakthrough`, and the Abode Power reason routes as `rival_defense`.
- Guardian project sabotage uses `sabotageSeverityScore = HostileReach + ProjectExposure + DamageIntent + DamageAchieved + PlayerComplicity`; classifications are `<=2 nuisance`, `3..4 minor interference`, `5..7 major sabotage`, `8+ grand strike`. Abode Power deltas are assist `+1/+2/+3` for minor/meaningful/major and sabotage `-1/-2/-4` for minor/major/grand.
- Guardian project `soul_preparation` is next-life-only setup, not a current afterlife reward. Completed `soul_preparation` projects require `projectOutcomeAudit.preparationBudgetPoints` and `projectOutcomeAudit.preparationClaimPriorityBonus`; their `effectState` carries `targetIncarnation`, `preparationBudgetPointsGranted`, `preparationBudgetPointsSpent`, `preparationClaimPriorityBonusGranted`, and `consumedAtLifeStart`. Sabotaged `soul_preparation` projects require `projectOutcomeAudit.hostilePriorityTokensGranted`; their `effectState` carries `targetIncarnation`, `hostilePriorityTokensGranted`, `hostilePriorityTokensSpent`, and `consumedAtLifeStart`. The client consumes these fields during next-life Guardian correction resolution, so the GM must not spend them manually or leave them as perpetual bonuses.
- Guardian corrections are generated by the client at Mortal bootstrap from `next_life_scenario_core.json`, active Guardian state, rival claims, and temporary `soul_preparation` effects. The resulting `game_state/world/guardian_corrections.json` is a readable current-life journal, not a pending file. Matching Abode Power spends use `guardianPowerEvents.reasonType=correction_spend`, `sourceSurface=guardian_corrections`, `sourceId=<correctionId>`, and audit fields `correctionId`, `slotId`, `slotType`, `severity`, `claimStrength`, and `intent`; the event appears in Guardian/Abode Power chronology, but the GM does not author `guardian_corrections.json`.
- Shining blessing-card rarity order is `common -> uncommon -> rare -> epic -> legendary -> radiant`. Project-card bonuses upgrade only one step on that ladder; a tier-3 project that starts at `rare` becomes `epic`, not immediately `radiant`.
- Shining blessing-card token fields are strict everywhere they appear in `gates.*BlessingCards[]` and `preparedIncarnationPackage.selectedCards[]`: `sourceType = head | project | resident_descent`; `effectFamily = lore | social | resource | memory | descent | survival | relic | route`; `rarity = common | uncommon | rare | epic | legendary | radiant`.
- Shining forge blessing rerolls are a client-side `pendingShiningBlessingEffects.relicRefinementEntitlements` resource. The preview must show rerolls before/after; cancel preserves the entitlement, and the client consumes rerolls only after the final forge-request confirmation.
- Shining trade inventory must match the pending request's derived values: `tradeCycleId`, `generatedAtUtc`, `generationTradeTier`, `generationRarityCeiling` (`none`, `common`, `uncommon`, `rare`, `radiant`), `merchantProfile` (normally `shining_faction`), `serviceMultiplierSnapshot`, and item slots with positive `priceInFeathers` plus nested `relicData`. The player preview may show an `expectedTradeInventory` scaffold; it is an audit aid for the canonical `faction.tradeInventory`, not an extra response field.
- Shining trade `relicData.relicId` values are new Soul Relic identities. They must be unique inside one `tradeInventory.items[]` array and must not collide with any existing `soulRelics.equipped[]` or `soulRelics.stored[]` relic id; local purchase appends the relic and rejects overwrite/upsert collisions.
- Guardian trade inventory is stored on the Guardian, while Shining trade inventory is stored on the Shining faction. Do not swap these surfaces.
- Resident history log entries use canonical history identity fields such as `entryId`, `residentId`, `title`, `summary`, `revealedAtTurn`, and `revealedAtUtc`; receipts reference those entries by id when closing history requests.
- Actor journal entries require stable identity and timing. Use explicit entry ids/timestamps rather than anonymous narrative fragments.
- `preparedIncarnationPackage` is frozen state. During Shining handoff the GM preserves it; during `prepare_incarnation_package` the GM creates it with selected cards and draft version; only runtime clears it after successful bootstrap.
- Every `coreActionReceipts[]` entry that closes `pending_shining_abode_actions.json` must echo `quotedCostFeathers` and `quotedCostLightSparks` exactly from the request, including zero-cost actions. Forge receipts are stricter: `forge_relic.retune_property` echoes the exact `replacementProperty`, `forge_relic.uplift_rarity` echoes the exact `addedProperties`, and other forge actions must not invent those payload fields.
- `afterlifeArchiveUpdates` is a GM-authored Soul-state update surface only for exceptional/system archive rewards. Valid commands are `add` with a complete archive `entry` (`archiveId`, `entryType=lore_fragment|secret_record`, `title`, `summary`, `rarity`, `sourceLife`, `acquiredAtUtc`, optional source Guardian fields/tags/reservation) or `remove` with `archiveId`. Ordinary archive consultation/project-fuel closures use `archiveActionResolutions`, not improvised add/remove.
- Archive enum vocabulary is strict. Archive entries use `entryType = lore_fragment | secret_record`; `sourceKind = codex | system`; `reservation.kind = consultation | project_fuel`; pending archive requests use `requestedMode = consultation | project_fuel`; archive action resolutions use `status = accepted | rejected | cancelled`; project-fuel accepted results use `resultMode = project_work | pressure_relief`; consultation accepted outcomes may use `guaranteedArchiveQuestCount`, `questHookCount`, `specialQuestLineUnlocks`, `visibleRivalClueBonus`, and `archiveWarningTierBonus`. Every accepted archive resolution must preserve `requestId`, `archiveId`, requested mode, and the concrete reservation/source identity instead of summarizing them away.
- Derived notification triggers: `afterlife_notifications.json` is client-owned and derived from canonical state/receipts. The list below is the current exhaustive notification-type vocabulary; future runtime additions must update this matrix and coverage tests in the same change. The GM writes only the canonical receipt/state, never manual notification entries.
  - Guardian trade and quest/foundation: `guardian_trade_inventory_ready`, `guardian_trade_pending_attention`, `guardian_quest_available`, `player_guardian_foundation_resolved`.
  - Resident roster/direct rewards/manifestation/social pressure/transfer: `abode_residents_ready`, `abode_resident_quest_available`, `abode_resident_relic_granted`, `abode_resident_manifestation_ready`, `companion_imprint_manifestation_ready`, `abode_resident_talk_answered`, `abode_resident_history_revealed`, `abode_resident_history_refused`, `abode_resident_wavering`, `abode_resident_restless`, `abode_resident_considering_departure`, `abode_resident_transfer_pending`, `abode_resident_transfer_accepted`, `abode_resident_transfer_refused`, `abode_resident_transfer_departed`.
  - Archive actions: `archive_consultation_accepted`, `archive_consultation_rejected`, `archive_consultation_cancelled`, `archive_consultation_pending_attention`, `archive_project_fuel_accepted`, `archive_project_fuel_rejected`, `archive_project_fuel_cancelled`, `archive_project_fuel_pending_attention`.
  - Shining Abode: `shining_trade_inventory_ready`, `shining_core_action_resolved`, `shining_faction_founding_resolved`, `shining_faction_realignment_resolved`, `shining_faction_leadership_resolved`.
- `pendingShiningBlessingEffects` is runtime-created from `preparedIncarnationPackage.selectedCards` after Mortal bootstrap. Supported pending families/statuses include `memorySelection.status=pending_pre_turn_one_selection`, `resourceGrant.status=applied_at_bootstrap`, `relicRefinementEntitlements.status=pending_relic_entitlement`, `pendingSocialEffects.status=pending_first_relation_commit`, `pendingRouteEffects.status=pending_early_route_seed`, `pendingLoreEffects.status=pending_lore_insertion`, `pendingSurvivalEffects.status=pending_first_ruinous_failure`, and `pendingDescentEffects.status=pending_resident_descent`. Most families are later consumed through ordinary Mortal surfaces, but `relicRefinementEntitlements` is the explicit exception: Shining forge previews/requests can consume its rerolls/freeShape/freeRetune entitlement in Shining Abode, and the GM closes only the resulting `pending_shining_abode_actions.json` forge receipt/state contract. For `relicRefinementEntitlements`, terminal status is only `consumed` with `consumedAtTurn` and `consumedAtUtc`; do not write `expired` there. Deadline-based `expired` statuses with `expiredAtTurn`/`expiredAtUtc` apply only to supported deferred effect arrays such as route/lore/descent. `resourceGrant.grantId` and hidden `_shiningBootstrapResourceGrantIds` markers in player/inventory state are client-owned idempotency audit data; the GM must not author or spend them manually.
- `afterlife_notifications.json` is never a GM-authored proof surface.

## Example Coverage Map

| Need | Example |
|---|---|
| Chaos Sea catch-up, Guardian project, resident history | 1 |
| Active Shining core action, faction/trade/resident living world | 2 |
| Shining pending-bootstrap handoff | 3 |
| Guardian trade request | 4 |
| Archive consultation/project fuel | 5 |
| Ordinary Chaos Sea incarnation | 6 |
| Direct Chaos Sea gacha and Guardian-mediated gacha | 7 |
| Afterlife Abode Offering | 8 |
| Shining founding/realignment/leadership | 9 |
| Resident roster request | 10 |
| Resident transfer request | 11 |
| Guardian social interaction | 12 |
| Player-founded Guardian foundation | 13 |
| Shining core action variants | 14 |
| Freeform Guardian commands | 15 |
| Combined Chaos Sea scheduler + offering + action | 16 |
| Combined active Shining scheduler + core action + trade | 17 |
| Prepare package now, bootstrap later | 18 |
| Ordinary Chaos Sea living world without pending files | 19 |
| System Eternal Guardian attraction / `system_guardian_attraction.json` | 20 |
| Protected first afterlife turn / `afterlife_return_guard.json` | 21 |
| Direct resident action tags and pending-backed routing tags | 22 |
| Freeform Chaos Sea Abode search from `/обители` | 23 |
| `afterlifeArchiveUpdates`, derived notification triggers, post-bootstrap Shining blessing effects, and resident companion manifestation handoff notes | 22 |
